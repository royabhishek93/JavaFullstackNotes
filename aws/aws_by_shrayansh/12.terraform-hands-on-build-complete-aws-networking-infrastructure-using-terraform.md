# Terraform Hands-on | Build Complete AWS Networking Infrastructure using Terraform

**Video ID:** W-xmFGxsv2A  
**Source:** YouTube - AWS by Shrayansh  

---

Guys, welcome back. So, till now, we have learned a little bit about the Terraform files, Terraform architecture, different blocks. Okay? Now, what we will do in this is like whatever the infrastructure which we have created in our like network part, we have created VPC, uh public subnets, private subnet, route tables, internet gateway, NAT gateway, we have done the all this association. Now, we will create it via IAC, infrastructure as a code, and we will make use of Terraform. Okay? So, let's start. Again, I have already told you, technically, we can whatever the Terraform files we are going to create, we can define all in one single itself. But, for better organization and maintainability, we can create multiple files based on their purpose. Okay? First thing first, we have to set up the Terraform. Now, can you recall which block is required to set up our Terraform? The Terraform block. >> [snorts] >> Okay? So, this is the block, and I have uh kept it under provider.tf. Okay? So, here required version is greater than equals to one. So, this will define for Terraform core. Required_provider, like which provider I want to use? AWS. Okay? And this is the version for the AWS provider plugin I want. Okay? So, this is the Terraform block I have provided. The second thing is, okay, I wanted AWS, but how to connect it? So, I have provided a provider block also, provider AWS, and I provided a region. That Now, here if you see, I used a variable, variable.tf, one Terraform file I have created, variable.tf. The variable block I have given a name AWS_region, type is a string, and the value I have given AP-South-1. Then this is the region in which we have to create our resources. So, I have given the region where dot AWS underscore region. Okay? So, we have created Terraform block. We have created provider block. And this variable is we have created first. So, we have set up our Terraform. This is clear, right? This is basic, right? I haven't provided keys here like my access key, my token. That I will provide in the run time. Okay? Because if you provide it here, that you can say that if you push these files to get, it might get leak. So, it's not recommended, but you can. Because provider AWS tells like how to connect and we need keys and secret to connect. Now, the second is I'm creating this VPC. So, we have set up our Terraform like what version and all we needed. Now, VPC resource. So, if you remember, this is what we have done manually. Let me zoom it a bit. What we have provided? We have provided a CIDR. Okay? That's what we provided and the tag is like the name what we have given. The same name comes here. Okay? The same thing. Now, this is our VPC.tf. Resource, this is the keyword you can say. AWS underscore VPC. That is same. The which resource you want. Again, you can't use any name. I told you that from registry, Terraform registry, you can check that for VPC what is the name. So, AWS underscore VPC we can give. Yeah, you can give any name to this block. You can say that. So, I given main. Now, this block is start argument. So, this is our argument. So, you can from the Terraform registry itself check what all argument is possible. So, CIDR_block I want to give. See, here we have provided, right? CIDR_block and I want to give this. So, I am not hardcoding it here. I am putting it into variable. So, in the variable this is what we have first like region. Now, I am adding this. VPC variable name, you can give anything. VPC_CIDR. Type is a string and I wanted this. 192.168.0.0/23. The same which we have put here. I am just referring it here. var. Which variable name? VPC_CIDR. Okay? Now, that name which you wanted for this VPC which you are going to create. So, you can give tags, right? Optional. So, this is optional. You don't need it. Internally when you create resource, it automatically gets certain number like VPC-1A4B something something something. But for better readability for humans, we always give certain name through which we can easily identify. Okay? So, here the same like tags. Here also we have one argument tags. Okay? Within that, it is you can say that it is of like map. So, you have a block and then the key value. The name. You have a name, right? What value? Name equals to I am giving like this. So, here I am concatenating the string. So, my is like VPC- the certain name dynamically or not dynamically in the variable I have provided. -prod. Because for multiple resources, I am going to use this name. So, I have kept it into variable. My project_name. So, variable variable name, whatever you want to give. project underscore name. Type is a string and the value is concept by Shreyansh, let's say. So, I am just using it here. The dollar sign and var.project_name. Okay? So, within this is string it will get concatenated. So, it will become VPC {hyphen} concepts by Shreyansh {hyphen} prod. Okay, but this is optional. If you don't want, you can remove it. So, it's simply like resource. Which resource you want to create? AWS_VPC. You can give any name to this block, you can say. And arguments. I have given only one argument, that is CIDR block. This one. Okay, clear? So, now we have created a VP We have not created the VPC, we have set our expectation in our file that yes, this VPC with this CIDR will should exist. Currently, nothing get created. We haven't run it anything. We are just creating files. The second is like subnet we have to create. So, if you remember when creating manually what creating a subnet what we have done is we have tell like in which VPC this subnet we have to create. Which VPC? After that, it's name. Which AZ we have to create this subnet? And its CIDR block. Okay? Once done what we have done is we have set up like for the public for the public subnet we have set up auto assign public IPV4 address. So, that in future when we create a EC2 instance into public subnet, it should get a public IP address. I already explained the reason. Now, the same thing in our Terraform files. We are going to create a So, I given it subnet.tf. Any name you can give. I want to create a resource, so that's why resource keyword AWS_subnet. So, this is a keyword. You can't use anything again, so it should be AWS_subnet only. I've given the custom name. So, it's my name, public_1a. Okay? So, this is the name through which this block I can refer. So, public_1a. This is the name, custom name you can give. Now, arguments you can know from that Terraform registry itself what all arguments. So, but we can know that VPC_ID VPC_ID like which VPC we need to connect. So, VPC we have already created right here. Uh here we have created a Terraform. So, for our VPC how we can refer, this is the main name This is the name which we have given to this block, right? So, this is how we can refer it. ID is like AWS_VPC .main.id. Okay? So, how you can refer this block? AWS_VPC, this name. . this block main . the output ID. So, I already told you that in the Terraform registry you have argument and also in the below you will get like what data it return also. So, in that you will see that it return ID. So, I want to associate this subnet with this VPC. AWS_VPC the block VPC block main .id. So, this one we have set. CIDR_block So, I have given a CIDR range. var.public_subnet_cidr. Can you read it now? So, var. So, means in the variable uh dot tf file, you can say that the variable. So, for variables I've created variable.tf file. So, it will check the variable block and this variable name public_subnet_cidr. public_subnet_cidr. So, it will read this value. 192.168.0.0/25. This is what we have defined here. Okay? availability_zone. Again, see these arguments, I don't have to remember it. You can open that uh Terraform Registry. In the Terraform Registry, open AWS_subnet and check the arguments. Okay? And whatever the manually we have set, you can set it here also. availability_zone. Now, I'm setting this one, availability_zone. So, where dot az_1a. Now, you can read it. Okay, variable block az_1a. variable block az_1a, variable name. So, it will have use this value ap ap-south-1a. This name. ap-south-1a, your availability zone. Okay? And there is one more argument, map_public_ip_on_launch. So, I made it true. That's the same thing we have goes to the action and we have enabled it, right? That any EC2 instance created into this public subnet should get automatically public IP address. So, I made it true. And the same, the tag. If you have to give any name, tag, the name you can give any name. So, here I'm just giving that project name, which is like concept by Shreyansh hyphen public hyphen subnet hyphen 1a. So, this is optional, but you can give any name to this subnet which you are creating. Which you will show see in the console. Okay. So, public subnet we created. Similarly, we can create private subnet. Only difference is in private subnet we don't want this. We want We don't want public IP for our EC2. It should be false. So, the same thing. Resource keyword AWS {underscore} subnet keyword we can't change it. It should be like if you have to create subnet, it should be like this AWS {underscore} subnet. You can give any name to this block so that you can refer it lately. Private {underscore} one B I've given. VPC ID AWS {underscore} VPC.main.id. So, like which VPC this subnet has to be created in into this. Its CIDR block private {underscore} subnet {underscore} CIDR, this one. So, this is the block. Which availability zone? var.az_1b one B. So, AP {hyphen} south {hyphen} one B. So, in this AZ this subnet will get created. Map public IP on launch false. We don't want public IP at And the tag the name which you want to create. Clear? So VPC we have set into our file, public subnet private subnet. Okay? Now, internet gateway. So, manually if you remember, let me zoom it a bit what we have did manually. So, the name from the tags and we have attached to the VPC. Okay, we have attached to the specific VPC. That's all. So, the same thing we have to do in the code also. internet_gateway.tf You can give any name. Resource is a keyword. AWS {hyphen} internet {hyphen} gateway is the resource which you want to create. So, this resource is already present there. The same name you have to use. In the Terraform registry you can check. The name of the block. So, I've given main. Okay? Don't get confused that, "Hey, I also given the main name uh to this VPC also." So, will they not conflict? No. Anytime you have have to access this resource, let's say from the code, you will use AWS_VPC.main. Okay? So, that's why there is never a conflict. So, if I have to use this AWS_internet_gateway, this one, this block, so I will use AWS_internet_gateway.main.something. Got it? So, resource the resource which we have to create AWS_internet_gateway, the name which you want to give to this block, VPC ID. Okay? Like which VPC we have to connect to? We have provided here attached to VPC. So, VPC_ID is like AWS_VPC.main.id. Okay? So, this internet gateway belong attached to this VPC. And the name to this internet gateway, you have to give like here we have provided tags equals to name equals to So, this will come from over where? Concept by Shash {hyphen} IGW. So, this name we will see on the console. Okay? So, we have set up a not set up, we have defined in our code that this is the internet gateway. Now, NAT gateway. So, during manual console, what we have done is we have provided name. We have provided a subnet. In which subnet this NAT gateway should be present, which should be in public subnet only, right? Allocate elastic IP because we don't want this IP address to be changed. So, we want some permanent IP for this, permanent public IP for this. So, I've already explained the reason also. In the when we created this. So, we want a elastic IP and the tag name. So, now the same thing. net gateway.tf The first is elastic IP resource. Elastic IP is also resource. I told you that when we create this, it is chargeable. Okay? So, it's also a resource. So, resource aws_eip elastic IP. You can give any name. I given just net. domain equals to VPC. So, generally you can say that there were two values before that, but now this is the only one value lately we use it. Okay? It's just like elastic IP is like permanent IP address. So, what all you can use within a VPC. That's what it's used like it's a scope is within a VPC. I would say that. Okay? So, within a VPC if you have resources, this elastic IP can be used it for that. So, generally it has only one value VPC. So, we have created one resource. aws_eip elastic IP. This name should not be like we can use anything. You it's a resource name. What is the block name you want to give? You can give any name. Like I'm just giving NAT. And this is the argument domain equals to VPC. Like all the resources within a VPC, I should be able to assign EIP. So, this one I have created. Now resource aws_net_gateway. I'm creating a net gateway. Okay, this is the resource name. It should be used this only. You can check from a Terraform registry. The any name you want to give to this block. So, I'm giving main. Now the arguments. allocation ID. allocation ID is this only. So, this allocation ID, right? So, allocation ID is like aws_eip.net Whatever the ID it will return .id Okay, so we have assigned this now. Subnet ID this network gateway present into which subnet? So generally it should be public only right? So there is we have provided the subnet. So that's it. So AWS_subnet.public_1a.id. So what is our public subnet which we have created? So public subnet yeah. AWS_subnet.public_1a.id Okay, attach with that ID. So here it goes. So this net gateway we have attached with the public subnet. And the tag name is optional but yeah. Concept by Shreyansh hyphen net hyphen gateway will be shown into the console. Okay, and I have just mentioned here that depends on So this net hyphen gateway or this net gateway which we are creating it depends on this is also one more argument. Generally if you remember you can question this that hey why you are specifically adding depends on it's an array. What all things it depend on? I am just telling it depend on AWS_internet_gateway resource. main So it depend on this. I'm telling this AWS_internet_gateway.main So means this has to be created first and then net gateway has to be created. So this is the block right? So I'm what I'm saying is so depends on like this has to be created this should exist first then only net gateway. Now you can question that hey Shreyansh you told me that Terraform is declarative. We should not tell like which should create first which should second. It should already create a dependency graph. See, here if you see there is no explicitly dependency into like, hey does it has any So, it has a dependency with subnet. Yes, public subnet should be present, then only you can have NAT Gateway. Okay? So, it has like elastic IP should be present, then you can have put this value. So, it can create the graph. But, there is no way you can tell that, hey, Internet Gateway should be present, and then only you can have a NAT Gateway. There is no dependency, but you can say that AWS has this internally. And if you try to create NAT Gateway first before Internet Gateway generally it will show that create Internet Gateway first. That is the only thing way, or you can say that that's the only reason we have added depends on because between Internet Gateway network Gateway there is no explicitly dependency here. But, AWS has in some internal dependency. And dependency graph, I told you right, Terraform core has a dependent It creates a dependency graph. So, it doesn't know about internal dependency of AWS, but we know that. Like, you can't create NAT Gateway without Internet Gateway. So, that's why I had to written depends on array. Like, you can have multiple dependency. That's why an array, but I have only one that Internet Gateway should be present before you create a NAT Gateway. Clear? So, now we have VPC, we have public net, private subnet, we have file for this. Now, all we have left is this route tables. >> [clears throat] >> Okay. So, we need to have one route table for public subnet. And this route table should have one route which is for Internet Gateway. This private subnet should have one private route table and that have one route to net gateway. So that we have to make. So from console, how we used to create a public route table? So we have a route table first. Then and we click add route and we add one route for the internet gateway. So any traffic to 0.0.0 means if any traffic outside goes to internet gateway. Okay, so we create this entry. Then we attach it with the subnet. Then what we will do is we will goes to this action edit subnet association and we connect this route table with this subnet. We have already seen that. Similarly for private also. We in the private, what we have done is we first add any internet traffic goes to net gateway and then this route private route table attach it with the private subnet. The same thing now we have to write. Okay? Now same thing now we have to write. So here if you see, I am writing a public route table first. So resource [snorts] AWS underscore route table. So first we have to create a table itself. So we are saying a table. So resource keyword AWS underscore route underscore table. Okay? Means we want to create a table. Okay, this name should be specific. We can't give any name. You can give this block a name public. Okay? So this is my public. So this table should exist into which VPC? So here I'm just telling that VPC so public. VPC underscore ID equals to AWS underscore VPC dot main dot ID. Okay, like we have already seen that this should be within this particular VPC. Okay. Now we have in this table we have to add routes, multiple routes. So, resource AWS_route means you are adding a route. So, see for each, you can say that a route you have to have this. Because consider that each is one individual resource. Each route is a resource. So, I'm creating a resource AWS_route now. At top, I created a table, route table. I gave it a name, let's say this block, I gave it a name public. And I have just add that this table which I'm creating is within this VPC, okay? Now, within this table, I have to create one route. So, first resource, this route, AWS_route, this is a resource. You can use any name, this one. You can give any name to this, like whatever the route you are writing. So, I'm writing public internet access. You can give any name. Okay, this is just a custom name. We can give any name to this block. Now, the route which I am writing, it should be like for this table. Okay, this route which I am writing, it should be present for this table. So, route_table_id. So, this is an argument. I am not writing this name by own. So, we have to check the arguments for AWS_route. Okay, resource and then you can see that okay, this is mandatory, we have to give that. route_table_id So, this route which you are creating it's belongs to which table? So, it belongs to AWS_route_table.public.id. So, dot ID is like a return type you get. Okay, so means I have attached this route to this table. Now, destination_cidr_block destination block. So, that's what like you You say that the destination block. While creation, we put right destination block. So, any traffic which goes to 0.0.0.0 gateway ID is like AWS internet gateway. main.id. The gateway which we wanted to have. It should goes to that. So, in simple term, what we have done is we have created one table and within that table, we have added one row. Like this is the destination and it should goes to this gateway ID. Okay, so we have done this. We have we have created one table and we have added this row. Now, we have to associate with the subnet public subnet because public subnet should have this route. So, resource AWS route table association. So, this is now the resource I'm creating, association one. You can't use any name. The name which I've given is public one A. Any name you can give to this block. The first argument is subnet ID. So, this association like it's like a table versus subnet association. Which table? Route table ID. AWS route table .public.id. So, this table associated with this subnet. Subnet ID is AWS subnet.public one. public one A. ID. So, the public subnet which we have created. Clear, right? So, here public one A. ID. So, it will connect with this one. Similarly, the private. The first create a table new table. AWS resource AWS route table. So, this is the resource. I give it a block name private. Now, I have to create one route within it. Okay. So, resource AWS route. You give any name to this route. Attach this route with the table first. So, I'm attaching it route table ID AWS route table. private.id. So, now this route which I'm going to create is associated with this table. Now, in that destination CIDR block is any traffic which going outside, it should goes to net gateway ID and this one. AWS net gateway resource like which block. dot main.id. Now, we have to Now, this table is created. All the routes are created. Now, we have to attach this with the subnet. So, resource AWS route table association. Give any name to this block. Route table ID AWS route table.private.id. So, attach this table with this subnet ID AWS subnet private 1b.id. So, with private subnet we have attached it. Okay. So, now we have done this. We have already attached this table with the private subnet. Public route table with the public subnet. It has the gateway. This is done. It has This is done. Okay. And there is one default route table. I have also created this default route table also which is of no use. I already told you it is no one is using this. So, we have created this now. All done. Whatever the infrastructure we have created, we have created everything. We have created VPC, public subnet, private subnet, net gateway, route to public route table, private private route table, all this association we have done. Internet gateway all done. Now, only thing is we have to run this. So, we have to execute this TF files. How to execute it? First, install Terraform. Okay? Now, we will also execute it. So, currently I have already installed it. So, we can have Terraform version. So, here if you see, I already have this version. Okay? Okay, so you have to check like is Terraform already installed or not? So, if not, if you are using Mac OS, you can download from the homebrew. Like, this is the command. It will install the latest stable version. Or if you are on Windows, you can download it from this link. Okay? Make sure that in the Terraform block in the Terraform block, if you remember whatever the version you have written here, greater than equals to 1.0. So, make sure that when you download it, you have greater than equals to you maintain that uh thing. Otherwise, validation will get failed. Okay? Once you run it, validation will get failed. So, make sure that you download the version greater than equals to what we have defined into the block uh Terraform block. Okay, so once done, you can check Terraform uh Terraform version. Certain version will come. So, means it's done. We can run this Terraform files. But, one thing important is that we need a secret to connect to the AWS. We haven't provided into the provider. I told you. It's never encouraged to add access key and token here. Otherwise, how we will connect to the AWS? So, we can always put into environment variables. Okay? So, we can set the AWS access key ID into the environment variable. Similarly, AWS secret access key into the environment variable. Our key and secret we can put into environment variables. Okay? So, now let's say I am going to security credentials. Let me delete it. So, see this is just for testing purpose. I am showing it here. But, you know that it never need to be shared. Because after that I will delete it. So, I'm not encouraging that you keep the same access ID. So, I'm creating the access key. I understand. Create access key. So, I have access key and I have a secret. So, we can have use it here like So, we can provide like this. So, this is my access key. Okay? So, I have run this. I have provided my access key and the secret key. So, this is set into the environment variable. Now, my provider can know that whenever my AWS SDK need to go. So, AWS SDK will you can say that check the because it need to connect with the AWS, right? So, you can say that it first check you can say that provider block. Let me get So, Terraform core From core it goes to the AWS plugin. So, Terraform core provide all the details. So, Terraform core need to provide the key and secret. Correct? Terraform core has to provide it. So, now here if you see in the provider block we have not provided. Correct? So, there is certain chain where Terraform core look for this credential. The first highest priority is this provider block itself. In the provider block, it check the access key and secret. If it not find, the second priority is you can say that the environment variable. So, in environment variable, it find it, it pass it to the AWS, which AWS will use it to connect. Okay, so we have provided into the environment variable. Now, the thing third is we have to perform Terraform init, initialize. So, means initialize means this one. This in the provider.tf, we have Terraform block and the provider block. So, now it will like here. The Terraform block, which AWS provider plugin it has to download. So, it will do that. And also, it will make sure that whichever the Terraform you are using, it has this version greater than equals to this version. So, okay? So, that's when what happen when you do Terraform init. Validate the Terraform version, download AWS provider plugin. So, Terraform init. Okay, so now it is initializing. So, it is downloading uh your AWS plugin, whatever you have a specified see here. It has whatever we have a specified, it is downloading that one. And also validating that the Terraform version which you are using, it's the same which you have provided into the block. Now, this is optional. You can review the plan. So, but currently, we are creating for the first time. See here, if you see in our AWS, currently, we have nothing. So, we can check let's say VPC. Our VPC, currently no VPC exists. This is the default one. Uh currently, this is the default one. No subnet. This is the default one. So, currently we haven't so NAT gateways. So, here if you see no NAT gateways found, nothing. Okay? So, currently we haven't set up anything. So, I we don't have to create a plan. So, it is an optional. Now, we can directly apply the Terraform. Terraform apply. Terraform apply. Now, what it will do Now, here if you see This is the folder where we have created this internet gateway.tf, so all this thing. NAT gateway.tf, provider.tf, route table.tf, subnet.tf. This is all what we have created, right? TF estate is like after you do Terraform apply, it will create after that one. I told you I will show you also. Okay? So, now here if you see Yes, I will accept. It will start creating it. So, now it is creating an infrastructure for us. It will take certain time. Your VPC, let me refresh. Is it created yet? Huh. See, VPC-concept-by-Shreyansh-prod. This is the tag name which we have given, right? So, it has created the VPC. Okay? Subnets, it created the two subnets, public and private subnet which we have given in the tag name. Route table, it has created till now private, but if you refresh, it will also create public subnet also. Till now it's not created, but still it will create. Internet gateway, it has created. Concept-by-Shreyansh-IGW which we have created. NAT gateway, it also created. Elastic IP, it also have one. So, public subnet is public subnet also came. So, now if you if you go within the VPC, click your one. See. We have in within this VPC, we have public subnet within this AZ, private subnet within this AZ, public subnet has this route table. This route table connects to the internet gateway. This private subnet connect with this route table. This route table internally connects to the internet gateway. Okay? So, here if you see, this private route table do not connect with this gateway. Let me check. Let's refresh it. Maybe after it's might be still working. Is it completed or is it it is still creating? So, there should be a link present from this over private route table. We have done this association, right? So, it should be connecting this to the net gateway. Huh. So, it is also connecting with the net gateway. Cool. So, over it is completed. Yeah, it is completed. So, our infrastructure is completed now. That's the same infrastructure we created manually. Now, we have created through infrastructure. Now, here if you see, open the folder which you in which you have kept all these files which we have created. There you will see that Terraform.tfstate. I told you this is what the state manager is. If you open with See. Let me zoom it a bit. It has all the information. Whatever the resource we have created, let's say it has created VPC. Okay? Now, attributes is like the output you can say that. We got an ARN number. Like this is the VPC block uh CIDR block. What is the ID? It will also have one ID. This is the ID. Okay? So, it has all the information. Now, let's say in future in future, if uh vpc.tf open with text Okay? So, you have changed something in this CIDR block. Let's say I will say that 10.0.something something something that /23. You have changed it. What it will do? Will it blindly create? No. It will first check. It will first check its own .tf state. Okay? So, it will first check its own. Okay, this is the AWS VPC main. Okay, that's the same name. Yeah, it's know that this is a block. Now, it will say that any update happen. So, it will say it will check that okay the one you wanted is for this AWS _vpc_main CIDR block is 10.0.0.0/23, but what I have is I have is CIDR block is this. Something got changed. Now, it will check using this ID, ARN number, and this ID, it query the AWS resource and check it and see what is present within that. Okay? See, it do not blindly rely on this one. It double-check hey, what previously run and what there in the console AWS resource is actually the same. So, it will say that yes in AWS console also VPC I have this uh this detail only. So, this is our VPC, right? CIDR 198.168.0.0/23. Okay? So, it will check. Yes, this got matched. But, now expectation is that we this CIDR block should be different. So, what it will do it will the existing VPC and it will create new for us. But, let's say if it is nothing, it is again same. 198.0.0.0/23. Nothing got changed, let's say. For example, nothing got changed. It will say that, "Okay, if you run it, Terraform apply again, it will say that, "Okay, you want VPC, the block is main, the CIDR block is this. What previously was this VPC main, I have the same. And in the console also, when it find out this, describe this, using ARN resource and this VPC ID. See, 3180. So, so it will have this 3180, right? So, it will see that, "Okay, you have the same VPC. 3180, the same CIDR block is also present into the console, so nothing got changed, so it won't do anything." Clear? Now, another advantage of this Terraform is like earlier, when we have created it, we have to delete it manually, right? Now, we don't have to manually delete it here. I can run the command Terraform destroy. Now, it will ask me, "Hey, are you sure?" Yes, delete it. Now, all the infrastructure which I created, it will delete it for me. I don't have to manually go because elastic IPs chargeable, NAT gateways chargeable. Uh so, it will automatically now delete. See, deleting. So, it is deleting now. That is another advantage of using Terraform. Okay? So, clear? And yes, always make sure that your key and secret I never encourage that you disclose your key and secret here. So, always delete it if it got exposed. Okay? Okay, guys. Thank you and we'll see you in the next video. Bye.
