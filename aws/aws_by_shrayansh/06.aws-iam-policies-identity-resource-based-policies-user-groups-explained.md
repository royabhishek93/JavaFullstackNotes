# AWS IAM Policies (Identity & Resource-Based Policies) & User Groups Explained

**Video ID:** Hpkb88J7b9o  
**Source:** YouTube - AWS by Shrayansh  

---

start with IAM policy. So, in the IAM user part, we have already you can say that authentication part is done. Like we have already AWS knows that who we are. Through username password or through keys or secret key, access key and the secret key. Okay? Now, the next question is are you allowed to do what you're trying to do? So, I logged in. I logged in as Shreyansh. That's okay. Authentication is done. But, are you allowed to do what you're trying to do? Who will answer this? So, IAM policy helps to answer this. So, it is of two type. Policy is of two type. Generally, if you read it, okay? Mostly discussed is identity. And very less discussed about resource-based policy, but I equally find that this is also very important. Why it is very important, I will also show you. Identity-based policy and resource-based policy. Both is same. Generally, both is same. Just perceptive is different. What is the perceptive? Let's say you are a user. Okay? And this is your resource. So, like resource user we have created is Shreyansh. So, this is Shreyansh user. And this is my resource, let's say S3 bucket. Identity-based policy, you can say that this holds the permission. So, let's say this is the permission. So, this user holds. Okay? So, this is identity-based. In the resource-based, S3 bucket, our resource holds the permission. That's the only difference. But, both does the same thing. So, when user try to access the S3, Okay? So, S3 will ask to IAM that, "Hey, this is the user. This user tried to perform get operation Okay? On this S3 bucket 1 2 3 is specified bucket. Allow or not allow?" So, IAM user has access to the permission which is attached to this user. Which is attached to this user. So, it will able to tell allow or not allow. And the same thing when user calls to S3, S3 goes to IAM, it will ask the same thing. The user trying to access this resource. So, now in this case user trying to do perform get operation on this resource S3 bucket 1 2 3. Now, the permissions are attached with this resource. So, still IAM able to find that. And it will see that yes, this user is allowed or not. So, both answer the same thing, but the perceptive is different. Identity based, you can say that perceptive from the user. Resource based is perceptive from the resource. But, things will get more clear. So, identity based policy. Policy is attached with IAM user. Okay? IAM entity, your user, group, role. So, now here if you see this is the policy. Policy document generally we write in JSON. So, here if you see, understand this policy very clear. So, there are so many other variables you can add it into policy, but I tried to keep it minimal for better understanding. The first thing is version. Version is generally always the same. This is not a date. Okay? So, this 17 uh 2012 10 17 is the version of this policy language. So, generally you have to keep it same. Then we have a statement, array of rules. So, this is an array of you can say that rules. So, you can this is rule one. You can have more rules. Okay? Now, in each rule what we have is let's say before the effect, let's say action. The action is like the service and operation, the API. So, this generally answer what? The user wants to access what? So, the service and its API. So, see S3 service can have hundred of API. Okay? So, which is specific for this S3 service, which is specific API user is interested in? That's we put it here. Service API operation. So, currently I have it only one. So, you can have like this, but if you have more operations or more API you want to give access, you can have an array also. Action and then array and just put a comma separated. Service {comma} get object. Service {comma} whatever the list bucket. This is the considered as an API. Okay? So, our policy, very important, the rule which first it has is answer what? What user wanted to access? Like exact which API for this service this API one user is interested in. Okay? Now, resource. Which resource? So, this S3 what are its resources like bucket one. So, we will cover bucket and all when we see S3, but understand that S3 is just a service. Within the service, there are resources. Let's say bucket one, which is my prod. So, here all the prod files goes in. So, here I have another bucket Dave, which is all my development goes in. And I have another sandbox. So, all the files which are related to sandbox environment will go in. So, whether you want to access this service, this API, which resource you want it on this prod, on this day, on this sandbox, or not all. So, that is answered by your second the resource part. So, in very simple term, the policy holds the information that what API user want to access on which resource. And then final the verdict, whether allow or deny. Okay? So, let's say here again in the resource also, here I have only one, so that's why I have put here. If you want If you have an array, you can use like resource array and then comma separated resources. Let's say I want to give only on bucket A and bucket B, I want to give the permission, you can give like this. And another good part is this policy accepts uh wildcard. So, now let's say S3 service has 100 of API. You can't just have like comma comma like all the API. You can just simply put a star. So, on S3 service all the APIs I am going to allow. Okay? Similarly, action you can also put simply star. All services, all operations. Okay? Similarly, on resource you can put simply star. All resources you are granting the access. Okay? So, wildcard is also allowed. But, in very short, all I just wanted to tell that in the policy, what it has is first action which is like your what APIs user are interested in. Second resource. Okay? On whatever the service So, here action tells also the service. On whatever the service user is asking for any specific resource they are interested in. Okay? So, and the verdict. Allow or deny. So, now once this policy is created in the identity-based policy, we have to attach it to the user. We have to attach it to the user. Okay? In the resource-based policy, now let's come to resource-based policy. Resource-based policy is also very similar, but only one additional thing is required. So, we have a user. We have a let's say S3 service. And in the S3 service, we have one bucket prod. Okay? We have bucket prod. Now, let's create its policy. This is the policy document for we are creating for the resource S3. The same thing. We have a version which need to be the same. A statement which is nothing but an array of rules. So, this is our currently it has only one rule. Now, in the rule, I told you two important part. One is action. The action tells for which service what specific API you are interested in. So, S3 service get object API I am interested in. Okay? So, I have written action. Now, once you have got this API access, on which resource you want to perform this operation. You might have prod, you might have develop, you might have sandbox. You have might have different resources, different bucket. In case of S3, you have different buckets. On which a specific resource you wanted to do? So, you specify the resource. That, hey, I am interested in this particular resource. Okay. So, that's also done. And the verdict, these three things very similar to what identity we have also seen, allow or deny. Now, in the resource-based, one more thing is principle. Okay? Which user should we allow? Because, see, understand that in identity-based policy, the policy is attached with the user. So, we have information about action, like what API, okay? What service, what service, what API, on which a specific resource, and which user. So, this policy is attached with user. Now, in case of resource, policy is not not attached with user. So, we need to tell like what all users are allowed to use this policy. You can say that. So, here, we have added into this principle. So, that's why the same action, resource, the verdict, and the fourth is principle. In the principle, you can tell the specific user which are allowed to access this. Okay? So, the same thing I mentioned here. So, since policy is now attached to resource, resource must specify who, the who part. That is nothing but your principle. Who is allowed to access me? The principle. What operation they can perform? The action. And on which resource? This third part. Okay? So, principal answer the who part. So, principal can be your user, your role, different account, different AWS service also. Then? In the resource based entity, we create a policy. But only thing is we it has to answer all three. The who part, the principal. Who is allowed to access this resource? What operation they can perform through action? On which resource? A specific resource we have to tell. Okay? Now, let's create that. Now, let's create that and see it. So, theoretically now we understood what does identity based what is resource based. Now, let's see. So, in the previous, I have shown that we have created this user Shreyansh. We have created this user Shreyansh. And when we try to access the S3 bucket, we say that we don't have any permission. Now, let's say we have to give the permission. So, again, now let's go to our root user account. So, this is our root user. So, here if you see, so we have created the Shreyansh. Okay? So, now the first thing we have to do is create a policy. So, currently first I'm showing you identity based policy. This. Identity based policy. So, let's create let's go to the IAM. Let me zoom it a bit. IAM. Within the IAM, click on this policy part. Policies. So, see, AWS already provide us so many policies. And we can search for it if anything we found. But for our better understanding, we will write our own policy. So, create on policy. The JSON. Okay, now we write our own policy. So, our agenda is that the policy which we have to write is give full permission of S3 bucket. So, we will do that. So, now here if you see version, we have to keep it same. See, SID is you can say that a statement ID. The You can have many rules, right? This is the rule one in this statement. You can have many rule. So, you can say that it's a unique statement name. Like which rule allow it specifically. Okay. So, it is not mandatory. It can We can remove it for now. So, we let's focus on the important part. So, we need to provide an action. We need to provide a resource. I don't want an array. I just want have one. So, action is like what operation I am allowing on this policy. S3 service all all operation. Okay. So, S3 So, here let me zoom it a bit. So, I'm doing S3 colon a star. I told you, right? Wildcard is allowed. So, on S3 service all APIs, whatever it has, I want to allow it. On which specific resource? Whatever. Like all resources. So, let's say whatever the resources are there, I want to allow it. Okay. I don't want that a specific resource for now. So, it's like a complete S3 access I want to give. S3 has, let's say, 100 of APIs. All APIs allowed on all the resources. No matter how many buckets it has, whatever the bucket files it has, everything, every access I need to provide. Okay. So, and the verdict, allow. Yes, I I to allow it. That's Next, click on next. Give the policy meaningful name and I'm giving the name, let's say allow full S3 access. So, this is the policy name. And here if you see full access I'm giving on all resources. That's what we have selected, right? Through JSON. Uh access level full access. So, here. And resource all resources we have used the wildcard. So, that's why all resources. So, create policy. So, now our policy is created. Now, let's search it. Allow full. This is the our policy is created. Now, we have to in the identity policy we have to attach it with the user. So, our user is Shreyansh. So, click on the user. Okay? Now, here if you see in this permission, we can add this. Okay, create. So, you can either create policy from within this or we have already created, we can just add it. Add permission. Click on this. Add attach policy directly. Search your policy. Allow full. This is the policy I want to allow to the user Shreyansh. Okay? Add permission. So, we have added this permission. Allow full S3 access. Okay? With this user Shreyansh. Now, this IAM user here in the incognito window here if you see, Shreyansh is already logged in. And earlier this S3 bucket I'm getting this error, access denied. Shall I refresh it now? Let's see that. I'm refreshing it. No issues. Okay? I have already created one bucket, that's why it is showing. Uh but now see that the error has gone. Now, even through So, let's say my code application is already running, okay, from the previous IAM user we have created this, right? And through Postman I showed you that we were getting "Shreyansh is not authorized." Now, let's hit it again. Okay, this access ID we have to create the access ID. We deleted it the access ID, right? So, let me create this access ID again for this security credentials. Generate API key. Uh not API key, access key. Create access key. Create access key. So, update our code. And update the secret key. Okay. Now, stop the server and start the server again. Now, hit it again. So, here success. So, now we are not seeing access denied. And again, I told you whenever you see that your secret key is uh uh like exposed or something, you can always delete it also. So, security credentials. This is your access key. So, you can first deactivate and then delete. Okay? So, always always keep your credentials safe. Access ID secret key keeps it safe. And never stored it into your application.properties. Put it into vault. But here, now what we have seen is that to a particular user, Shreyansh, now we have uh granted this permission. S3 access and now it able to use it properly. So, that's what we have done. We have created the policy. The same thing I have shown here. Okay, so I am goes to the IAM policy, create policy. Clicked on the JSON. In the JSON, I created a policy. And the policy I already told you version, a statement, action, service S3, all its APIs, resource all bucket, no matter what. And this particular bucket, if it is any files, complete access I want to give. So, all resources. And give the proper policy name. Create the policy. Now, once our policy is created, we have to attach it to the user. So, in the user, IAM user goes to the particular user, let's say Shreyansh. And here, we can add permission. Okay? You can add permission. Here, attach policy directly. Search your policy and add it. So, earlier when we were logging as Shreyansh, you were seeing that we were getting access denied. When we refresh it, no issue gone. Okay? Similarly from Postman also, earlier it was an error, now success. Okay? Now, similarly we can do the resource-based policy also. One thing before I start resource-based policy is that not all resources, like resource-based policy is like not all resources we have to go with resource-based policy. Only for certain resources support this. As when we work with different, let's say DynamoDB, S3, different resources, we will get to know that, this support resource base policy or not. But at this point of time, all we want to know is that we can create policy and we can attach policy with user, then it's an identity based policy. If we attach policy with a resource, that's a resource based policy. And how to attach with the resource? Let's see that. So let's first delete the policy what we have attached to the user because we're going to test with again with the resource base. So we have to make sure that over these are all clear. So we have to go to the root account. IAM policies allow. So let's delete this. I am deleting this policy itself. Policy deleted. And in the user, double check that permission policy is zero. No policy is attached. So we are good. And the same thing is that in the Shreyansh, now if you go to the S3 and try to access it, we should be able to get permission denied because we have deleted the policy. Now in resource based policy, so what we have to do is we have to first create a resource uh then goes to S3 and click create bucket. So now, here what we are doing is in the resource based policy, we have to first create a resource. Okay? So there could be resource like DynamoDB is your one resource. And like similarly S3 is your resource. So I am creating an S3. So I'm going to the S3. Okay? So here if you see, create bucket. So I have already have one bucket created, let me delete it also. So let me delete it. We will create fresh. This [snorts] bucket is not empty. Empty bucket first. Let's empty it first. So, now it is empty. And now we will delete it also. The complete bucket we will delete it. >> [clears throat] >> Okay? So, now we don't have any bucket. So, now first thing we are just create a bucket. So, very minimal things we are using it. So, keep everything all the settings same. We don't have to change anything. So, we just created create a bucket. Okay? And just provide the bucket name. Rest all keep everything same. So, let's me see the bucket name is let's say your uh uh give concepts by SJ prod. This is my prod. Okay? So, this is the name I given. Rest everything I'm keeping the same. Create bucket. Okay? Bucket name must not contain uppercase letter. So, let's see. concepts by Shreyansh prod. Okay? Create bucket. So, my bucket is created. One thing is done. Okay? So, now here if you see that open the newly created bucket. Now, this is you can say that it's my resource. This bucket is my resource. S3 is a service. Within that S3 service, this is my resource. Okay? So, now let's go to the bucket. Okay? So, the old one, let me delete it. Okay, so I have only one bucket which we have created. Now, what you are doing is we are going to have a resource-based policy, resource-based policy. So, now this is my resource. Okay? So, now here if you go to the permission, go to the permission. Now, in this permission, you will see that bucket policy. So, currently no policy is present. Just click on edit. Okay? Now, we have to create a policy here. So, now the same thing. Bucket policy, go to the permission. In the bucket policy, click on edit. Now, you have to add the policy JSON document. Whatever the JSON policy, we have to add it here. So, the same thing. You have to write it. Okay? The first thing is version. Version is always the same. So, just try to let's write it. Version. It's capital V. It's a 2012 10 17. So, this is version, {comma} and then we have an array of rules, which is nothing but a statement. So, we have a statement. Okay? So, now here what we're going to have is that rules, different rules we have to provide. So, so in the area of a statement, our first rule. So, we what we can have is action. We can have is resource. We can have is the verdict. The whether allow it, not allow it, right? So, effect. And also one more thing it has is principal. Who to allow it? Okay? So, one thing, action, is like full access to S3. Now, one thing, very important part. So, in the next step, I'm just opening this resource. Okay? Metadata properties, huh. So, concept by ShreyanshProd, this is the bucket which we have created, right? This is the resource. So, this resource has this name. So, now what we are creating? We are attaching the policy to a resource. We are attaching the policy to a resource. Okay? So, now here if you see, this resource is your actual which you are attaching with. Okay? So, here my resource is this, concept by Shreyansh-Prod. This is the bucket I've created. So, this is the Amazon resource name, ARN number. So, I've copied this. Okay? So, let's say I am giving area of permission. So, this. And also I've given {slash} {asterisk}. What does it mean now? So, here I am giving access to this bucket and all its {slash} {asterisk} is like all its internal objects also. So, see, there are many operations you can perform on the bucket level, high level. Like when it created uh what uh let's say what region this bucket belongs to. Okay? So, what is the name of this bucket? There are many things you can invoke on this bucket. But this bucket can have multiple files. Okay? Understand that. You have a bucket bucket one. Now within that bucket, you can have let's say file one, file two, file three. So, it is possible that you want to give only permission for this specific file. Okay? So, what I have done is I have done bucket one {slash} asterisk. Hey, all the permission on all the resources which this bucket has internally. And I also give bucket, only bucket. So, on this bucket high level what all the APIs which you can call that is also allowed. Okay? So, I have given like this. So, this is the resource. Okay? Because see, we are attaching the policy to a particular resource. So, that is my particular resource. Okay? So, whatever I can do is like allow this specific object within this resource itself. Now action is like API, let's say S3 service. S3 service, all APIs. I don't want to restrict like only one API. All API I would be able to do get, post, and all. Effect is allow. Okay? And the principal. Principal, let's check out the Shreyansh user. Goes to IAM. IAM user, Shreyansh, it has its ARN number. So, this is I copied it. Now goes to this one. So, here added. So that's what I'm doing it here itself. So I added the principal also the who Who is allowed? What all API it can invoke on a service and on which is specific resource. So I am attaching the so is now here if you see in the S3 bucket this is one resource this bucket is one resource. Okay. So So currently it's say any error let's say comma is missing here here comma is missing. Okay, now it's done. So save changes. Okay. So now what we have done is we have created a policy against and attach it with the resource. Now So we have done this step. Now here if you see again we are now hitting through first AI this API access key and the secret key. So we need to make sure that we have a valid access key present. So we have there create access key. So application running. So let's create an access key. I always create and show it and delete it just want to make sure that I never encourage that expose your like access key secret key or put it into the get. This is just for testing purpose. So even though after every time I do a operation I deleted here just for setting up an example that don't do this mistake. Okay, if it is exposed deleted. Okay, so now for this user Okay. For this user we have set up an access key and currently this user doesn't have any permission. Okay. All we have attached the permission or policy with the resource. Now this user will try to access the S3 bucket and we know that we have put an asterisk the full permission we have given. So now if you see hit it. Server we need to make it up. Hit it. Shreyansh is not authorized. We need to see why. List all my buckets. Okay. Now here very interesting part. See. Understand with this one what happened. The same thing will come with the console also. Then let me show here console also. Now here if you see Amazon S3 general purpose bucket you will get the same. Refresh it. Here. The same issue you come. Why? Because see now the S3 service we have resource which is our bucket. So bucket is our what we have created concepts by Shreyansh hyphen prod. This is our resource. Correct? Now the API which we are invoking currently list bucket. That is like here. It has more bucket. It's So I am not performing operation on this resource currently. So the permission the policy is attached with this resource. Currently I am operating on here. Which list down all the buckets which I have created. Okay. But I have only given the permission the policy the permission I have created S3.star, all APIs I am allowed to do, but on which resource? Attached to this resource only. So, you use this resource specifically. So, now let's say this is another API, bucket-location. This bucket which I have created is present in which location? Which region, let's say. Okay. So, now I'm directly performing on this. So, bucket name. Let's put the bucket name. What is our bucket name? Let's copy that one. So, in here goes to S3. This is the bucket name. Let's copy it in our code. Just update it. Now, this is a bucket name. Now, I am specifically working on a particular bucket. Okay. Give me the region. So, bucket-location. So, this endpoint I have to hit, bucket-location. Now, it will give me the output. The specified bucket do not exist. Concepts by Shreyansh-prod This do exist. Concepts by Shreyansh-prod Okay. And uh I have updated application.properties. Yes, properly updated. Um What happened here? See here, AP South 1. Okay. So, I am able to get the output properly. I've just restarted the application again. So, now see, we are working on a specific resource with the policy I had attached to. Okay. And that's why if in the UI, if you try to do the same thing like here because in UI we we can't directly goes to a specific bucket. It's always first list down all the bucket and from there you can select. So that's why at the first itself it's blocked. You are not able to see. But I hope you got the point. Clear, right? This is clear, right? So we have What we have done is we have created a res- first in the S3 service, we are creating a bucket. We have created the resource. Okay? So this is the bucket we created. Now in the bucket we have Now in the specific bucket or specific resource we goes to the permission. We have attached the policy with the resource. So our resource is within this SJ payment prod, right? So here if you see resource is this and whatever beneath it we are allowing it. Okay? You have like what all operation is allowed on this resource? Who is allowed? And I have shown you the example here bucket location when we are using this specific bucket specific resource through which this policy is attached we are properly getting the output. And but in the UI, when you click here, you are not working on that specific bucket. So general purpose bucket is like you are it's just a list of buckets. So it's you can say that account level. Okay? On this account what all buckets exist, let's say. But what we have done is within one bucket we have attached the policy. So when we work on this specific bucket, it will work fine. But currently we are working on this API is like you can say that account level. So for this you need an identity policy. So you have to attach to the user that if you this user try to hit this API S3 colon is specific. Let's say it I think it is get all list something. Okay? Then allow it. So that's what I have explained here. So policy is clear? Now this very important. Remember this flow always. When the call arrives it check is there any explicit deny in the applicable policy or not. So one thing is we have explicit deny. We have implicit deny. And we have let's say explicit allow. There is nothing implicit allow. I told you, let's say any resource. Let's say S3 has bucket, let's say bucket 1 2 3. No policy is attached. By default implicit is deny only. Explicit deny, explicit allow we can do through policy. Like we have effect, right? We can allow it or deny it. So whenever any call arrives the first ex- the first priority it gives to the explicit deny. If explicit deny is there, deny the flow itself. If explicit deny is not done, then it check if explicit allow is present or not. Okay? If explicit allow present, then allow. If allow is also not there, then implicit deny. Consider this with an example. Now explicit deny example. I told you about identity policy and resource policy, right? Now this say that I have used both. In identity policy with user, I have said that allow let's say user 1 2 3 something. Effect allow all operation of the S3 service and all resources itself all bucket and whatever the objects within the bucket allow everything. But in the resource policy I have said one that when user this is the resource Okay? In this resource this action this operation for this user Shreyansh user is denied. Do not allow this user this operation on this resource is denied. But this identity policy say that the same for Shreyansh allow all operations even the get on all resources even on this resource. Now there is a conflict. Now when let's say user hit get object API on this resource. Okay? Now let's say user hit get object on this resource. SGA payment prod and within this. Now whether it should be allowed or it should be denied. So it should go through this flow. Is there any explicit deny in applicable policy? So this is one applicable policy. Yes. So what it does is it collect all the policy first. I am collect all the policy. There is no policy like priority. It collect club all the policies applicable. So yes this is applicable. Call if call is for S3 get object. Okay? And this is user for user Shreyansh and it is trying to access the resource XYZ. So this is applicable policy? Yes. Action is correct. Resource is also correct. From this user Shreyansh, yes. So, this is one applicable policy. Similarly, this is also an applicable policy. Now, the effect is different. One say allow, one say deny. So, the one say that is there any explicit deny? If yes, deny the operation. So, yes, one say allow, this is explicitly we are saying. One say deny, so deny always take the priority. Implicit deny means, let's say no policy present itself. No identity, no. So, what would happen is request comes, get object. So, it will club all the policy. Currently, there is no policy, so there is any explicitly deny? No. There is no policy itself. So, it goes to is there any allow present? Ex- There is nothing like an implicit allow. Is there any explicit allow? No, no policy itself. So, but still it will goes to implicit deny. No, you are don't have any access to this resource. Explicit allow is like in the let's say we have only one identity policy, it say specifically say allow. When this call comes, it it look all the applicable policy. This, yes, this user, this action on this resource is allow. So, there is no explicit deny. So, it come here. Is there any explicit allow present? Yes. Allow. Got it? So, this flow always works. Always helps to identify that uh when you create multiple policies and if sometimes you say that, "Hey, why it got rejected? Or my why my operation got denied?" You can check this flow. So, policies are clear? Right? Policies are super clear, right? Like uh Both ident- I can I can only cover identity based policy itself. Like majority of the time that works, but I wanted to show resource based policy also because that is also I feel is important. Now the next is IAM group. Very small topic. Now we'll cover within 2 minutes. Group is nothing but a container for the user. You can't log in as a group. You can't authenticate as a group. Nothing. It's just simply a container. Okay? Now one problem is that you have let's say 10 users. User one, user two, user three, user four, user five, user six. And let's say you have created one policy. Now you have to attach, let's say in case of identity based policy, you have to attach to this and you have to attach the same policy to this, to this, to this. If you have 100 users, then the same thing. What we can do is that we can create one group, let's say developer group. Developer group. Now when we create user, user one, user two, by creation itself we can add them as a part of a particular group itself. And the policy we can attach to the group itself. So we can attach to the group. So now all the policies which are present for the group, automatically all the user will inherit it. That's one good advantage. The same thing I have explained here. So I have created a group, let's say back end engineer, and I have attached few policies with it. Okay? Now all the users which are attached with this group will inherit this policies automatically. Okay? So the same thing, how you can do that? Very simple, this is my root user account. So IAM, goes to the IAM. Click on IAM user groups. Create group. Provide group Provide any group name. Let's say back-end engineers. Okay. So, here you have also an option to add the users or later on also we can add it. Here also we can attach the permission with this uh group also. Now, let's say I want to attach permission with this is we have created one allow. So, I think we have deleted that, right? So, let's create one policy first. Do we have that policy allow or not? Let's create one policy allow JSON. S3 colon all APIs on all resources. Simple. Create. Policy name is allow full S3 access. Create policy. Now, policy is created. Now, I'm going to the user group again. Create group. My group name is back-end developers. I will add users later, but I want to add permissions policies allow full. This policy I want to attach. Create user group. So, my group is created. Now, I want to let's say attach user later. So, each user goes to particular user. So, now here if you see that groups add user to group. Yes, add this. Add user to group. So, now here if you see it inherit the permissions automatically from this group. Okay? Now, this user will refresh it. It will work. No issue comes to this user Shreyansh. Okay? I am user Shreyansh, no issue comes. We have given the access. The same thing I have shown here. Okay, cool? So, policies and groups are clear, right? Next, we will start I am role, which is also very important. Any doubt, let me know. We can discuss further. Bye.
