# AWS: EC2 for Beginners | AMI, Instance Types, vCPU, Key Pair & Launch EC2

**Video ID:** iOzZos13-aw  
**Source:** YouTube - AWS by Shrayansh  

---

Shanda said, "Welcome back." And today in AWS, we are going to start with EC2, Elastic Compute Cloud. So, before I start, uh so there are different compute you can say that uh provided by AWS like we have EC2, then we have something called Elastic Beanstalk, then we have ECS, EKS. So, these are different. So, today I'm starting with EC2, and slowly we will cover all. Okay? So, we will also identify like in which scenario which one is more suitable for us depending upon our use cases. So, let's start with EC2, Elastic Compute Cloud. We can think of it as renting a virtual computer, like a virtual machine itself. Okay? So, each EC2 behaves like an independent computer where we can access remotely via SSH. You can install any software like Java, MySQL. You decide which version you want. Run whichever application you want 24/7, and pay only for what we use. Okay? So, consider it as a virtual machine itself where you have complete freedom which services you want. Okay? Like which operating system you want. That freedom you have. Okay? So, consider this with this diagram. You have a physical server. So, this is one machine. One physical machine, I would say that. One one physical machine. And there is a software which AWS use hypervisor like AWS Nitro. Which task is to do virtualization. So, here one physical server is divided into multiple virtual machines. So, instead of virtual machine, if I say EC2 machine consider that it is virtual machine. So EC2 one So here I have operating system Ubuntu two virtual CPU 4GB RAM 20GB hard disk. So one resource one resource is divided into multiple virtual machine. Each virtual machine that like they are working independently that they own the complete machine. You can say that. Similarly on the same physical server another EC2 machine is running which has Windows installed and has four virtual CPU 8GB RAM 50GB disk. Similarly you can have more EC2 instances running with different operating system different RAM and CPU configuration. Okay? So got it? So if I ask at this point of time can you tell me when you use EC2? So the very simple answer would be like it's a complete virtual machine where we want full control from operating system to which application we have to install. Okay? And it's a scaling and like version. Generally what happen is legacy applications. >> [snorts] >> All legacy applications generally are on EC2. Why? Because we need full control. Okay? We can't just go with the beanstalk like where virtual machine is created by AWS with version decided by let's say Java it always use 17. For example with the legacy I have to have a very specific Java version installed. So I need a full control. So whenever you need a full control from operating system to application versions go for EC2. Okay? So there are certain components of EC2. So if you see let's go inside EC2 and we will launch one. Get it started. Okay? So, the first thing is name and tag. Just give any name. Let's say my {hyphen} EC2 uh something app or let's say That's all. My EC2 and you can give any name like something, whichever you want. Now, the another important component is AMI, Amazon Machine Image. So, understand this. Let's say you buy a laptop. You buy a laptop, but that laptop you get is hardware. All you got is hardware. But suppose there is no operating system in it. Will you be able to boot this laptop? No, there is nothing to boot. There is no operating system available itself. Like there is no operating system, you won't be able to install any software, you won't be able to do anything. All you have is hardware. So, you need certain softwares, right? So, Amazon Machine Image. Here, it's a pre-configured template used to launch EC2 instance. So, we have to start from somewhere, right? So, it provide us various options that okay, which operating system you want? Amazon Linux, Mac OS, Ubuntu, Windows, Red Hat. There are different. You can browse it. So, you can choose which are currently here we are doing testing. So, whichever is free, okay, so you can use it. And uh let's say I am going with Amazon Linux. So, it is available here in the free tire itself. So, we can use it. So, let's say for Mac OS also, if it is free tier eligible, you can use it also. Okay? so I'm using here Amazon Linux. It is free tier eligible. So means I won't be charged any money for this one. So, this is one is starting point. So it will just give you an starting point. Okay, after you have a machine with CPU, RAM, hard disk like this is the OS which will get installed. Okay? Now before I select and proceed further, let me go one level more deeper into this. Now let's say I have created a EC2 instance. Okay? And while creation because I need some starting, right? Which OS I wanted. Let's say I chose Amazon Linux. And my EC2 is created. EC2 instance is created. After that I have installed multiple software like Java. Okay, I have installed let's say Git. I have installed let's say different multiple softwares I have installed into my EC2 machine. Once done, what I can do is I can create an image AMI image from here. So what it would happen is once I create my AMI image from this EC2 now it will have like okay, what operating system, what all applications I need to be installed. So it has all the information now. Now in future when you have to create another EC2 instance then you can choose your AMI Amazon machine image. You can search your AMI. And when you let's say I give my AMI let's say my app AMI then you can search it. And once you select it, then automatically when your EC2 instance is created, it has not only operating system, let's say which is Linux, it also has all the softwares which were installed. Like Java, Git, etc. Okay? So, that's why I told it's a pre-configured template used to launch EC2 instances. Think of it this way. Without an AMI, it's like buying a laptop with no OS installed. Hardware exist, but it has nothing to boot, so we cannot use it. So, when we launch an EC2 from an AMI, AWS create a new virtual machine using everything is stored in that AMI. So, currently, whatever we are selecting, it's only OS, nothing, no pre-installed software will come with it, only OS. But, I will also show you that once we have created an EC2 initially with, let's say, Linux, after that, if we have installed multiple software, we can create our custom AMI. Okay, we can create that. And from future, any EC2 instance we can create, we can use our AMI, so that it will not only have Linux, but also the other softwares. Okay? So, here I have selected Amazon Linux, which is free tier eligible, and generally it is recommended for AWS. Cool. Now, another we have is that instance type. All this I have explained. This is the same thing I have explained everything that you have multiple software installed. So, when we launch a new EC2 using custom AMI, all those are already available. So, the same thing I have explained. The next component while creating an EC2 comes is your instance type. So, see, understand this one that one thing is here we have to choose like how much CPU and RAM is required for your server. So, it has multiple family. T M C R etc. There are many. So, each has their own purposes. Like I will show you I will tell you like their purposes. But first of all, understand that let's say I am using T family. T micro what I am getting with it? I am getting two VCPU virtual CPU 1 gigabit RAM. This I am getting it. Okay, so this is free tier eligible so I am using it. Let's say. But many times when saying it we get two CPUs two virtual CPU. What exactly is two virtual CPU? There is nothing like hey I have two CPUs three CPUs. Okay, understand this that when whenever a VCPU concept comes in we have in a physical machine we have one CPU which has many core. For example, I am saying that CPU with four core. What does four core means? Four core means four task it can run in parallel. No context switching. Actual in parallel it can run four task. Okay. Four core. So, now when we say VCPU virtual CPU what exactly this VCPU means? We have only one CPU with four core. What is VCPU? So, now here if you see in case of AWS each core let's say I am using four core concept it could be eight core 16 core 64 cores. So, this is just an example four core. So, let's say core one, it has two hardware thread, hardware thread one, hardware thread two. Core two, core three, core four, each one has two hardware thread. Okay? So, this is generally you can say that virtual CPUs. Now, I am using let's say t3.micro instance type. With with using this instance type, I am getting two virtual CPU. So, two virtual CPU means I am not getting two cores. No. I am getting two threads, hardware thread. Okay? So, core one, thread one, considered as a virtual CPU one. Core one, thread two, virtual CPU two. Now, you can ask that, "Hey, what is the use of it?" So, see, I'll tell you my understanding. See, this is my understanding totally. I might be wrong, also. My understanding is the reason why we have two threads over one core is for a better efficiency. For example, see, we have a I have explained in Java, I have explained a concept of context switching, I think in memory management video. So, context switching is done by operating system. And this is not a you can say that this is heavy task. And generally happen when let's say any IO operation has to be done, and CPU has to wait. But, for example, within a CPU, like within one core, let's say core one. Okay? Now, it has task is to read from memory. Now, it has to read from memory, it might take let's say 100 CPU cycles. Like certain Like you can say that microseconds it needed, nanoseconds. Okay? So, there is no need for context switching. But here, still certain CPU cycles are getting wasted. So, that's where to even enhance the efficiency, it has two threads per core. Okay? So, even if certain CPU cycles, which are getting wasted for very, very, let's say, certain nanoseconds, in that it internally does handle multiple threads. Okay? So, it's an overall concept of just utilizing the core or you can say that slicing the CPU core CPU core time efficiently. Okay? Now, here if you see, there are different family of instance type, like T, which is purpose is burstable. Burstable means Burstable means, let's say, uh with I'm going with the micro. Let's say micro. With micro, I am getting, let's say, 5% baseline. Now, what happened? 5% baseline. 5% baseline means, let's say, 100% my CPU core one. Okay? Understand this just for an example. Only out of this 100% CPU, I am just getting 5% baseline. Means you will you will only get 5% uh time of the CPU. That's all. Okay? It's a burstable. And baseline is 5%. Means I will only get 5% of it. And there is a concept of credit. So, when I am within the baseline, let's say, I am just using only 3%, 4%, 2%, so every hour it give me certain credit. Okay? So, every hour my credit points get increased. Let's say 200, 300 every hour I'm getting let's say 100. So, 400, 500. Now, suddenly a burst traffic comes in. So, what it will do is it will increase from 5% to let's say 10, 20, and ultimately reach to 100% but it will now consume my credit points. But as soon as my credit points are fully zero, okay, it will again bring me back to 5%. Okay, and again if my traffic is still more, but my credit is zero, I can only consume 5% of the traffic. That's all. 5% of the CPU only. That's all. So, that's what burstable. Okay, so you just getting a certain percentage of the CPU allotted to you. Okay, and you will get certain credit limit which you keep on adding if you are within the limit. And once burst traffic comes in, you will use more percentage of the CPU, but your credit point will use. Once your credit points got exhausted, you will again bring back to the your baseline. So, that's what the size is nano, micro, small, medium, large, extra large, 2x large, 4x large. There are more. So, it just like you can search it. There are so many. So, okay. So, let's say T3 is small. With the T3 is small, I am getting two virtual CPU and two gigabit of RAM. Okay, so there are so many things. So, free eligible is only this. So, for testing purpose, T3 micro is sufficient for us. But you got it, right? Every family has different purpose. Let's say M family. Okay. It is general purpose, balanced. And in this case now in this case in M, you won't get 5% of 100%. Okay, so now let's say one thread you are getting, right? Let's say this thread. You are getting 100% off it. Like when when you are working on it, you are getting When you get Let's say this thread got a time to work on this core, it will work with 100% capacity. Okay? But general purpose and balance means the CPU and the RAM which you get is the balance. Okay? Similarly, compute optimized. See, in this you get more CPU than RAM. Then you have R family, memory optimized, which is like you get more RAM than CPU. So, depending upon your task application, you decide which family you want to go. And on that family generally burstable is I would say that for testing purpose and for like UAT testing only environment is sufficient. Otherwise, it's very unpredictable in for the production. So, once you decide your family, then you just select the size. How many virtual CPU, RAM because that get changed with medium, large, X-large, and all. Okay? So, instance type in short is like how many virtual CPU and how many RAM you will need for your virtual machine. Got it? If anybody asks you that, "Hey, I get two VCPU." And they said that, "I get two VCPU." They And somebody say that I got two CPU. Then you can correct them. Like it's not two CPU, actually. It's this concept. Hardware threads are Each core has two threads. So, each thread considered as a virtual CPU. So, generally two threads you got. Okay? It could be in the same core. Let's say VCPU one in this core, VCPU two in this core. It's possible. Okay? So, instance type done. Correct. I have selected this instance type. AMI is done. Then we have key pair login. So, I am creating a new key pair. Give any name. My app my EC2 key. PEM. So, what is this? What happened now? So, see. Key pair is used SSH key pair is used to securely login to our EC2 instance without using a password. Okay? So, now uh key pair is based on public and private key. Public key and private key. Public key is stays into our EC2. And private key get downloaded. Get downloaded in our machine. Okay? So, now when I have done create key pair, a one private key got downloaded. It currently it would be there into my downloads folder. So, private key would have been stored. Now, when my EC2 machine is created and I have to login to that EC2 machine so that later I can install certain Java or certain I can run certain scripts, whatever. Whatever you have to do on your machine, you can first login through SSH. Okay? And after that, you can access your machine. So, SSH key pair is used to securely login to our EC2 instance without using a password. Private key dot PEM downloaded to our local machine. Public key is stored inside the EC2 instance in this path. Okay? Now, when we try to login, we provide our private key. So, what it will do it will The signature is done using the private key. Okay, when we try to log in the EC2 instance already has the public key, so it will just verify using the signature. And if the verification succeed, access is granted. If you open the HLD playlist in that there is a video I've created like cryptography. Cryptography, in that I have explained this public private key concept. And also, I think I have explained how signature works, how verify works. Okay? So, it's like during private key using private key we done a signature. And uh EC2 instance verify it using the public key. And if it is uh properly verification succeeded, access is granted. So key pair login is required so that we can have access to our EC2 machine. Okay? Then we have network settings. See from the previous video, I think I've already explained in depth about VPC, subnet. So, network setting I think there should not be any doubt in that. So, we have a VPC. We can have public subnet. We can have private subnet. Okay? And EC2 instance is generally created within a subnet. And you decide whether you have to create within a private or public. For now, I am not creating my own VPC, I'm not creating my own subnet. I am using whatever the default is present. And if you remember from the previous video, the default is that it has only one public subnet. Okay? So, the VPC, the default one, and the default subnet it will use, which is like a public subnet. Okay? But if you want, you can have your own private subnet. But with the private subnet, you would have to create a NAT gateway, which NAT gateway you can only connect to the internet gateway, but the NAT gateway is chargeable. Okay? So, I'm using currently the default one itself, which is the public subnet, and within this public subnet, our EC2 instance will get created. If you also remember, I've also covered the security groups, which is like you can say that a firewall. We can add like you can create a security group. How we have created I have shown you already in the previous videos. You can just select the existing one, or you can just create security group. It will create new. Like yes, allow SSH traffic from anywhere. Allow HTTP traffic, allow HTTPS traffic, whatever you want, and later also we can have inbound rules and outbound rules. We can configure that. So, security group, you know, right? If you remember, if you can recall, for each, let's say, EC2 instance, we can add rules. What all traffic can come in, what all traffic can go out. I've already explained this at least network concept in depth. Okay? For now, I am just creating I'm not creating any existing security group. I'm just using it create security group and just selected allow SSH traffic from anywhere. Okay? And I'm using the default VPC, default subnet. That's done. And here we have a configure storage. Okay? The next component is configure storage. So, now here, if you see, the operating system, let's say Linux, you might have certain OS files, right? Where it will get stored. It needs certain hard disk, right? Certain disk space. So, that's where. So, here if you see, when we select an AMI, it already have it give us this 8 GB. Okay, it's a root volume. Root volume means uh the volume the disk space where our OS files are present, which is required to boot up the your let's say EC2 instance. Okay. Uh So, root volume is clear. In this in this space in this disk space, all our OS files are present. Okay? So, this is when we have to boot up our machine, this disk is this volume is accessed. Okay? So, it give us an option to add more volume. Let's say you have a requirement to have more disk space for storing the images or something. You can add more volume, 20 20 GB more. You can add more volumes, and you can mount it. Okay? So, this is like configure storage. This one automatically comes when we select an AMI, and this is like 8 GB currently they are using it. And root volume, you know that root volume means in this OS files are present. You can add more volume, but currently we don't need uh we don't have to store anywhere something else on the disk. So, I'm creating not adding more volume. We can always add it later also if you require. File system. You have multiple S3 files, EFS, FSX. Currently none. I am selecting none. So, file system is like let's say you have EC2 instance one, EC2 instance two, multiple. So, this one the storage is there. Okay? So, let's say it's disk space. It has its disk space. But, file system is like we can have like file system which both the EC2 instance can use. Both the EC2 instance can access. Okay? Like S3 files. Currently, I don't have any requirement where I have multiple EC2 instance to have a shared uh access of the file. So, currently I've choose none. So, file system is like when you want multiple EC2 instance can read write to the same file system. Okay? So, here we have configured the storage, but over here motto is that this storage is like we can configure later also. If you need more disk space, we can add later also. We can create more volume and we can attach it with this our EC2 instance. Okay? We can easily do that. Now, with this here if you see we are ready. At least we know what is AMI, what is instance type, key pair login, network setting, configure storage, like you can add more volume if you required. Okay? So, currently I'm removing it as we don't require it for now. And I don't need file system also. For now I've selected none and we are good to go. Launch instance. So, here if you see our EC2 instance is running. Okay? This one I have a stop. This is previously. So, just like the running one. So, this is the running. Our EC2 instance is running. Okay? So, that's till now here we have done. Our EC2 instance is created properly. Now, what we will do next is like we will install the JDK. We will log in to our EC2 application virtual machine. We will install JDK and we will deploy our one Spring Boot application and run it. Okay? So, see you in the next video. But, if with this creation of EC2 if you have any doubt, let me know. We can discuss into the comment section. Okay? Okay, guys. Thank you. Bye.
