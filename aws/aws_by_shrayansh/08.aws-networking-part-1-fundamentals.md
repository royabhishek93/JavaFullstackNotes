# AWS Networking (Part-1): Fundamentals

**Video ID:** d7WdXmP9uuQ  
**Source:** YouTube - AWS by Shrayansh  

---

Welcome back and today in AWS we are going to start with networks AWS networks. But before we start with AWS networks we first understand certain network fundamentals. Why? Because now let's say if after IM completion generally what we do is we go and try to create an instance. Now let's say EC2 instance I'm trying to create. Now if you see this while creation of the EC2 instance what we do is we generally provide name and other details and here if you see we just provide a security group which is a part of generally a uh you can say that AWS network only but the major thing which we skip is discussing about this network this subnets. Okay. So here if you see the same thing before we try to launch EC2 instance we notice that most of the network details are set to default that's when I say that network some value is present here if you see some value is present for the network. Similarly some value would be present for the subnet also. So some default network default subnet is selected and only thing while setting up EC2 instances it asks us to select a security group that's all. So generally what we do is we just cover security group and start creation of the EC2 instance but we miss the very important concept the network because without this like we don't have production level information or knowledge. So this going to be a little like I I I try to go in depth as much as possible. So I'll divide this into multiple subp parts so that uh I don't cover everything in one very very long video. Okay. So let's start without wasting any further time. So my recommendation would be not to proceed further without understanding networking concept first because by using default network configuration by using this default one there is like a important or introduce a very important security risk and it is not production ready. So if your application is deployed in AWS they are never deployed using this default network. I will tell you at the end the reason also. So in this part of the video I will cover some networking fundamentals first. Then in the next part I will jump to AWS network terms specific like here we have VPC. So here if you see right VPC okay then you have subnet. Okay. So there would be more terms like this for AWS network but to understand that properly we first need to have certain fundamentals of networking. Okay. So I will start with this networking fundamental today. So the very first question is what is network? So in my terms I would say that a group of host your host can be your computers. A group of host that can communicate with each other is called a network and two different network to talk each other using routers. So let's say this is network one this is network two. So in this network one nothing but a group of certain computers which can directly talk with each other generally through switch. So switch considered switch is like a box where you have multiple sockets. Okay. So multiple devices let's say your printer, your TV, your laptop directly attach with this switch. Now they can directly talk with each other without any use of the router. So router you can say that is needed when you have to communicate with other device which is present outside of this network. Okay. So two devices can communicate with each other using router. Very simple term. Now let's say from network one packet need to reach to network two. So what generally happen is no single router knows how to reach from network one to network two. Each router hold the information about the next hub and this detail is called this detail is stored in routing table. What does that mean? So here if you see then when the packet let's say this system needs to communicate with this system. Okay, both are present in different network. Network one, network two. So we know that it passed through the router but in reality it is not just one router. There are a mash of routers. You can say that. So multiple routers are interconnected through each other. Okay. So you can say that it's a mesh. It's a mesh of routers and no one router has the complete information that okay this system A need to communicate with system B. Okay, it directly has this address. No. So it goes through hop. Let's say the packet receive at this router. It says that okay the next hop is this. It says that the next hop is this and ultimately the destination. So you can say that there could be many path to deliver the packet. Okay. And that is decided by the router and based on their routing table. So now let's say the router one has received the packet. It has one routing table. In routing table it says that okay this packet which is coming its destination is this 912.168.1.0. This is the destination. It tell that okay next hop is router two. So it goes to the router two. Now the router two says that okay your destination is this you want to go at this next router is next hop is router 3. So it reach router 3. Now router 3 says that okay the next destination okay your destination is this. I know this you don't have to go any other hop. I will directly deliver to this system. So at this point of time all I want to know that uh we are in sync up that network is nothing but a group of system group of devices that can communicate each other directly. But when any device wants to communicate with another device that is not present into the same network, it is done through router and the path is decided by the routing table because there is no one router. There could be a hundreds of routers in reality. Okay. And no one router has a complete information. So pack it just do lot of hopping from one router to another to another and to the next where the next hop is that is determined by the routing table. Okay. Now another fundamental is IP address. Now what is IP address? IP address is the address of a device on a network used for communication between devices. So here if you see that when this device A wants to communicate with this device B. So how it determine through IP address. So this 192.168.1.0 is nothing but an IP address. So this is its IP address unique identifier you can say that for this device. Similarly, its IP address. Okay. So, we see IP address every day. Below is an example of IPv4. We I think we all have learned about IP addresses before in colleges and we have we work with day day in day out, right? Uh so we know what is IP address. So generally IPv4 is a 32bit number. Okay. So let's say 192 168 1. So it's all eight bits. Okay. So 1 2 3 4. So 32 bits overall. So in binary it would be represented like this. The same 192.168.1.1. So in binary form it is this. Now here if you see how many total IPv4 address are possible today. So today it's possible right 2 to power 32 which is like a 4 billion 4.3 billion addresses possible today. Okay. Now before we uh proceed further just one thing here is that now in each octed so this our total one by is equals to 8 bit right. So here 2 raised to power 8= to 256. So 0 to 255 is the maximum value you can get like after doing 1111 8 times the maximum you can get is 255. So like 192.168.1.1 so generally you can say that its range is 0.0.0.0 to 255.255.255.255 255 that is the range of your IP V4 you can say that okay so 32 bit number you know that and uh like 8 bits 8 bits 8 bits 8 bits so the lower value is 0000 0 and the maximum value it can have is 2 raised to power 8 256 combinations so 0 to 255 so 0 to 255 so that many IPv4 4 addresses and total how many it's possible 2 to^ 32 which is like 4.3 billion IP V4 addresses are available today. Now one question might have comes to you that okay in IPv4 4.3 billion addresses are possible but today world we have tens of billions of devices which are connected to the internet and yes we have been like it's long that this IPv4 addresses are totally finished okay and that's why you remember that in the college also we have learned about IPv6 which is 128 bits which is not 32 bits 128 bits. So how many total combination 2^ 128 which is like 3.4 into 10^ 26 trillion addresses like for us we can say that it's like infinite kind of thing where we don't have to worry about the IPv4 address exhaustion exhaustion okay so 3.4 4 into 10 d about 26 trillion addresses. So so many things right but if you see 99% of the time if you check the IP address of any host you will still find IPv4 only. So one question definitely should have comes to you that okay if total IPv4 addresses are 4.3 billion and total device on the internets are tens or hundreds of billion then how today 99% of the host which are connected still has IPv4 how come it is possible how internet is working that's where the private IPs comes into the picture this This is like very important and uh you can say that the core concepts of network private IPs okay so because of this only even with 4.3 billion IPv4 address we are able to accommodate tens of billions of host over the internet each one able to get the address how private IPs So below are the three private range. So the one which is known as C. So when I say that range now it is I will be call it as C classless interd domain routing. So understand it very properly. So one range so these are three range 1 2 3. So 10.0.0.0/8. This is one range of IP. Now you can say that hey how to read this how it is a range. So the slash8 means that leftmost 8 bit are fixed. Okay. So now let's first convert this to a binary form. So 10 you know that 8 bits how to convert it to 10? 1 2 4 8. So 8 and this one. Okay. So 8 bits. 8 bits. 8 bits. 8 bits. So 10. So this is our 10 0 0 0 0. So 0 0 0 and this is 10. Now slash8 slash 8 means the 8 bit the leftmost eight bits are fixed. So here if you see this yellow one this is now fixed. It cannot be changed. Only this can vary. This can vary. This can vary. Means some can be one some can be one some can be zero. Only this can vary. But this leftmost eight bits are fixed. So now how many total combinations are possible? Okay. So here if you see the address is the lowest would be 10 because this is fixed and uh rest can be 000000. So 10.0.0.0 0 this could be the lowest address and maximum could be this is fixed 10 dot max I told you right uh the maximum it can have is like all one 1 so that's nothing but a 255 so 10 dot 255 255 255 so that's the range of this C this private IP range so total how many total available IPs within this range change we can easily calculate that 8 bits are only fixed right out of 32 bits only eight are fixed so 32 - 8 24 left so the total number of combination is 2 to power 24 see 8 are fixed rest all 24 can vary okay so 2^ 24 16.7 million IPS are reserved in this private range Okay. So that is one range. Second, the second C is 172.16.0.0/12. Now what does that mean? /12 means that leftmost 12 bits are fixed. So let's represent in binary form 172 16. So this is your 16 0 0 0. So this is 0. This is 0. This is 16 and this is it's 172. So 12 12 bits left most bits are fixed. So 8 this is fixed and four bits here. So these are fixed. Now rest can vary. So what is the lowest address possible? So this is fixed. and the rest can all be zero. So 172 this 16 0 0. So this is the lowest and highest would be so or represent all this 00 0 here which can vary with 11 1 here. So 172 and so this is this yellow one is fixed right and rest zero can vary. So the maximum is like when they all have one. So this is like 172. This one this octed become 31. This is like 255 255. So this is the highest range. So total number of private IPs which can be available here is total out of 32 is fixed. Okay. Rest only can vary. So here if you see 2 to^ 20 1 million IPs are available within this private range. The third range is 192.168.0.0/16. Now pause here. You itself compute what does this mean? How many total number of IP ranges available here. So / 16 means left most 16 bits are fixed. So 8 8 16 totally fixed the rest this eight and this eight can vary. So what is the lowest range? This is fixed right? So this one can't change. So 192 this one is 168. This can vary but for lowest this all should be 0 0. So 0 0 and for highest this all should be 1 255 255. So total number of IPs available within this range you can say that 16 32 minus 16 2^ 16 which is 65,536 IPs available appro so this many IPs private IPs are available. So one thing you can say that at this point of time okay I understand that there are some private uh private IP ranges available but what exactly is private IP? So the what makes them private that is the question. So the thing is that no public router on the internet will forward packet to these addresses. If your address is within this range IP address no in internet they will not deliver your packet they the internet pretend that this address does not even exist. That's the first you can say that property of private IP. In internet nobody able to deliver packet to this addresses. The second thing is that anyone can use them simultaneously without conflict. That's the major part. Why 4.3 billion IPv4 can accommodate tens of hundreds of billions of devices? Because multiple devices can use this IPv4 private IPv4 address without conflict. So let's say in my home my laptop IP address is 192.168.1.0. In your home the same IP address is also being used. So now we are using the same IP address same private IP address without conflict. Okay. So if that is clear now the the next question come is that okay if internet is not delivering packets to these address how I am able let's say in my home I have this device how I am able to communicate with the internet I still able to uh open google.com I am still able to search anything okay so but how is that working if internet is dropping my packet because they don't support they don't understand private uh private ids let's say that's where the concept of NAT comes into the picture network address translation okay so NAT is a software which runs on a router that modifies the IP address while they are in transit so now let's say this is my home network this is my laptop which has a private this address 10.0.1.10 1 0 okay this is a private address range so I am using this one this range within this range one IP I am using it okay now I have to query my destination is 88.8.8 which is let's say for Google for goo I'm trying to hit Google and uh the IP address for the Google is 8.8.8.8 8 and here is my IP address which is private. Now we know that internet uh do not or pretend that IP address do not exist. So they never deliver to this address. So what happen how net is helping? So the first packet goes to net which is you can say that a specialized router which has a net functionality network address translation it's a software you can say that and router is a hardware you can say that so on this router hardware we know that when your packet need to go outside your network it has to go through router okay so net is you can say that a software running on certain router router. So now the net receive this packet. What it does it it modify the IP address. So what it will do this net has its public address. So what it does is when it get the packet now in the routing table first it will update it has its own routing table. The net has its routing table. It say that okay the user want to go is 8888 this is your destination okay it knows that the next hop to the internet gateway internet gateway is considered as that which send the packet to the internet. So internet gateway is you can say that a specialized router which task is to just send the packet to the internet web. Okay. So when the net receives the packet what it does is it knows that okay the packet which I am getting from the host which has private IP it wants to call this destination and it knows that this destination I have to invoke internet gateway it has to goes to the internet but before it send to the internet it can't directly send this packet because see it has private IP internet doesn't understand this address it doesn't understand this private address. So what net does is it modify. So in the mapping table what is does that its public address its public IP address and it does a mapping that hey I am mapping it with this private address. Okay. So 5210.20.30 is a public IP address of this net. Okay. And its port number which it is using. Similarly the private IP. Okay. So it add this and its port number. So it add the mapping. Now it just modify this packet. So here if you see the source IP address is update it its public address 52.10.20.30 and its source port number like whichever it is using 4001. Okay. And it then it's sent to the destination. Okay. Now internet it goes to the internet gateway. Internet gateway just push it to the world like web. Now when the response comes from the internet to the internet gateway internet gateway will push it to the net. Now what net will do? It knows that the packet the destination is 5210 2030 because internet only have this information right. So it will send the same thing. So it when it reply it reply to this address from where the request has come. So that's why the destination in the response is the net one because net has updated its own public address. Okay. And its port. Now net will check its mapping table that 52.10.2030 and this port number what is the mapping of the private host 10.0.1.0 0 and this is the port number. So it knows that which ultimately host private host I need to pass this data to. So you got it. This one is clear. Okay. So net is properly clear right. So net is an specialized you can say that router where its uh task is to modify the packet. Okay. And when it do the modification what it does it it update the the source it update the source which has private IP with its own public IP address and store the mapping in its mapping table. And when the response comes through the mapping table, it knows that uh for this which private host it need to send this packet to. Okay. Now at this point of time we have covered network, IPv4, switch router, routing table, net and internet gateway all individually. So many times confusion comes that how to connect the dots between all of this. So I will just share one diagram where I will connect the dots between all. Okay. So let's try to see the whole connection flow everything together. very important right go slow because see these are very interesting like once you learn right network is always very interesting but yes as a backend engineer we have generally if you're working in MNC's generally we have very less uh hands-on over all this thing so I hope you will find it interesting also so now let's see first allocate IP address for our network so what we are doing is uh the first thing is we have to create a network okay this network we have to create so within a network I told you that it's a group of host group of devices which communicate together so how many groups or not how many group how many devices you want to have this in network let's say 500,000 10,000 how many devices so you need that number of IP address right so you have to select a range so that's what the first step is allocate IP IP address for our network. So whatever the network we are creating we have to allocate IP address range select it properly because once we done it like that number of IP range we can use like uh let's say if you have a requirement always consider future that today you have a requirement of 500 hosts but so always it's a better that you take for thousand let's say in future you have to have a 600 require 100 more you can easily accommodate it because that many available IPs present present in your range. So requirement is that max 500 devices can be within this network. So currently the range private range I am using is this one 192 16800/16. This one this range 192 16800 16. So I am using this range this IP ranges. So how many IPs are available with this? So /16 means the 16 bits left most are fixed rest can vary. So we have already seen that how many available host are possible 2 to power 32 - 16 are fixed and rest 16 can vary. So 2 to power 16 65,000 IPs are available if I use this range but my requirement is only 500 devices. Why I need 65,000? Why I need to reserve this much? Okay. So what I did is that uh I can reduce the count. Let's say I the range I am using is this. Now how I come up with this like 192 16800 0/16 how I come up with /23. So here if you see this is the range overall. We know that the 16 is already fixed. We can't change it. 8 here 8 here. 16 is fixed and rest can vary. So I can further fixed out of this. Right? So it will reduce the range. So out of this 8 and 816 which are fixed this was fixed earlier. I have further if you see 1 2 3 4 5 6 7 more bits I have made a fixed to zero. This I have also fixed. So now total available is 32 minus 23 which is 2^ 9 only. These all can vary rest all are fixed. So only 512 IPS I can have. So you got it what I did. So see when I use this range 192 168 0 0 16 is already fixed. Okay that cannot be changed. But since I have less requirement so I have further more fixed it. Okay with 0 0 0. You can have like fixed date with 1 0 0 because see they can vary. They can have any value 1 0 1 0 0 0 or 1 1 1 1 it can have any value. So you can fix it with anything whichever you want. Let's say if you want 1.0 so you can fix it with uh uh let's say 2.0. So 1 2 so you will have one here. So you can fix it with this. Okay. But for now I have fixed it with zero. So means this one is zero currently and uh this all can vary. So now available host is 2 to^ 9. So 8 bits here, one bit here which can vary. So 512 IPs I can have in my network now. Okay. So the fixed part 8 bits, 8 bits and 7 bits, right? 1 2 3 4 5 6 7. Yeah. So this fixed part generally identifies our network. Okay. and the remaining one identifies a unique host within our network. So in a network is identified with this. Okay. 192.1680023. So this is the lowest range. This is the highest range. Okay. So our device can get an IP address private IP address within this range. Lowest and high highest. So you know that how to compute the lowest and highest right? So you can easily do that you can pause it. So let's say this is 23 bits fixed right. So lowest could be like all the variable should be zero. So 192 168 0 0 highest should be like all this are one one and this eight bits are one. So this is 255 and this is one. So 192 168 1 255. So this is our highest range. So any host which will we will create within this network are within this range and also note that there are few addresses within this range are reserved. Let's say broadcast address. So let's say you have 100 host within this network. So 192 168.255 the highest one is reserved for broadcast. Anything you publish here it will everyone will receive it. Okay. And the lowest one is used to identify the network. Okay. So like let's say you have 100 host out of 100 host like which they belongs to which network there can be multiple network present. So we have to identify this network. How to identify with this lowest one. So this is also reserved. Apart from these two the host can get any range within this. Okay. So one thing we have done at this point of time is whatever we have done we have created a network okay with a range. Now the second is host within that network. Now let's say I have created host one host two host three and allocated an IP addresses. So the IP address will get allocated within this range whatever this network support I already told you its range is 168.0.0 0 lowest and highest would be 168.1.255. Okay, this is highest and this is reserved for identifying the network like which network this is for broadcast rest apart from this it can have out of this 512 are reserved. So out of 510 any range can be any value can be assigned to your host. So let's say 0 1 02 03. So this IP addresses are assigned to the host. Now host within the network are connected via switch. So now here if you see this all host within a network is connected via switch. So switch has multiple sockets. They all connected directly. Now very interesting part here is you if you see switch always work with MAC address not IP address. MAC address. So see these are IP address but switch only switch belongs to layer two it only understand MAC address it doesn't understand IP address so what's the flow okay so now let's say this is our host it want to send packet to the destination 192 16803 so now let's say that this host wants to send packet to this Okay. So it has its IP address 192 16803. This is the destination for this one. Okay. The first thing this host will do itself whether this network within my network or outside the network. So every host also maintain a routing table. Every host even your laptop which you currently working at. If you are on Mac, you can run this command in the terminal route and get default. You will see the routing table and this is dynamic. This routing table is dynamic like it's not static. So whenever you connect to the internet uh your uh the Mac or your host system automatically update this routing table. Okay. So this routing table has this that okay the destination you are want like 192 16803 so it has this C range 192 16800/23 so it has a proper lowest range and highest range so it know that any address within that belongs to that range is local means it don't have to go outside the network okay so 190 to 168 8 03 okay so it's under this C only so that's why next hop is local and I you can easily tell like by now by the time you would already clear right for this destination IP it has two part the fixed part and the host part the one which can vary the fixed part is your /23 So 23 bits are fixed that identifies your network itself. Okay, 23 bits which are fixed and all the first address I told you that first address is always reserved for the network. So 23 left bits are fixed which identify your network and the first is always reserved for the network. So 0 0. So it can easily identify that this IP address this is your network and it say that hey it's a local. So whichever the network you are connected the sender host the destination is also belongs to the same network. Okay the one thing which sender host know that okay this is the local now I need a MAC address of it. So now what it will do is your local host itself also maintains one ARP table address resolution protocol table. You can run this command in the terminal ARP - A. You will see this address IP address versus MAC address. Okay. So the switch whatever the devices you can say that uh they are interconnected. Let's say the router. Okay. So the switch only understand MAC addresses and everyone maintains the two thing routing table and the ARP table. Routing table ARP address and these are all sync with each other. They all keep updating themselves like if there is any new addition into this or if there are any removal. So everyone is properly sync up. So now with the ARP address resolution protocol table for this IP address it find out the MAC address. Okay. And it will get this MAC address. Now sender host does all this computation first. Okay. Before sending the data to the switch. So it first check okay this is the destination address. Is it like within my network or outside the network? So, it will check its routing table. It find out that the next hop is local. Local means within the network itself. Okay, got it. So, it's uh know that it's within my network. Now, it's want its MAC address. So, it check its ARP table from for this IP address. It find out the MAC address. And then the next step now it will add the packet. The source IP this is sender host 192 16801. Destination IP is this 192 16803. It has additional information. Source MAC address its own MAC address. Destination MAC address which it get through this computation its MAC address. Now it pass it to the switch. Now switch directly connect with the destination node. So see switch has all the wires connected to devices. Okay. So it worked on the MAC address. So as soon as it get the packet from anywhere based on the MAC address, it will directly pass it the packet to it. So the MAC is for this one. The MAC address is for this destination. So it will pass the packet to this device directly. Okay. So this is clear host within network are connected via switch. Now routers. So we have seen network we have set up the host then host are connected through switch and we have seen that a packet which need to move from which need to move within a network. Now one thing is you remember that the router is needed when the packet has to move outside of the network. Okay. So now let's see its flow. The the sender h the sender host the this a wants to send packet to 8888 let's say Google. It wants to send packets to Google. The first thing it has to determine is what's the next hop like to whom I need to send. Okay. And uh we can easily identify through routing table. So its routing table will have an entry. If your destination address matches this means this is your local network means the whatever the destination you are trying to set present within your network only. Apart from anything send it to this 192 16807. So now let's say the your switch is connected with this router. Okay. So it has its IP address. So in the routing table everybody connected together in this with this switches this devices in their all routing table they know that if anything apart from my local network anything any other address comes 000000/ any any address okay I need to send it to the default router. So this is the IP address. Okay. So now the one thing it knows that okay if I need to go talk to google.com the next hop I need to talk to this I need to send the packet to 192 16807 but you know that the data first goes to the switch. So switch only understand MAC address. So we have to find out the MAC address for this. So again it will check its ARP address resolution protocol table. So if this is connected then definitely everyone has ARP updated also they also have an entry that for this IP address 192 16807 for this router what is the MAC address. So it has MAC address stored. So it will return this MAC address. So now sender host in its packet add source IP, destination IP, its own IP, google.com IP and the source MAC and destination MAC. Now it pass to the switch. Now switch see that okay the MAC address and it will pass the packet to this router directly. Okay see switch is you can say that it's a it doesn't have any knowledge. It just work on MAC address. I got a packet based on the MAC address it will directly pass to that one. So it so the MAC address we got is of a router. So it directly pass to this packet. Cool. So who determine that uh the packet need to be delivered within a network or outside the network is your routing table. So routing table tell that the packet destination address is within your local network or it need to go outside. But what's the next hop it can take? Okay. So we have determined here. Now one doubt might come is why we need router mesh. Hey, why we have so many routers let's say. Okay. And each router has let's say their private address because it's within a network, right? So they still have their own private IP address and each maintain their routing table. But why a network mesh? So see there are could be many reasons. I tell you a couple of reasons. The one is fall tolerance. Now let's say any packet which is getting delivered earlier it was delivered like from router D to router A, router A to router B and from here outside. But what if router A got like uh there is some problem and it is nonfunctional. So because of this mash the packet can take another path. Each one will update their routing table if they find out anything. uh not reachable so the packet will take another route. So you can say that we have a fault tolerance any uh problem with the router do not block the packet. The second is load balancing. Let's say the router B has two let's say 100 packets need to deliver to router B. So if we have a mesh we can let's say delivered or we can distribute the traffic across multiple path. So this path taking 50% of data packet this path is taking 50% of data packet. So no one router is let's say you can say that overwhelmed with the packets. The third is long distance handling. So see uh today your host your host switch and router they need to connect. So your host data goes to switch from switch it has to goes to router. Okay. If the distance is too long let's say 100 kilometer. So everything has a limit. You can't have just one cable 100 km right? So everything has a limit. Every physical device has a limit. So that's where instead of one long cable of 100 kilometer directly connected you can have multiple routers each one is like multiple they managing their li within working within their limit. Okay. So destination is present in different network which is very far and every every device has a limit. So possible that one router is not sufficient and we need multiple router to forward before it reach the destination. So that's why there are multiple reason we have uh so many routers present to deliver the packet. Then the fifth is we have to connect the dot is with the net. Okay. So we have already seen what net is. So now in our diagram we have set up a range of the C for our network. We have set up a host the switch the router. Now I told you that uh when the packet has to go out and if you have this private network though this is our private network right so this IP address it can't talk to the outside world internet with this private address it it need a public address and that is where net comes into the picture so the packet has to reach to the network uh net and then from the net itself it goes to the internet gateway and from internet gateway is goes to the outside world internet. Okay. So this is where our net fit which has public IP also and you know that what net does it creates a mapping table and whenever any data comes it update its mapping table. Okay. So this is one flow and another flow is that no router mesh in the middle. Okay. So switch is directly passing to the net. You know that when this is true when you have not much distance between switch and net. Let's say they are in same building. Okay. When they're in same building you can easily you don't need a router. Okay. So net is also nothing but a router with just net capability. So it's again it's a router only right but with a net capability. So you don't need a another router mesh when you both have a switch and router in the same building. So I have specifically told that you need multiple routers when the packet has to travel long distance. But if they're in same building distance is not uh much and there is less traffic, it's directly pass you can directly pass to the net also. Okay. So this is the flow. Let's say your sender host 192.168.0.1 he say I want to send the data to 8888 destination IP which is for Google. The first thing it tells to determine its routing table the next hop. So is it your local network? No. for any other apart from local network it says that forward it to 192 16808 it's net router IP address okay since it is connected with the switch switch has uh known its MAC address so it's everyone updated their IP address also uh MAC address also their ARP table is updated so now this host will check its ARP table address resolution protocol for this net router IP address that what its MAC address. So it will get the MAC address. Now it will send the packet to the switch with source MAC and destination MAC. So destination MAC is the net router MAC. Now switch will forward the packet to the net router. Net will pass it to the internet gateway. Internet gateway pass it to the your internet. Okay. Internet gateway. So we have set up this. We have set up this. We have set up understand this net internet gateway is just uh you can say that window its work is just to push the packet to the outside wall. Okay. So either you can have directly this or you can have a router mesh between the switch and the net depending upon the distance between your switch and the net. Okay. So this is clear. I hope at this point of time we are on same page. So this is the network fundamental. Now till now here I haven't used a single AWS network term which we will see in the next video. But at this point of time we should be clear what is C. Very important. Okay. It is nothing but a range of private IP for your network. Okay. Then we have routers, net, internet, gateways, private, public IP should be very clear, host, okay, switch. So this I think routing table very important. So this we should have on the same page. Okay guys, see you in the next video. We will we will be discussing AWS network perceptor. Bye.
