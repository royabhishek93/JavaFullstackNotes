# AWS IAM Role Explained with Demo

**Video ID:** Y759sLI_Dag  
**Source:** YouTube - AWS by Shrayansh  

---

Let's start with IAM role, the another very important entity of IAM. So, what is it? So, till now we have seen that in the user we create username, password, or we create our access key and secret key, correct? These are long-lived credentials. Okay? So, let's say after 100 days, 200 days, these might still be valid. So, you have to make sure that you time to time rotate it. So, that's where IAM role comes into the picture. IAM roles provide temporary identity. You won't require long-lived credentials. With IAM role, you can get temporary credentials, generally valid for 1 hour, 2 hour, we can change it. So, here if you see that instead of storing long-lived AWS access and secret key, which can cause security risk, user services can assume a role and get temporary credentials. So, what does it mean? Assume a role. Here I have you seen that assume a role. What does it mean? Now, for example, I'm taking day-to-day example. Let's say in our team we have one intern person and we have one senior engineer SD3, let's say. Now, intern has only read-only permission on S3, whereas the senior developer has read-write permission on S3. But, for any scenario, let's say for 1 hour, you want intern to have the senior developer role or senior developer permission. How you can do that? That only for 1 hour intern should have all only those permission which senior developer has. How you can do that? via role. So, what we can do is that understand that our intern generally on a regular basis has only read permission. But, whenever it required senior developer role it you can say that we are a mask. Okay, it wear a mask. Let's say for 1 hour. Now, it would be act as a senior developer. It has all the access which senior developer has like write permission, read permission on S3. After 1 hour it will again switch back to its original permission zone. You can say that. So, what is required for the role? The first thing is we have to create a role. Let's say senior SD role. So, we have a role. Senior SD role. The one thing is we need to attach all the permission which senior SD should have with this role. So, permission one, permission two, permission three. All the permissions which are required attach with this role. Okay? So, that this role has all the permission. First. Second. Maintain the list of identities which can use this role. Why it is important? Now, let's say you have created the role and this role has all this permission. Read permission, write permission, let's say something delete permission, everything. What all permission is this attached with this role. Now, I have user one, user two, user three, user four, user five, like this. Now, if all can just wear this mask and perform, then what's the use? Correct? So, the second thing we need is like the list of identities which can use this role. Let's say only user one and user two is allowed to use this role. So, only these two user, whenever required, they can wear the mask or you can they say they can you can say that assume this role. Okay? User three, user four, user five, user six, if they try to assume this role, they won't be able to do because they are not in the you can say that the allowed list. So, let's say we have our intern guy here and this is our role which we have created senior SD role. So, it has two part. The first part and the second part. The first part is let's say the trust policy. The where I told you right maintain a list of identities which can use this role. So, that's known as trust policy. Second part is permission. All the permission, permission one, two, which are attached with this role, you can say that permission policy. So, with the role, you have trust policy. So, trust policy again, see everything policy has the same kind of syntax itself. Version same, statement, you can say that area of rules, and then I have only one rule in that we have action. Okay? So, action is like what this trust policy is saying? Who can assume the role? Okay, now who can assume the role? So, one thing is understand one thing before I told you the action. When the role, I told you that temporary temporary credentials are provided. Who provide the temporary credentials? So, there is a service called STS. That's your I think security token service. Security token service. So, yeah. Security token service, STS. So, it has let's say one endpoint, assume role endpoint. So, role whenever this endpoint this service this endpoint is invoked it do certain validation. It check the policy and everything. If it is allowed, then only it will create temporary credentials. So, that's where in the trust policy So, what we are trying to do? We are trying to make sure that who can who can wear this role? Who can assume this role? Who can wear this mask? So, principal. You know that, right? Principal means who part. So, here I have tell Shreyansh is the user who can assume this role. Action is your what part? What operation? So, Shreyansh can invoke this assume role. You can say that. So, STS I told you security token service. This is the service. This is the you can say that endpoint. So, I am allowing Shreyansh to assume this role, to invoke this endpoint. And after this endpoint is invoked certain validation would be checked. I will show you. And after that temporary credential would be created. So, yes, first part is done. This part, maintain a list of identities. You if you have more, you can add an array of this. So, currently we are only trusting or we are only allowing Shreyansh only one user to call this assume role API. Okay? So, that is one. The second part is we have to create a policies, different policies and then we have to attach with this role. So now let's say uh I have only one policy that uh action is S3 on this service, S3, all its endpoint is allowed. Resources, all resources within the S3 service, different buckets, those bucket have different objects. So all resources this operations are allowed. Effect is allow. And we will attach it with the role. We have to attach it. So we have to attach it with the role. So a role has two part, part one, part two. So again all the policies, all the permissions that a role can have, we have to attach it. And another is like who can assume this role? Otherwise, if you don't uh have this part any user can just assume the role and start using or you can say that performing the operation which they are not intend to. Okay? So that's why this part one is very important, the trust policy. Like who, which user can assume the role? Okay? So with single role, these two are very important. And also there is another validation, you can say that at the intern side where which is we have to put identity based policy that intern we need to allow the intern to invoke the endpoint, right? So action, STS assume role resources on this. On this senior SD role, whatever the role we are going to create on the particular role, this is the our resource. On this particular role we need to allow the user to invoke the STS assume role endpoint. Okay? Because see, I told you day-to-day activity, intern will only have its read permission, let's say. Only when required, it can wear the mask. How it will wear the mask in programmatic though? It will invoke certain endpoint, which is nothing but our STS assume role. This endpoint, okay? And it will tell that, "Hey, for this role I am assuming as assume role. You have to Which role you have to assume?" So, you tell the role also. So, this is our resource. So, you have to invoke that endpoint assume role and you provide the role which you want to assume. And then only you wear a cap for now for some time. Okay? So, we need to allow this intern or this user to invoke this endpoint, this action on this resource. So, this is also required. Okay? So, this is clear? So, now what we have is three things. On a role, when we create it, we will have trust policy. Then we will have permissions attached to it. And then we have on the intern, we have to allow that, okay, assume role it has to invoke on this particular role itself. So, now let's create it. Okay? So, let's go to the IAM. So, this is our root account. I have already cleared everything. Let's say this is our user. So, here if you see, currently no policies are attached. Okay? I have removed group and everything. So, then now here if you see in IAM, we have something called roles. So, now go inside this role. Click the create role. So, now we are creating the role. So, we will write our own custom trust policy. So, we will write our own policy our cell. So, this is optional. Effect is allow. Principal. The first thing we are doing is so see this is a trust policy. So, in the role I told you that we have two part, trust policy and the permissions. So, first we are creating trust policy. Trust policy means who can assume the role. Action is this. This is the endpoint. Principal is who? So, we need a user, a specific user. So, I'm going to the IAM. User. This Shreyansh user, I am allowing this user. Okay? So, okay. So, we have we are allowing this user to assume this role. So, this is our first. So, we went to roles, create roles, custom trust policy. And now here if you see, the principal is the user to whom we will allow this action to assume the role. Okay? So, that is you can say that one validation. Trust policy is like validation, first validation itself. So, that's done. Click next. Currently it now it is asking for attach all the permission. So, in the previous video policies I have already created one policy allow. Yeah, allow full. This policy I have already created. So, we can create a policy goes to the same IAM policies. We can create the policies. So, here if you see this IAM this policy has this info. So, here if you see we have created this. All S3 endpoint, all resources. So, this policy we have already created. So, now we are just attaching. So, the second part. I'm telling you now the second part. Trust policy we have created. Now that all the permissions we have to attach it, right? So, now I am attaching only. So, uh let's do next. Now you have to give the role name. Let's say this is my senior SDE role. This is the role. So, now role has two things. One is trust policy and the permission. Okay? So, let's say the senior SDE has all this permission. You can add more. Okay? And who is allowed which user is allowed to wear this mask or wear this hat? SDE role hat. So, you can give that, okay, this user principal. Only this user should be able to invoke assume role endpoint. Okay? So, this is you can say that the role side validation it put. So, I create the role. Now role is created. Okay? Role is created. We have done this part. So, we have created the trust policy. We have attached the permission. We have created the name. And in the we have validated that yes, our permission and trust policy is proper. Now we have to allow the user also to invoke. Okay? So, now let's goes to our user. Shreyansh. Either we create a policy. So here if you see in the permission we have add permission. Either we create a proper policy or we can directly create from inline also. So go to the inline. JSON. What we are doing? We are allowing. So here I am allowing STS assume role. So user will invoke that, hey I need to invoke this endpoint on this service. On which resource? Our role. Which we have created. So let's go to the IAM roles. Our role is senior SD role. This is the role. It's resource. So here I am putting this resource. Okay? So now we are giving a permission to user that yes, you can invoke this uh endpoint for this resource. You can say that this is extra validation we are adding it. Okay? You You can read it like this. Op- action is nothing but a operation. What What you can do? What this user can do? It can invoke this endpoint on this service. On which resource? Okay? So in which resource you can consider that uh assume role need to know that, okay, for which role you want to Which role you want to assume? So we have to tell that, right? Hey, this role I want to assume. So this is the resource we have to tell. So we have added. So since we are creating inline policy, generally is nothing but a policy creation. So we have to give a policy name. Let's say assume. Assume senior SDE policy. This is something I've created. Give any name. So here if you see service is STE. And the resource is the role name. Okay, the role name which it want to assume. So, I'm creating the policy now. So, it has a permission. So, this we have created also. So, this part also we have created. So, this is done. Now, what we have to do is we have to login as Shreyansh user which has this role assigned and try to access S3. So, now here if you see in incognito mode incognito mode, I already have see Shreyansh as already login. Now, currently if I refresh access denied. Now, you can ask here why why Shreyansh user is getting access denied when here in the Shreyansh user, we have added the permission. But here if you see, what permission we have added? The permission which we have added is that it can invoke this assume role endpoint. It can invoke this endpoint. Okay? And when it invoke this endpoint assume role and it is applying for you can see that this role. Then only whatever the policies which are attached with this role. Okay? So, whatever the permission policies attached with this role which is like allow all S3 action, then only it will get. So, it has to first ask, right? It has to assume the role. When I say that assume the role, it has to ask that. When in the incognito mode, when we logged in as this user Shreyansh Shreyansh user and you try to access the S3 bucket I'm refreshing we are getting you don't have permission. So we have to assume we have to ask. So in UI how we can invoke the assume and point? So go to the role. Whatever the role we have created senior SD role. It says that link to switch role in console. For console I copied this. Now where I am my Shreyansh user is login and that browser itself I am typing that link. So now it show me this. Okay? So yes, it is saying that we are switching the role to this. Yes, switch role. So now you can say that my user Shreyansh has wear the hat of senior SD. So still my I am signing as a Shreyansh only. But currently active as senior SD role. Now here if you see if I go to S3 and click on general purpose bucket now here I see no issues. I am able to list all all the bucket which are present. Even though you refresh this no issues come. It's working fine. Got it? So here that's what I have shown. So when we login as Shreyansh in a new incognito window you while accessing the S3 you got the access denied. But you have to ask for it. Okay, you have to invoke the end point. So in UI uh in the role which we have created they have a console which help us to uh invoke the assume role end point to STS. So I just copied that and the tab where my Shreyansh user was login in that I put it I just click on switch role and yes, as I even though I'm signed in as Shreyansh but currently active as this role and S3 is working fine. Okay? And the good thing is in the coding part if you see earlier in the coding part uh here earlier we need to have an access key secret key. But when we work with uh let's say this role you don't need this credential itself. You don't need this access key secret key. You can just put instance profile credential builder.create. That's all. Internally SDK itself will call the STS. Okay? And that will do the validation. STS will do the validation that okay in the trust policy in the trust policy this is trusted or not. If yes, it will generate the temporary credentials and that will be automatically added. We don't have to add the access key secret key. SDK part I will show you when we will ultimately work with S3 and all. But for now my only intention is to have on the same page that with the role we don't need long-lived keys. And most of the cases we don't need long-lived keys itself. Everything most of the time we can do work with the role itself. And that's what we will prefer. Also, whenever we work with S3, whenever we work with anything, we will try to first go with role and then see if it works or not. If not, then we will see that why. But we will always first try to go with role itself. That is more important. Okay? So everything clear, right? Like why trust policy is required why permission policy is required. Because if you don't have trust policy, if anybody invoked it, okay? That role would be given to it. Okay? So, it need another level of validation that the role itself manage who can assume this role. So, you can say that role is self-sufficient that it has all the permission plus also maintain a white list users who can assume it. Okay? And this is like user side because before it has to invoke, it need a validation it need a permission also, right? Yes, you can invoke this. So, that's why it can invoke it and it can ask for this role itself. Okay? So, we need to provide the permission. Everything required permission in AWS. So, it need to invoke an endpoint, so it need a permission. Yes, it can invoke. After that, STS need trust policy for validation. So, you can say that it STS has internal logic where it's validation based on trust policy. Okay, I got a request from user that uh it want to assume role endpoint. It hit the API assume role for this role name. Now, it has to run its validation, STS. That can I give or not at that it find it through trust policy. Okay, it then it uh see that yes, this user which is call has come, it's present into the principal. And yes, it is uh for this action only, assume role only. Yes, it has invoked assume role API only. So, once this validation is clear, then only this permission is granted. So, you can say that we have double validation. First validation as user side, it can invoke or not. The second is once it invoked, then STS do the validation based on trust policy. And then only the role is assigned to that user. Clear? Try it out. And uh it's very simple. And in future, we're going to try to make use of role more and more instead of creating uh username credentials or or you can say not username credential because we for console, we might need it. But you can say that access key plus secret ID. Just take one example. Let's say GitHub action. This is CI/CD pipeline. CI/CD pipeline. Now, in this scenario one use case is use case one. We generate access key plus secret ID. And this ID and all in the GitHub action, in this job we put it. And whenever it has to deploy it use this access ID and secret key. So, now one problem here is that our this access ID and secret key we are storing here into the Git secret. Okay? So, instead of this, it says that hey, you can use role itself. So, GitHub action doesn't have any access ID. Whenever it has to run, it will ask you can say that it will try to assume the role. It will try to assume the role. Hey, I need deployment access. Let's say that as assume role the role is deployment role something. Then it goes to the STS. STS will validate whether GitHub is allowed or not. If yes, it can generate give the credential temporary, and then it goes with that temporary credentials, which is valid for maybe for 1 hour hours. So, this is another very important use case. I don't want to go here, but here if you remember when I goes to this uh IAM here we have, right? IAM roles. We created the role. Here if you see, right? Web identity. AWS account. SAML 2.0 federation. So, see. Till now we have only seen AWS services. Let's say user. Web identity is let's say uh you are you can have let's say for GitHub actions CICD pipeline because they are not your part of a uh your AWS account. They are not AWS uh they're outside AWS, right? So, they might be uh invoking different endpoint. So, now let's say web identity. Next. Uh let's say Google, something. So, audience ABC. Let's see. Next. Uh assume let's say a lawful access. So, here see. They might be invoking some different endpoint. That's what I wanted to show. They might not be invoking assume role. They might be invoking different API of STS. STS can have multiple API. So, I don't want to go in that. It might create confusion. But when we will cover GitHub actions. Okay? The CICD part, how to with the CICD we can deploy it. Then we will come here because in that we might be creating role again. Okay? So, in that we will see. But at this point of time I only showed you the use case where like we can is still have exercise these uh and the secret key stored in GitHub action and it can work properly. But instead it can we can create a role itself for the GitHub actions, say. And whenever it want it can ask assume the role, get a temporary access, do the deployment, and done. No need to temporary store it. This long-lived sessions. Okay? Any doubt, let me know. We can discuss further, but try it out with the hand. Okay, guys. Thank you. Bye.
