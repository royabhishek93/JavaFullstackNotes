# AWS: Deploy Spring Boot Application on EC2

**Video ID:** w2HvYOskA8M  
**Source:** YouTube - AWS by Shrayansh  

---

So, till now, what we have done is we have created an EC2 instance. Okay, so this over virtual machine is got created. Now, the next which we have to do is we will log in to this machine. Okay, and what we will do is we will install JDK in that. Plus, we will run one Spring Boot application into it. Okay, let's see how to do that. So, first thing is let's log in to our virtual machine. So, in the previous part, we have already downloaded our private key, which is currently present in our download folder. Okay. So, step one. It's an optional, whether you want to do it or not. Move your private key from downloads to hidden folder, .ssh. Generally, .ssh folder is hidden. Okay. So, it's just an optional part, whether you have to do it or not. So, let me do that. Okay. So, let's do that. Okay, so my I have renamed my private key as my-ec2-key.pem. So, let's run this one. Let's run this command. Okay, so I already have it. So, yes, do you want to override? Yes, I want to override. So, done. Now, the another requirement for this is like uh you have to change the mode. So, you we have to it's uh you can say that it's a requirement. It should be only read-only. Your private key which you have, it should be only read read-only. So, this is also you can say that you have to do. You have to change the mode. That's also done. Okay, so once this step is done then we are good to connect with our EC2. So, SSH -i the path of your private key EC2 -user at the rate. This is static, you don't have to change it and the public IP address. Okay, let's see the public IP address. So, here in your EC2 instance, click here. This is the public IPV4 address. Copy it. Use it here. Click it here. Yes. So, now here if you see we are in our EC2 machine. So, we are able to log in properly. And also I will just show you one more thing. If you run the command ls cpu, I think. Yeah. So, here if you notice CPU here something thread per core two. Okay, so if you remember in the previous video, I have already showed you like what is virtual CPU, VCPU, right? Every core has two threads. Every core has two threads. So, that's where we can confirm with this. So, threads per core is two. Cool. So, that is clear. Our understanding is correct as of now. Now, what we have done is we are currently able to connect with our EC2. So, this a step we have done. Now, the thing we have to do is we have to install Java 17. Okay, so we can run this command. So, if you are using Mac, so I think uh so this is package manager, yum. So, in Mac you can use Homebrew. And so like different uh so this this commands you can easily uh check it out. We don't have to recall it. And currently I am using Amazon Corretto Devil JDK version Java 17. So, we have multiple, right? You have Oracle JDK. Then you have I think Azul JDK. Okay, similarly Amazon also has this JDK which I am currently using it. Okay, for ACS we are working with AWS so try to see whatever Amazon provides use that. Okay, so now here it is done. Let's say Java hyphen version. So, it's coming now. Okay. >> [snorts] >> So, Java is installed. Now, this is step we have covered. Now, what we will do is that we will create a jar of our Spring Boot project. So, from a Spring Boot Initializr I have just downloaded the demo project. Okay, so if you just go to a Spring Initializr.io and just download it. So, you will get this demo application. All I have just put one rest controller here that whenever the slash comes I'm just printing hi all from Shreyas. That's all. Nothing much. Very simple Spring Boot application. Nothing else. All we have to do is now just create a package out of it. All we have to run is Maven clean package. What it will do is that it will create a dot jar file within this target folder. Within this target folder now I have this my project.jar. This is what we going to uh put it into our EC2 machine and run it from there. So, we have run this command Maven clean package and within the target the jar file has appeared. If you don't know about this command, open the Spring Boot playlist. In that, I have Maven lifecycle video. That will help you to understand that how what exactly happen when we do Maven clean package. What are the step it follows and how this JAR file is created. Okay? So, now we have this JAR file. What we have to do is we have to upload this JAR file to our EC2 instance. So, I am running this command SCP {hyphen} I. This is my private key which I have. Okay? And my JAR is present into this path. Downloads demo target, this one. And I have to copy this to into a my EC2 instance. So, EC2 users, your public IP address of the EC2 and at this path home {slash} EC2 user. Okay? So, let's So, we have to run it not from the place where we have logged in from other. Like where we have not logged in to our machine. Okay? So, our terminal machine local machine terminal, you can say that. So, here I'm just changing the public IP address. This is the public IP address I have put in. Okay? So, yes, the JAR is still present into my same path and my key name is also the same and it is also present into {dot} SSH. Everything else is same. I just run it. Okay? So, now it is copying this JAR file from there to my EC2 machine and it's done. Okay? So, we have copied the JAR file to our EC2 machine. Now, what we have to do is we have to run the Spring Boot app. Okay? Now, just go to your machine where we are you have logged in. So, this is I've logged in to my use So, now we have to run this. Okay? So, Java {hyphen} jar and your jar name. So, Java {hyphen} jar and I think it's demo {hyphen} 0.0.1 {hyphen} snapshot .jar. So, we are running the Spring Boot application. And here, if you see, it's started. The Spring Boot application is a started. Okay? So, now let's test it. Clear this one. Now, what we will do is we have a started over Spring Boot application. So, from our terminal we can run this command. curl http the public IP address. This is my public IP address. I copy it. The port number it started is I think 8080. 8080 it started. Okay? So, here I'm putting 8080 {slash}. I'm getting something? No. Why? Because now let's see our machine once. Instance. Here goes to a machine. Goes to the security. Goes to the security group. Inbound rules. So, here if you see, only SSH is allowed. 8080 is not allowed. So, edit inbound rules. Add rule. Custom TCP. 8080. Okay? Any traffic. Port range is this is allowed. Save rules. Okay? So, 8080 I have put it into my white list. You can say that. Now, let's Let's to run it. Hi all from Shreyansh. it's come. And the same we can run in incognito window. Continue to the site. Yes. So, it's working. Okay. So, here if you see we have added into our the security group inbound rules. And once we have whitelisted this 8080 port number where our application is running, we are able to get the response from our API. Means our Spring Boot web server is running. Now, one problem is that here this is my uh Spring Boot is running, right? What if you close this? Okay. Now, here if you see I close this. What if you let's say terminate it. Will your Spring Boot application is still be up and running? Okay. Let's test it. No. Your server also goes down. So, what to do? So, like do we always have to The question is like if you close the SSH your app also get destroyed. And another is like do we always have to manually login to our virtual machine and then run the command Java {hyphen} jar and then this one. No, in production or in you can say that companies MNCs we never login to the virtual machine and run the jar manually. We give this responsibility to the Linux itself. So, in Linux whenever the Linux boots a systemd also start. This is the demon. Okay. And this demon always reads dot service files dot service files and it load into memory. Now, it keeps waiting for the command like service to start, to stop, etc. So, this flow we can use it to automate this. Like whenever our let's say application is start, restart. Okay? So, two use cases. First time first time your EC2 instance is up, then also automatically your application should start. You don't have to manually log in and run this. Second is during restart also your application should be up and running. How to do that? Okay? So, again, no need to uh like we have to remember this command. No need to remember this command. All we have to know the purpose. If you know the purpose, let's say if you have this in mind. If somebody ask you that, "Okay, yeah, this is tedious like it's not something that's straightforward that, 'Okay, we have uh publish our JAR to our EC2 machine, virtual machine, but it's not very comfortable that I log in to my virtual machine and then I'm running Java {hyphen} JAR.' Okay? Then at least we should have something in our mind that yes, we can use something like this. Okay? So, I always encourage that we don't have to keep remembering this commands. There is no use of learning this command itself. All we have to do is like we always have is there a way to do that? If yes, then we can easily find this thing. So, one thing is first thing very simple is we have to create this service file. Okay? So, this is the Linux command. You can create my app I am just giving the name myapp.service. You can give any command. So, now let's do that also. So, first I have to log in to the machine again. So, currently we have logged out. So, Okay, sorry. Okay. So, we have to log in to our machine. So, SSH command, where is SSH command? Yeah. Okay. And what is the public IP address we have? 43205 Okay. So, now I am logging to my virtual machine. So, now let's run this command. The first thing is I want to create this myapp.service. So, currently Okay, it's have nothing. Now I am adding this information within it. What this information is? Let's say paste it. Now, what this is? Unit is just a description. It's an a Spring Boot application. Like whatever you have to readable format for other engineers, you can give it. Like what this myapp.service is for. Then this is important. You are telling Linux that how to start this service. myapp.service, how to start. So, what command we have given, right? To start this Java {hyphen} jar and then your actual jar file. Right? That we have given, the same thing. So, Java {hyphen} jar, complete path we are giving. Java {hyphen} jar and this is the place where our demo jar is present. Home, EC2 {hyphen} user. Okay, so this command it will execute. Restart equals to always. So, this is the for scenarios when our EC2 virtual machine is stopped and after that it restart. In that scenario, this should also execute. We need to reach run this command. Okay? And when it when this command need to be run? When multi-user.target. What this says that once the Linux boots successfully after that only this command should run, not before that. Okay? So, this is done. So, you can just save it. So, that is done. So, we have just created a myapp.service. We have just tell that this is the command which we have to run when when it get restarted. Okay? And at what time? Even the restart when the boot is fully completed. If it is successful, then only this command should be run. Now, we have saved this file properly. Now, we need to tell the demon that hey, we have created this new service. Update yourself. So, what generally happen is demon maintain a list in memory that these are all the service files it is tracking. Since we have created a new service file, demon might have not noticed it. So, we are just telling the demon that hey, you reload yourself. Okay? So, we are telling demon to reload itself. Done. After that, we are telling that during the first time also. So, here for restart we have tell your application your EC2 machine is started. And after that it is stops and then restart. That time we have told yes, this command has to run. But we also have to tell that during first time also. First time creation also this has to be executed. Okay? So, we can give this like auto start. Okay? So, auto start my app. This is the my app.service we have created, right? So, we are just adding that. During first time, when Linux boots, does service need to be started? Yes, it should. So, you do that. And after that, what we have to do is instead of manually you have to do that Java {hyphen} jar. Now, we are the first time only we have to start this. That's all. Now, internally Linux will run this command which you have put into this service file. Now, let's stop this instance. So, this EC2 virtual machine, I'm stopping it. Okay? So, stop. So, now what we will do is once it gets successfully stopped, we will start it again. Okay? Uh my {hyphen} EC2, we will start it again. And once we start it again, we will not run any command Java {hyphen} jar anything. What we are expecting is that it will be taken care by our this Linux itself. Okay? So, let's see that. Once it is let it uh stop fully. After that, we will restart it again. Okay? So, I am restarting this machine again. So, currently status check is initializing. So, once it get it started again, then we will see that we don't have to log in again to our virtual machine. And we don't have to run the command manually Java Java hyphen jar and your this one, the jar file. So, that should be done automatically as soon as the our virtual machine is started successfully. Cool. So, it take couple of minutes, but once now here if you see our virtual machine is properly initialized. Now, let's see that our application got to started or not automatically. I have not logged in into my machine and run the command. So, only thing is when we restart our EC2 the public IPV4 address got changed. HTTP {slash} 8080 in the terminal curl, I am getting the output. Okay. Now, another problem you can easily identify that whenever your EC2 instance restarted, your IPV4 address changed. Okay. So, that's why it's always says that you can assign some elastic IP address because it's static IP address. Okay. So, here if you go within the EC2 bottom, elastic IPs allocate elastic IP address. If you remember the same thing we have done to our NAT gateways, the same thing. Okay. So, allocate. We should be able to access with this also. Let's try on the curl itself. HTTP 80 80 Hi all from Shah. So, this is our static IP. No matter if you how many times you will restart your applic- uh your machine. Now, this elastic IP will not change. Okay? So, this will get attached to it. This one. Cool. Okay, so here as you see, elastic IP address, this one is also public, but this will never change. This might get changed every time you uh start. So, that is clear. >> Yeah. >> Okay, guys. Thank you. Any doubt, let me know. We can discuss further. Bye.
