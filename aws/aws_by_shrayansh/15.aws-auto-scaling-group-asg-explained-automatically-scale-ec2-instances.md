# AWS Auto Scaling Group (ASG) Explained | Automatically Scale EC2 Instances

**Video ID:** O8tVez5n6uA  
**Source:** YouTube - AWS by Shrayansh  

---

back and today in the AWS we going to cover auto scaling group. You might have heard this name a lot while working in a company itself, right? So, what exactly it is and why it is so important? The prerequisite is the video which we have already covered elastic compute cloud EC2. And if you can recall, we have done this setup where in one of the public subnet we have added over EC2, okay? And we have created an AMI, instance type, key pair, network setting, and storage. Using all of this, we created an EC2 instance. After that, what we have done is using SSH key, using SSH, we have logged in into our EC2 machine. We have installed the Java and also uploaded our jar to this EC2 instance. Okay? And one more thing which we have done in this video in this video is we have added our Spring Boot application as systemd daemon. So, that whenever any restart happen, we don't have to manually SSH into our machine and then start the application. Okay? So, whenever any start happen, restart happen, it will automatically start our application itself. So, till then we have already done. But, now let's move forward the problem it has. The very first problem it has is single point of failure. As soon as this machine goes down, and there can be multiple reason. Okay? One such reason is that your EC2 instance is a stop accidentally or operating system got crashed, Java process crashes, your disk become full, CPU reaches 100%, memory become full, or your availability zone has a problem. There could be many reason your EC2 instance got terminated. Okay, or stopped. Then, what would happen is your complete application becomes unavailable. That's why it is called a single point of failure. And another thing is no automatic scaling. Let's say today you have one EC2 machine. Okay. So, 10 user works fine. With 1,000 users, it become little slow. And with 10,000 users, your instance become fail. So, what you have to do is you have to scale it. So, you have to manually go and have one more EC2 instance. Okay. So, we manually need to create more EC2 instances, which is slow and risky, too. And again, let's say once there is a high traffic gone and the traffic is again slow or less, you again have to just remove this again. So, the scaling is manual, which is slow and risky. So, these are just some of the problem with only having one EC2 instance. And that's where auto scaling group ASG comes into the picture. So, auto scaling group main job is if the instance fails, replace it with an active instance. If traffic increases, ASG launch more instance. If traffic decreases, ASG can remove extra instance. So, it takes care of your You can say that traffic. Based on the traffic and your health, you can say that the health, it increases more instance, it remove extra instances. So, very important part is that ASG can only work when there is like we don't have manual intervention. So, one very important part is let's say you have this we have created an EC2 instance. Okay? We have created this EC2 instance manually first. After what we have done manually, we have SSH into our EC2 instance. This is also manual work which we have done. After that what we have done, we have installed Java. This is also manually we have done. Then what we have done, then we have uploaded jar. That's also manually we have done. And after that we have restart the application. So, all these things we have done manually. Now, when you want ASG. Okay? You want this scaling automatically. Then definitely we don't have to do do this manually, right? So, everything should be automatic. So, because see this ASG need to create instances automatically. So, there should not be any manual intervention. So, how to do that? Okay? So, that's why the very first thing is whatever the EC2 application we have over let's say Spring Boot application, very first important thing is let's say that it should be whenever it is started, okay? Or restarted, your application, the Spring Boot application, whatever the jar, it should automatically get run. Okay? That we have already done that. So, we are good on that. Okay? So, start the application that automatically we have already done that. So, we don't need to manually we have to go and start it. So, that's okay. But, what about all? We have created an EC2 like AMI and everything. So, there is currently a manual intervention. So, we have to make it automatic. So, that's where the first we have to create a template. Okay? So, we have to create a launch template. This is important. Because using this only your ASG would get created. Because using this template only. And this template has all the information which is required to create an EC2 instance. Like your AMI. Okay? Everything same. Instance type, key pair, network setting. Like whatever we have done right here. AMI, instance type, key pair. So, you can say that during launch template we have to create this also. And based on that ASG is created. Automatic scaling group is created. And one more part. AMI. You remember AMI? AMI has generally, let's say, operating system. But, if you recall here we have we have done an SSH. After that we have installed Java. We have uploaded the files. Okay? So, we need those also. So, that's where AMI image comes into the picture. So, whatever the EC2 instance which we already have where we already have Java installed where we already have our JAR file. Okay? Everything. So, from that we will create an AMI image. Okay? From that EC2 instance we will create an AMI image so that we also have OS plus all the softwares which are installed plus files like JAR files and all. Okay? So that you have everything. So we will not use any AMI like only OS. No. We will use an AMI which is from the running application, the one from which we have to create. Like this one instance we have created successfully. Properly installed everything. From that we will create an AMI image so that it has all the softwares plus files plus configuration everything apart from OS. Okay? So this launch template has the AMI image. Okay? Plus instance type key pair everything which is required to create a EC2 template. Don't worry, we will go one by one with an example and this will get super clear. So let's first set up a template. Let's say image template first. AMI AMI image we are going to set up first. So let's also do that. Okay, so currently this is our instance. If you remember this is the instance which is currently up and running. Okay? And this has our application which is running. Let's see if application is over running or not. HTTP 8080 So see. So this is our currently application which is up and running. Now on top of it first we have to create an AMI image. So we have to go to action. Image and template create image. Click on create image. Okay? The same thing. Yeah, image and template. After this, create image. Now, give the name, anything. Let's say name we are giving it my app {hyphen} AMI {hyphen} version one. Okay? Image description, you can give anything like uh this is AMI with JDK uh and a Spring Boot JAR. Okay? You can add more information. Okay? So, that's create image. That's all. Okay? So, the first thing I have done is the same thing. Now, it will take some time. Then, once our AMI becomes available. So, let's go to the AMI EC2 within the EC2 AMIs. So, this is the one. So, currently it is in pending. Once it become available So, currently it's uh pending. So, once it become available, we will create a launch template. Okay? So, let's wait for it to become uh available. So, now it's the status changed to available. So, we have now custom AMI image, right? Now, we will launch a template. So, within the EC2, there is something called launch template. Go inside this. Okay? So, let's uh first delete any old one. Delete template. Delete. Okay? So, now our AMI image status changed to available. Now, we have to create a launch template. Okay? So, go to the launch template. Create launch template template. Give any name. Let's say my app {hyphen} my app {hyphen} launch template {hyphen} version one, something. Okay, give any description that launch template for a spring boot ASG with custom AMI. Okay, so you can give any description. So, do you going to need this launch template for auto scaling group? Yes, just click it. Now, we have to select the AMI. So, my AMI is within that this is the one which we have created. So, we have selected that. Okay, fine. Okay? Instance type, here if you see one which you have like free eligible, we have already discussed what are instance type, just choose anyone. Okay? Key pair, if you want whatever the machine you want SSH permission, you can add it. Okay, you can whatever we have chosen in our original machine uh EC2 one, the first one. Or if you don't want any SSH permission to be given, don't add it. Network setting, don't add it like uh which subnet on all, we will tell into the while we will create ASG group. Okay? In the ASG group also, we have a option where we can tell the instance which we have to create, we have to create in which availability zone. Because see, ASG is going to create EC2 instances. Okay? So, there it provides an option that what all like uh how you want them or where you want them. Which availability zone? So, there we will get an option, so we'll see that. So, at this point of time, we are not providing any availability zone or subnet information. Okay? The only thing is like the security group rule we can add. Whatever the EC2 instances for testing purpose we want this traffic to be allowed on 8080 port number from the internet. Okay? So, you can have that one. So, currently, I already have this my security group. So, in the my security group, I have already covered that one. But, only thing is here it I have added one rule that a law in So, inbound rule. It's an inbound rule. So, custom TCP. Okay? Allow any traffic. 0.0.0 like something and port 8080. On this port, allow the traffic. Okay? So, that we going to we we should be able to test it. Okay? Rest, if you need extra volume, you can add it, but currently, whatever we have during our AMI, whatever we have for the root, that's keep it same. And what else? Create launch template. That's all what we have done. So, while creating a launch template, we have provided the template name. Okay? Description. We have selected this auto scaling Yes, we want we're going to use this template for our EC2 auto scaling. Okay? We have selected our AMI. Okay? Instance type we have selected like if you want T3 micro, macro, nano. Okay? Do you want a key pair for your EC2 machine or not? SSH you you should be able to do SSH or not? Because see, everything now we have automatic like we don't need SSH for now. So, that's why I haven't selected. Subnet and availability we haven't set here. Uh group I have selected the group where it has inbound rule to allow TCP connection on 8080. And rest everything is same. Launch template. So, now let's see we have a template present now. So now our template is ready. Now we are good to create the auto scaling group. So within the EC2 we have auto scaling group. So create auto scaling group. Provide a group name. Let's say my app hyphen auto scaling group demo. Give something. The launch template. Okay, so because I told you right? Auto scaling group need the template. So launch template, this is the template which we have created. Okay. Next. Now here see it give us a network option. Which VPC, which availability zone. So VPC you can keep the same in which you have your original EC2 machine, okay? Or if you have any different you can choose it. The VPC machine the VPC and the availability zone. So it's always preferred that you at least select two availability zone. Okay? Why? Because see there is one reason that your EC2 could be un- unreachable because your availability zone has an issue. So if you have two availability zone and let's say your EC2 EC2 instances divided among here. Then at least if this is down, at least you have still have this up and running. So that's why give an option of this two availability zone. How you want to distribute the EC2 instance creation into two this balance best effort. Okay? So it will just try to balance the EC2 instances in both of your availability zone. That's all. Next. Load balancing. Currently we are not adding load balancer. We will see in the next video. So currently no load balancer is going to add. Health check. So, health check is automatically done. So, now in your ASG, automatic scaling group, it internally keep check your EC2 instances health. Okay? See, there is one difference that within the EC2 instances you have a Spring Boot application. So, this might be healthy. Your machine is healthy. But, your application is down. So, this health check this health check from this scaling group, automatic scaling group, cannot determine this. So, this health check only determine the health of your EC2 machine. Within the EC2 machine, if your Spring Boot application is down, this check won't be able to catch it. But, when we will see load balancer, load balancer can determine like the application running within an EC2 instance, that is also up and healthy or not. But, we will see that. For now, the health check which internally it does, it checks the instance health, not the application running within that. Okay? Next. Now, here if you see, it has something called desired capacity, minimum desired capacity, maximum desired capacity. Let's say desired capacity is three, minimum two, maximum five. What does it mean? So, here this is scaling group we have a template. This template has the information how to how to create EC2 instance. So, now we have desired desired minimum maximum. What does it mean? Minimum I have told two, maximum I have told five, and desired I have set three. What does that mean? So, see, desired means at this point of time, how many EC2 instance you want to up up and running. Okay? So, it start with three. So, it will create three instance at a time. At this point of it start with this three. So, this group when we will start it, it will create this desired. So, consider desired as current. Okay? So, currently it will create three. Now, let's say traffic is more. Okay? So, we have this policy. What are the policy? No scaling policy, target tracking scaling policy. So, yes, target tracking scaling policy we wanted. For example, CPU utilization. Target value is 50. So, as soon as your CPU is utilized above 50%, it will try to increase the number of this EC2 instance. Okay? So, whenever there is a policy constraint, you can say that limit reach that now you have to create new instance, it will create new EC2 instance till max max it can create is this five. Because the automatically it creating. Automatically it creates, but it cannot go beyond this maximum. Now, once here if you see, so current here it become four after this. So, it will create let's say more traffic comes, EC2 machine current become five. So, now you see that this desired is kind like a current number of machines. Okay? So, this current cannot go beyond maximum. And now when traffic is low, then it will remove some of the instances. So, it will just change like again four, again three. Then, let's say this one instance is also extra, it will again change to two. But, it will make sure that this number don't go beyond this minimum. At least this number of EC2 instance should be up and running. Okay? So, this is just like a current. How many EC2 instances are currently up and running. If it need more, it will increase this number, but it never goes beyond this. So, consider this minimum and maximum as range. And your current desired one is just like move. Keep on moving between this range only. Okay? Depending upon your policy. So, policy for now, you can add more more than one policy also. So, for example, here one policy I've set is CPU utilization 50%. Okay? So, if above 50% as soon as limit increase, it will try to create more EC2 instance. Okay? So, that's what I have mentioned here. So, group size here ASG will try to keep average CPU around 50%. If CPU is high, ASG scales out. If CPU is low, ASG may scale in. Okay? So, that's all we have. And after that, next currently we don't have to set up any notification. Tags you can add like the name. Name for the machine it will create, right? Let's say spring boot hyphen ASG instance. Okay? And that's next. Create auto scaling group. So, now see it is up and running. It might take some time. And once it is done, then we will see that it should create a start with how many? Three. It should create three instances automatically. So, till now we have done this. Group size. Now, verify ASG creation. So, it should be Let's see. Okay, healthy. Instances three. So, now let's go to instances. So, here if you see this is a Spring Boot ASG we created, right? Running, running, running. So, currently initialized it is running. So, once it is done, we should be able to hit the traffic over Spring Boot application is up and running, so we should be able to hit the endpoint. This one. 65 2962. Okay. So, see now all three are able to like uh successfully process the request now. So, three instances we created. So, how to validate? Now, let's say uh let me refresh it. Okay. Now, let let me let's say delete one. So, this is selected. And let me action terminate. So, I'm terminating one. So, what would happen? Will it create new? Might not be. Why? Okay. So, here if you see why it will not create new? Because our minimum is two. Maximum is five. Current or desired is three. But desired does not mean you need that much. Okay? It keep on changing. It's uh it just like in start it it started with three, but after that depend upon your policy it will determine whether I have to uh add more EC2 instance or reduce it. Okay, but minimum is two. So, but currently my minimum is is still present. So, this is one, this is two. So, that's totally fine. But, what if I reduce one more? Let's delete it. Terminate one more. So, now we have only one instance up and running. Okay? So, we have now only one, so it should create one more. So, let's refresh it. So, see, this is the my EC2, this is the existing one. This is one or auto scaling group has created. Okay? So, now it should create one more because minimum is two. Let's refresh it. So, we can also check in the auto scaling group. There is something called activities. So, you can double check the activities here. What Whatever the auto scaling group is doing, you can check for here also. So, but it should create one more. Then only we can determine whether our AC automatic scaling group works properly or not. Okay? So, let's see. It should create one. Mhm. Terminating. Launching a new EC2 instance. Not yet in service, so it will add. Let's refresh it. Yeah. See here. So, it maintains at minimum two, what we have set. Okay? This is clear? So, our ASG automatic scaling group is working properly. We have already tried removing, and we have seen that as soon as it go below the minimum, it added new. But, there is one limitation of this automatic scaling group. The limitation is that let's say it able to create multiple instances now. But, each have their own public IP. It has its own public IP. Now, do you expect user to know this public IP? Like, okay, this traffic should goes to this. This traffic should goes to this. This traffic should goes to this. No, right? So, that's why automatic scaling group is always used with load balancer. So, between this user and this automatic scaling group which has EC2 multiple EC2 instances there is a load balancer also. User now talks to this load balancer. Load balancer distribute the traffic between this group. Target between this group. Okay? So, how we can add load balancer between user and this automatic scaling group, I will tell you in the next, but I hope with this if anyone say automatic scaling group, we should be clear what exactly it is. Okay, guys. Thank you. Bye.
