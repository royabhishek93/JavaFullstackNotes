# AWS Networking (Part-3): Security Groups and NACL

**Video ID:** YbAkglgDuhg  
**Source:** YouTube - AWS by Shrayansh  

---

back. So, till now, we have set up here all this part. We have set up AWS region, VPC, within the AZ we have set up public sub- uh subnet, and another AZ we have set up private subnet, we have set up route table, we have set up NAT gateways, internet gateways, everything. Now, the another main part is security group and NACL. What it is it? So, this is you can say that part of security, both. Okay? So, NACL, I will tell you what it is. It applies over all a particular subnet. So, it's a subnet level. Okay? And security group is applied over your particular host level. Okay? Now, let's see what it is. So, here NACL, network access control list. So, incoming and outgoing rules applied at subnet level. So, we can add rules, both incoming rules. So, see there are different type of traffic. You have, let's say, TCP traffic, UDP traffic. Within the TCP, you can have HTTP traffic. You can have HTTPS traffic. You can have another SSH traffic. So, you can have, let's say, SMTP protocol traffic. So, you can have multiple different type of traffics. Okay? So, you can control. You can add rules that what traffic are allowed for incoming when data is entering into the subnet, outgoing rule when the data is moving out of the subnet. Okay? So, subnet level. So, we can apply rules. So, it's like a security guard outside the building. Security group, it's incoming and outgoing rule again applied at host level. So, your EC2 instance. So, this security groups we can add for this particular EC2 group. So, like a security guard outside a particular door. Okay? So, we can apply it at a particular instance level security group and private uh NACL applied at a subnet level. The NACL is stateless. Means, for each traffic, let's say you have NACL, the incoming and the outgoing, for each, you need to have a separate rule. Like, you must explicitly set rule for what is allowed, what is what is allowed to be go come inside, what is allowed to go outside. So, you have an explicitly rule rule for incoming and outgoing. Whereas, security group is stateful. Means, replies are auto allowed. Let's say this is your EC2 instance. So, the security group you have set income incoming is let's say HTTPS 443. Okay, any traffic coming from this HTTPS 443 is allowed to go inside to EC2. And if your server is sending response, that is you can say that automatically allowed. You don't need to set outbound rule for it. Outgoing rule for it that, okay, uh allow this traffic also. No. If you have set up any incoming rule, any traffic comes passing that rule, response is auto allowed. So, that's why it is known as stateful. So, I just set up two example. Let's say outbound, I have put in the outbound. Allow HTTPS. So, means, when EC2 talks to Google, let's say, so, Google is now, let's say on HTTPS 443. So, your EC2 the security group So, output outbound rule would get satisfied because Google is let's say sit on HTTPS 443. Allowed. Now, Google send any response. So, you don't need any incoming rule. It because it's this connection response would be remembered and response would be allowed. Okay? Similarly, incoming. Okay? Let's say you allow HTTPS 443. Now, let's say user connect with EC2. EC2 2 on this HTTPS 443. So, request is allowed to go inside. And EC2 to user, the response your server is sending a response that would auto allowed. Okay? You don't need to set a rule for that. But, in NACL, you have to explicitly set incoming and outgoing. There is no remembrance. You can say that. Now, NACL support both allow and deny rules. Okay? And security group only allow rules. Means, by default, everything is denied in security groups. So, everything is denied. Only you have to allow whatever you want. But, here we have to explicitly allow and deny. So, let's say incoming. Incoming rules. You have rule one, rule two, rule three. This allow. This is deny. This allow. This is deny kind of thing. Okay? Similarly, you have outbound rule. Rule one, rule two, rule three. Some would allow, some would deny, some would allow like this. And here one particular rule is that each rule number. So, these are rule number. So, the lower the rule number, higher the priority. So, first it will go by rule number. Rule number one, rule number two, rule number three. Any particular rule is satisfied, it won't proceed the further rule. It would be allow or deny based on upon the output. For this rule, what if this match whether you want to allow or deny. But here in the security group, there is nothing like allow or deny. Everything is denied by default. All you have to do is allow whichever you want, whichever traffic you want allowed, otherwise rest is by default all denied. So, consider this now that any traffic from the internet which comes like at say attached to pass through NACL, then through it will reach to your subnet. From subnet, it will go to the security group and from security group, it goes to a particular instance. So, all the rules need to be satisfied for NACL, all the rules need to be satisfied for your security group for that particular packet, then only it will reach to this EC2. So, both if anyone denied, the packet will not reach to EC2. So, this is the flow. Let's say currently today in this setup whatever in the network we have done, we have not done EC2 setup now. So, that's why I put future EC2. We will put into private subnet, right? So, now let's say our EC EC2 is present into private subnet and it is send a packet. The same. Just have this diagram in mind. So, our EC EC2 instance is present into private subnet and you know that it it has to go outside, it will pass through the network gateway. From network gateway, it ultimately goes to the internet gateway. So, what would be the flow? EC2 instance first So, EC2, then you have a security group. Then you have a So, this is your private subnet. Okay? Then we have an ACL here. Then you have public subnet. This is your public subnet. So, it has its an ACL. Then your net gateway. Then it's an ACL incoming outgoing rules. And then your internet gateway. So, now when the EC2 instance, which is on private subnet, send a packet the first packet need to leave this uh security group, right? So, outgoing rule of the security group will run. So, this is the first validation. So, it will check it will check the destination IP and port whether it is allowed. If yes then it has to cross this an ACL outgoing rules. Okay? So, this is the second validation. Outgoing rule for the an ACL for this private subnet. So, it will again check the destination port whether it is allowed to go this traffic is allowed to go or not. So, once done then it will reach to the public subnet. So, but before the traffic enter into the public subnet it first go through its an ACL incoming rules. Okay? So, third validation. Incoming rule of public subnet an ACL. If that says that okay, yes, this traffic is allowed to come in then ultimately it will reach to NAT. And from there it has to talk to the internet gateway. This packet need to go out. So this public subnet an ACL outgoing rule has to be run. So, outgoing rule, this fourth validation. Then it will reach to internet gateway to internet. Now, when response come from the internet, so from internet it will has to reach to NAT. But first, this subnet incoming rule will run. Okay, incoming rule of public subnet will run. If that is allowed, traffic is allowed, then it will reach to the NAT. Now, NAT need to pass to the private host. So, first the traffic need to go out of this subnet, right? So, it will check the outgoing outgoing rule of the public subnet. After that, the traffic need to go inside the private subnet. So, it will check the incoming rule of private subnet NACL, incoming rule of private subnet NACL. After that, the traffic need to go inside EC2 EC2. So, incoming rule of this EC2's security group it has to check. And then ultimately EC2 instance traffic reach. So, here if you see, for one traffic, how many validations happen? So, I will tell you just my recommendation, but definitely depend upon company to company and the level of security you want. Generally, NACL is not touched. Okay? Uh leave it default, which allow all traffic. Uh which allow all incoming traffic, all outgoing traffic. It don't do any such validation, it allow all. But security group is the one where developer can put a specific check. What I should allow and what I should deny. Okay? Uh what should be allowed in incoming rules and what should be allowed in outgoing rules. Okay? But again, it's depend upon company to company. So, let's do security group setup. So, we have already uh set up all these things. So, we will set up our security group. Okay? So, here if you see again, let's go to VPC. Within a VPC, you will see security groups. Click here. These are all default. Create security group. So, we will create our Okay? So, now let's see. The name I am giving is private security group for my EC2 instances. Okay? And you can give any description that private EC2 no inbound calls are allowed. Private EC2 no inbounds request allowed. Okay? This VPC we need to select concept by Shreyansh, the one which we created. Now, we need to set up an inbound rules, outbound rules. So, see the one we are saying that security group is no inbound request. So, all we are saying that our EC2 should send request. So, only outbound rules we are setting. So, what traffic is allowed to go? All TCP connection, all UDP connection. SSH, SMTP, what is allowed? So, now let's say I am allowing HTTPS 443 to go out. Okay? So, you can put a specific CIDR also. So, I just put all 0000/0 means any. Any IPv4 address over HTTPS I am setting. Done. Okay? So, I have put a security group name, attach it to to my VPC. I am not to specify any inbound rules, means no incoming call is allowed. I have just set up an output rule that only HTTPS traffic anywhere is allowed to go outside from my EC2 instance. and create security group. Okay? So, now when you create an EC2 instance, so today we haven't reached till EC2. Now when you create an EC2 instance, you remember we have VPC, subnet, and there is like select security group. So, now we can select our security group for this EC2 instance that this is the security group I wanted, which we have created, private SGEC2. Okay? And now you know that even though we have only set up outgoing rule. So, here outbound rule only we have set up that HTTPS uh any any IP any IP any destination whichever you have on 443 HTTPS is allowed to go out from EC2. But you know that it's a security group, right? So, it's a stateful. Means any response coming from that connection is automatically allowed. So, that's why I have There is no nothing in the inbound rule. But even when our EC2 instance initiate a connection, okay? So, its response would automatically be allowed because it's stateful. It remember this. Okay? But it won't do fresh connection. It won't allow fresh connection. Okay? So, it's just like our EC2 initiate an instance initiate a connection, the response of that connection is only allowed. Okay? Now NACL setup we can also do. So, here if you see network ACL these are the default. We can create our own network ACL. Just give any name. So, now let's say name I'm giving is private subnet NACL. Private subnet NACL. So, means I am attaching I'm going to attach this NACL with our private subnet. Okay? VPC concept by channel which we created. Create NACL. So, this is the NACL which we have created. Now, here if you see it has the rules action. So, first here if you see this NACL which we have created we have to attach with the subnet. So, I want to attach with private subnet or you can also attach with this with public also. But, I want to attach this with private subnet only. So, attachment is done. Now, you can add inbound rules, outbound rules specifically, explicitly you have to set. Okay? So, like you can set up all the rules like say inbound rules add new rules rule number 100 HTTPS, like whatever the traffic you want to allow. Or this is incoming, right? Whatever the traffic you want uh your you want to go inside your subnet, you can say that. That you can add. So, let's say I only want HTTPS. Okay? Any IP from sources any source but it should be HTTPS 443 only, allow it. Okay? Outbound rules also you have to specifically set. Added outbound rule. Add new rule. Whatever the rule number whatever the traffic you are interested in let's say only outbound should be let's say only SSH destination could be anything, allow or deny. So, it's a rule like depend upon your company to company like so, what project you are working on, what traffic you want to incoming and what traffic you want to allow to go out of your is uh subnet. You can decide at your subnet level. Okay? So, but the major agenda of mine is that at least we should be clear what is NACL and what is security group. NACL very simple term is that it associated with a particular subnet. So, that any traffic going inside when within this subnet applies to all. Security group is associated with particular instance or host. Okay, like our EC2 instance. So, if that is clear, then we are good for now. But, as when we create let's say in future you will create a project, you will get a requirement that our EC2 instance should receive only this incoming request. Okay, or in this outgoing request should be allowed. Now, you know that whether you have to put it into NACL or security group. And that's our major goal here. What they are. Okay? So, that's a quick video on security group and NACL. Another, before I like wind up this topic. See, now let's say you are the engineer who set up all this thing. You have set up all this region, VPC, public subnet, private subnet, internet gateway. Okay, you have done a setup through console. Now, you left the company. Now, someone some other engineer want to do the setup again. So, that is one is that the knowledge sharing. Another problem is that let's say if you remember multiple region one, region two. You have already done the setup in region one, all this. Now, you want the same setup in region two. Will you do manually all these things? So, generally all this configuration are not done manually. It is performed through you can say that Terraform. Okay, so I think it's a infrastructure as a code. So, all this infrastructure which we have created, VPC, private subnet, public subnet, uh your uh CIDR, your NAT gateway, internet gateway, we have written one file. And we have described everything there. Now, when we run that file, it automatically create everything for us. Okay, so all developer has to do is save this file. So, you can say that it's a knowledge save, so that whenever you have to run it again, you have to create the same structure again, you just run the file. You don't have to manually create that, which could be error-prone. So, if you want comment, I will also try to cover the Terraform, like the same which we have created manually, we will see that uh how we can create it through Terraform also. Okay, guys, thank you. Bye.
