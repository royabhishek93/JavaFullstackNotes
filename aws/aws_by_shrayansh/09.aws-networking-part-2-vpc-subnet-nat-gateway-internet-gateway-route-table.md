# AWS Networking (Part-2): VPC, Subnet, NAT Gateway, Internet Gateway, Route Table

**Video ID:** A5tDuJVAVEA  
**Source:** YouTube - AWS by Shrayansh  

---

Now in the part two, we will be mostly discussing with AWS network perceptive. So in the first part we have already covered this and we have made one or understanding about this diagram. Okay. Now today we will discuss with AWS network term. We will discuss about VPC, virtual private cloud, subnet, AWS route table, NAT gateways, internet gateway. But if you have understood this fundamentals and we have a good understanding of this diagram, this would be just a piece of cake. Okay? So mostly would be like a setting up. Let's start with first VPC, virtual private cloud. It is nothing but a private network block defined by the CIDR range. The same thing in the network fundamental, we have created a network and we have assigned a range of IP addresses, right? In AWS network, this is known as VPC. A private network. Okay? A private network defined by CIDR range. So here we have a region. Let's say AWS region, let's say your Mumbai. You have a region. Now within a particular region, we create a VPC, the private network. And I have defined a CIDR range, 192.168.0.0/23. The same thing we have already seen in the part one also. So means how many IP addresses this range can hold? 32 minus 23. Okay? So here if you see, total nine. So 512 IP addresses it can hold. Okay? So that's my requirement. I just have around let's say 300 IP addresses I want, but the next safe part is that okay, let's block this many private IP range for our network. So how to set up this VPC in AWS? Let's go step by step. So, now let's say this is the console. Okay? Go to all services. Select VPC. Okay? Create a VPC. Now here if you see option, multiple options. So, we have to use currently VPC only. Because see, we want to set up the subnet ourselves. We want to set up the route table ourselves. So, we are learning. Okay? So, we don't want anything to be abstracted from us. So, we say create only VPC, don't do more. We will do ourselves. The first thing is give the name. Okay? So, we have reached till all services VPC. Okay? Just See, you are creating a VPC in a particular region. So, the same diagram. So, in a particular region we create a VPC. Okay? So, currently I am in Mumbai. So, here currently I am in Mumbai region. Within that region we are creating a VPC. Okay? So, VPC only. Give any name. So, VPC hyphen concept by Shreyansh hyphen prod. So, this is the name I am giving. VPC hyphen concepts by Shreyansh hyphen prod. Okay? So, I have created this. See, you can have multiple network in a particular region. Like prod, sandbox, another network for let's say UAT testing. So, currently I'm creating one network. Okay? So, for the prod. So, that is done. Now here, the CIDR range. The CIDR range we want is that 192.168.0.0/23. So, this range I wanted. Okay? So, let's copy this one. 192 168 .0.0/23. Okay? I hope by the part one you already can read out this. When I return this, you know what does it mean. How many IP IPs, private IPs, are available within this range? What would be the lowest range? What would be the highest range? Okay? So, this many IP, so I think 512 IPs would be available for us. And within that, the first 192.168.0.0 is for the identification of the network. And 192.168, the highest one, the maximum one of its range. So, 255, I think dot one. Okay? So, 23, right? 8 8 16 Uh 17 18 19 20 21 22 23 the 24. 1 2 3 4 5 6 7 8. So, okay. So, 8 bit 8 bit and 23 and the 7 bits. So, these are fixed. Okay? These are fixed. The rest 8. These can vary. So, this all one eight, so that's 255. Only one bit left here that maximum could be one. So, 192.168.1.255, that can be maximum, which is booked for broadcast. So, around out of 512, 510 can be used for our host. Okay? Approx. So, there could be, let's say, an another IP address, let's say, one or two, which could be used for uh you can say that a specific purpose in AWS. But for sure, we know that the first and the last is reserved. Okay? So, always assume that 510, so always assume that 500, safer version, that this many hosts would be available for our network. So, that's why I always say that uh whatever the exact you have always uh the network range which you will select the IP range always have some buffer present. Okay? So let's create VPC. So our VPC is created. So here what we have done is we have provided the VPC only, we have given the name, we have provided the CIDR range. So means around 512 IP addresses we'll get and create VPC. So VPC is created and also notice that one default route table is also there. So here if you see route table. So currently consider that as a default route table. Generally in production also it is not recommended that this route table need to be used. It's just you can say the default when any within a particular network when any we will create more networks. I will we will soon see that. If those doesn't have a route table then this default can be used. Okay? But for now just see that it has one default route table. Okay? So currently our diagram looks like we have an AWS region. Within that AWS region we have created VPC with this IP address range. And which has a default route table. So nothing has that route table identifies that hey this network this CIDR is your local. Okay? Currently this is we have done. The second important AWS concept is subnet. Now the big network is divided into small subnetworks. So let's say that in our big network there are total 500 hosts. Okay? So what we can do is we can create a small small subnetworks. Okay, let's say this subnet has only 100 host. This has 100 host. This has 100 host. So, you can further you can do segregation. Okay? So, for example, our VPC 192.168.0.0/23, we know that it means around 500 plus host can be added into one VPC. Okay, that we have already know that. Now, we can create a small sub networks. Say around 100 IPs address in each subnet. Let's say this is your requirement. In each sub network, you want maximum 100 IPs allocation. 100 IP 100 IPs here. Like this you wanted. So, this you have to assign CIDR for this subnets, also. So, now can you pause this video here? Can you compute the CIDR for your subnet? Which requirement is that I can have around My requirement is that 100 host should be able to accommodate in a one sub network. How do you find out? See, currently our VPC is this, /23. 192.168.0.0/23 means total 512 host it can accommodate in a network. So, in a binary term, it can be look like this. /23 means eight, eight, and this seven, yellow one. These are fixed. We cannot change it. Okay? The rest, this can vary for the host. Now, I want 100 IP address in one subnet, in one subnet. So, means how many like say /23 if you go with the /23, you can have 512 host. So, what should be the perfect fit? So, let's say if you go with 2 raised to power 6, you will have 64 which is let's say too small. 2 raised to power 7 is 128. This is perfect for us. Okay, see everything is in binary. 2 raised to power 0, 2 raised to power 1, 2 raised to power 2 like this. So, everything is in 2 raised to power. So, 2 raised to power 7 gives us 128 which matches our requirement. 100 host I want. The nearest which fit our requirement is 2 raised to power 7. So, 32 bit minus 7. How much? You get 20 five. So, means if this many bits we can fix it. Okay, so here if you see, if /25 [snorts] I can use then I can have 128 host possible. So, that's what I do. In the subnet one, I created this CIDR. / 25. 192.168.0.0/25. Now, how do you read this? So, see your VPC has already fixed this 23 bits. We can't do that. But, since we use /25, means in your subnet one, you are fixing additional. So, this two is fixed by your subnet. Okay? Now, the rest only can vary. The how many left? 1 2 3 4 5 6 7. This can vary. So, seven can vary. With that /25, seven can vary means 2 raised to power 7, 128 host is possible. So, for the subnet one, I am using this range. 192.168.0.0/25. 0.0 means uh this is zero. This is zero. And this is your 192 and this is your 168. So, this is your lowest range and the maximum range can be 192. 168 and this is a still fixed, right? So, this is also zero. And this is one bit is fixed, right? By your subnet. Rest can vary. So, all could be one here. So, this is the maximum I can get. So, this is your 127. So, this is my IP range for this subnet. Total 128 IPs. Okay? Subnet two. What is its CIDR range? Can you compute it? So, let's say I am using this. 128. So, very simple way is that first write the binary. So, you know that this 23 this 23 is fixed by your VPC. Yellow one. Okay, we can't change that. Now, {slash} 25 we are using means another two bits are fixed by your subnet. So, when your VPC was there, so this is still can vary. So, now in the subnet, I have just fixed this zero one. Okay? I have fixed this. So, for my subnet 192 168 zero {dot} one {dot} one. Okay, not one. This is uh 1 uh 1 2 4 8 16 24 like 128. Zero, one. Two raised to power zero, two raised to zero two four eight 16 32 64 128. So, this is your lowest range. The maximum range could be like these two are fixed and the rest can be all one. One one one one one. So, total 255. So, this is your highest range. This can be your one subnet. So, you got it, right? I know it's a little confusing when you work with binary. So, but once you understand or able to read this CIDR, you it would be easy for you. So, again, one more time I will go with for the subnet three. You do it by yourself that uh Let's say This is the binary. This VPC /23 is fixed. Okay? And rest can vary earlier in the /23 one. So, for this subnet, I have used unique 910. This one I have fixed for it. Okay? So, 192.168 This I have fixed, right? So, this is your one. And the lowest range would be like all zero. So, this is your lowest range. Maximum range would be 192.168. This is one. This is fixed, right? 10 is fixed by your subnet. Only this can vary. So, this is your 120 seven. Okay? So, 192.168.1.127. This would be your range for subnet three. Similarly, you can have for subnet four. In simple term, your VPC has this range, which has 512 hosts it can accommodate. So, what we have done is we have created multiple subnets. Okay? And each subnet is created within one available zone. In global infrastructure video, I have already told what is region, what is available zone. Okay? You can check it out. So, each subnet is part of one available zone. So, I have created this subnet and its range. So, you can say that it's a subpart of this only. So, out of this 512, 128 host 128 hosts are available for this subnet. There is another AZ. Within that AZ, I created another subnet B. It also has 128 host and its range. Okay? So, this subnet has its own routing route table. This subnet has its own route table. And this VPC has its own route table, but generally this route table is not used. I will tell you why, because each subnet should maintain their own route table also. Okay? And now whatever the instances you will create that belongs to one subnet only, within that subnet. Okay? If you create the EC2 instance, that will be part of this one particular subnet. So, at this point of time, we have created VPC and we going to create a subnet which would be part of one particular AZ. Okay? And they have their own range. Now, before I do a setup of subnet, there are two type of subnets, private subnet, public subnet. Very important part. Private subnet routing table do not have entry for internet gateway. Okay? If you remember from this diagram, internet gateway is the one through which packet goes to the internet. Okay? So, this subnet, so you can say that a private subnet is the one that do not have entry for internet gateway in the routing table. So, this is the routing table, right? So, let's say destination 0.0.0.0/0, target should not be internet gateway. If they have an entry in the internet gateway, that would be considered as public subnet. Okay? That would be considered as this subnet would be considered as public subnet, because their route table has entry for the internet gateway. If any subnet doesn't have in their route table, doesn't have an entry for the internet gateway. Those are known as private subnet. Okay? But then you question that how the private subnet will talk to the internet world. You remember the net? That concept will come into the picture. So, it's let's say 0.0.0.0 any traffic comes which need to go outside talk to net. It's address. Net address would be present. Okay? So, private subnet is private subnet route table do not have entry for internet gateway. The host present in private subnet uses net gateway to talk to the internet. Generally our EC2 instance database we keep in a private subnet. We will see with an example. Public subnet are like the routing table which have entry for internet gateway. Any host sitting in this subnet has public IP address and can directly talk with the internet. What does it mean? Now, let's say this is public subnet. This public subnet means its route table has destination to the internet gateway. So, now if you create any instance in it, it will also get one public IP address. So, it will have public IP address and its route table it also has internet gateway. So, means it can directly talk to the it can directly talk to the internet gateway and from internet gateway it can directly talk to the internet. And similarly the traffic can also comes to this particular instance. Okay? So, in simple term, any host or any device present into public subnet. So, this is your public subnet. Any host present into this public subnet has public address, public IP address. Also, the route table has address for the internet gateway. The route for the internet gateway. So, using that because this is route table is associated with the subnet. So, means all host can use this route table. So, they have a public IP address this node this host and from the route table it knows that okay, any traffic comes which need to go outside. It has the internet gateway entry. So, it can directly calls to the internet gateway and from internet gateway can goes to the internet. But, private subnet don't have an entry in the route table for the internet gateway. They have only for the NAT gateway. Okay? So, our load balancer, our NAT gateways generally present into public subnet because they have to talk outside. So, understand with this diagram now. Very important. So, we have already have AWS region, VPC we have set up. Default route table we know we have created, but we are not using it. AZs we have already seen okay, how to create and all in the previous global infrastructure itself. Okay, now the public subnet Here if you see we have public subnet, private subnet. Public subnet you know that it's route table has entry route for internet gateway. So, means any traffic which need to go out, it knows that it need to pass to the internet gateway. From internet gateway it can route to the internet. The private subnet it's route table has entry any traffic which knows goes to the outside, it pass to the net. Not to the internet gateway, net gateways. So, net gateway is present into public subnet. Why the net gateway device? So, you know that, right? It's a router only. It's a software which present into router. So, it's a host. So, where this host exit? Whether into a public subnet or private subnet? Think for yourself. Just pause and think. So, see net has to talk to the internet gateway, right? So, it has to be uh uh present into public subnet. So, all we provide is an address. That in the private subnet, whenever any destination is outside of the network, call net gateway. So, internally AWS make use of internal routers and pass the request to the net gateway, which is present into public subnet. Net gateway use this route table destination and pass it to the internet gateway. So, like this is the flow. So, that's why our this private subnet do not talk directly with the internet. And the traffic from the internet do not come directly from the internet gateway to the our private subnet. So, now let's set up a subnet for us. So, we have already created the VPC, right? So, this is the our VPC. Here, if you see subnets. Click here. These are all the default subnets. But, we have to create our create subnet. Select the VPC first. So, the subnet work is just nothing but a uh part of your big network, right? So, big network is nothing but a VPC concept by Shreyansh, which we created. Give the name. The first I am creating is public. Okay, so the first I'm creating the public subnet. Public {hyphen} subnet, and I will put it into availability zone one A. So, there are availability zone. So, I am putting into this 1 A. I told you that each subnet belongs to one particular zone. Availability zone. Now, this is my VPC CIDR. I need to choose the subnet for the CIDR block for the subnet. So, 192 168 for this subnet I am using this range. 00/25. Okay? Create subnet. Okay, so my one public subnet is created. So, till now here if you see what we have done is we have selected the VPC. We have provided the name. The AZ in which we have to put the subnet CIDR and the create subnet. Okay? And one requirement of our is that I told you that any host which need to be created, let's say if you create EC2 instance here, any EC2 instance which you will create within this public subnet should also have a public address. Okay? So, we can enable it here itself. So, this is my public subnet which I created. Go to the action. Edit subnet setting. Enable auto-assign public IPV4 address. So, now in future whenever you will create any EC2 instance within this public subnet, it will also get apart from private address, it will also get public IPV4 address assigned. Because that's where that's what public IP address is required to talk to the internet. So, public IPV4 plus in the route table you should have an entry for the internet gateway. These two things are required for packet to move to the internet. The host should have a public address and the route table should have an entry for the internet gateway. So, that's what we have done. Okay, so here in the added subnet setting for the public subnet I have clicked auto assign. Save. Now, one subnet I have created. Now, we have to create a private subnet. Okay, the same thing. Create subnet. VPC again, my VPC only. Now, I am creating private subnet. Private {hyphen} subnet {hyphen} now in another availability zone. So, I am just going to put this subnet into availability zone 1B. So, that's why I just added {hyphen} 1B just for my clarity. That this subnet present into this availability zone. And it's CIDR. So, CIDR I'm using is this one. 192. 168.0.128 /25 Okay, I hope that now you can easily know that this CIDR, you can easily compute the low range and the highest range. Okay. Create subnet. So, subnet is created. Okay, so we have created. So, we don't want public IP address for any EC2 instance created into this. So, that's why I am not enabling that part. Okay, I don't need for public private subnet. We don't need this. Okay. Okay, so now we have created subnet. So, here AWS region done. VPC done. Default order table, it's created. We have set up a public subnet. We have set up a private subnet. But currently their route We haven't created We haven't created net. We haven't created internet gateway. Now, let's set up a internet gateway. So, you know that what is internet gateway? Internet gateway is just a gate through which your packet ultimately leaves for the internet. Okay? And for the packet to reach to the internet or pass through the internet gateway need two things. One is your subnet should have a internet gateway route in its route table. Let's say our public subnet should have this entry. Okay? And the host from the request which is coming should have a public IP. Okay? And uh any host which we'll create in this public network, we have already set up that they will get a public IP also. So, these two things are requirement. So, now let's set up internet gateway. So, go here VPC. Internet gateways, click here. Create internet gateway. So, we will create ours. So, let's give it a name, concept by Shreyansh {hyphen} internet gateway. Concept by Shreyansh {hyphen} internet gateway. Okay? Just create. Now, we have created it. Internet gateway, we have to associate it with our VPC. So, this is our gateway. Action, attach to VPC. The VPC which we have created, concept by Shreyansh. Attach it. Okay? So, we have attached this internet gateway with our VPC. Okay? So, now you can say that internet gateway is now attached with our VPC. So, internet gateway we have also set up. Now, net gateway we have to set up. You already know net gateways, right? So, the same thing. The in the previous video itself the network fundamental I explained all the private host traffic moves to the internet gateway through net gateway only because it maintains the mapping table. Kindly check it out. The same thing. So, I will just read it out so that you can recall it. It's a gate between private network and public network. It removes the sender IP address, which is like your private, and replace with its own public IP. Okay, so net net gateway also have its own public IP. So, whatever the packet which comes, which has private IP only, it replace with its public IP. And then it will pass it to the internet gateway. And when the response come back, it will like forward it down to the original chain, like original host. Okay? Another main advantage of net is that it's only support outgoing. Very important part. Very important part is let's say that host this EC2 instance present into private subnet. So, it has to initiate the talk with the internet world. Now, let's say it has to download something. Or it has to open a particular website. So, it calls the net. Net calls the internet gateway. Internet gateway reach to the internet. Now, the response which comes, it will check in the net gateway. I told you that it has a mapping table. Okay, it has a mapping table like this. So, when the response comes, it only allows when the entry is present that okay, the destination is this. I need to pass it to this host. Then only it will allow. But, let's say this entry is not present and a fresh the fresh connection is been trying to establish by some user out in the internet and they are trying to reach to this private. Will it be allowed? No, it won't be allowed by NAT because it don't have any entry into the mapping table. It don't know which host I need to pass to. And anyway, like uh your user doesn't know the private subnet, so they can't directly talk with the private IP itself. Okay? So, that's why it's always said that NAT is always outgoing. Outgoing means the traffic should be initiated from private to NAT to outside world. Because with that only we will have a proper mapping there. Your packet which has a private IP after NAT it's get changed to public IP and then only it will ultimately talk to the internet. Okay? And only the response now when it's coming now this mapping table has an entry so that NAT is properly able to do the conversion of this uh destination port which is coming in the response to this original host where it need to be sent. Then only it will work. Okay? So, now let's set up a NAT gateway. So, very important part and one more important thing before I uh set up a NAT gateway that it is chargeable. So, for learning purpose we will set up and if you don't intend to use it after setting up you can delete it. Okay? So, go to the VPC NAT gateways create a NAT gateway. Okay? Now, set up a name. So, let's say I'm using concept by Shashank {hyphen} NAT. Okay? Zonal I want it in which particular zone you know that NAT gateway need to be put inside a public subnet. So, which subnet So, we have created one this right? Public hyphen subnet one A. So, I want to put just recall from this diagram. This net gateway need to be put into public subnet only because from this net gateway traffic has to go out to internet gateway. Okay? So, it has to be in public sub- subnet because it's route table. We will keep this entry to the internet gateway. So, that's why in the zonal for the net gateway we need to put into public subnet. Okay? Elastic IP. This is very important. This is like allocate elastic IP. Again, this is chargeable. And after doing testing and all, you can have to delete the elastic IP also. I'll show you how you can delete. So, elastic IP means that it will allocate one public address. Public IP address to this net gateway. And this public IP would be permanent. Even if you restart this device, you will always get same public IP address. It won't change. See, this is important. Now, assume that traffic coming from net you have a public IP let's say X Y Z. And it goes to the internet gateway and to the internet. Now, let's say response comes. What if you restart this service and this address got changed to ABC? But when the response come from the internet, it has a destination address that hey, this is the XYZ. Will the packet will able to reach till this net? No, because now it's public address is changed. The packet won't be able to deliver to this net itself. So, it's very important that our net should have one permanent public address. So, even if it restart, it will always get XYZ only. So, that's why we need to have a elastic IP. Means public IP address would be assigned, but permanent one. So, let's create NAT Gateway. Okay. So, the same thing. Provide the name. Zonal. Important is that this NAT we will put it into the public subnet. Allocate elastic IP. Permanent public address. And save it. Please note NAT Gateway and elastic IPs both chargeable, so delete it if you don't intend to use it. We will delete it once we are done. So, what we have done is we have set up region, VPC, default route table, public subnet. Now NAT Gateway. Private subnet. Internet Gateway. Now we will set up this route table. Okay. So, goes to the VPC. Route table. Okay, these are default. We will create first uh our public route table. So, the name let me try to keep it the same. Public {hyphen} subnet route table. So, this route table is for public subnet. Route table. So, this belongs to VPC concept by Shreyansh. Let's create a route table. Okay. The one thing is first we need to associate with the particular subnet. So, this I am creating for public, right? So, associate it with public subnet. Okay. Save association. So, I have done this one. So, I have associated this public route table with this public subnet. Now I need to have an entry for this internet gateway, because this is a public route table. So, edit route. Add route. Any traffic apart from this. So, if it is within a network, it goes to the local. But, any other traffic apart from this network, it should goes to internet gateway. So, internet gateway, this is we created, right? Concept by hyphen internet gateway. Save changes. Okay, so we have put an entry, and we have also attached this public route table with our public subnet. So, this part is done. So, create route table, the name, attached with the VPC, created. After that, you can have an entry, add route, any traffic which need to go out, connect with our internet gateway, save changes. Similarly, need to associate whatever the public subnet you have created. Action, edit subnet association, select with the public subnet which you have created, done. So, public route table is also done. Now, private route table we have to create it. The same thing, VPC, route table, create route table. Now, I am creating this for my private subnet. Route table, the VPC is again concept by create route table. Associate this with the private subnet. Save association. Now, I need to add it route that any traffic which need to go outside apart from my local network, it should go to the network gateway. Net gateways. The net gateway also we have created concept by hyphen net. Save changes. So, we have added this entry, net gateway entry, and associate this with our private subnet also. So, that's the same thing. We have done. We have associated it. Done. So, we have done everything. Public subnet, uh public subnet, private subnet, NAT gateway, internet gateway, route public route table, private route table. All we have to do is just now verify. Go to the VPC, your VPC. This is the VPC we created, right? Concept by Sharan. Click here. Now, see this. You would be able to find this. The public subnet is connected with public subnet route table, which ultimately connected with this internet gateway. The private subnet is connected with this private subnet route table, which ultimately connected to the NAT, not internet gateway. Okay? So, now when we create an EC2 instance, we have an option whether to create in public or in private. If you create in public, means it will automatically get one public IP address, and it can directly talk with the internet gateway. So, that's what If you go inside VPCs, this is the default one, which we have not created. If you go inside one, see, it creates subnet. But, here if you see, they all connect directly. Each subnet is your like a public subnet. So, they are this subnet, this route table, directly with the internet gateway. So, this was the default. But, that's why I told you that default is never used. Why? Because any EC2 instance you will create within this subnet, default network, it can directly talk to the internet. And from internet, they can directly talk to your EC2 instance. But, the one which we have set up, we have created two subnets, public, private. In the public, we have only put the devices which need to talk to the internet like net gateways. Apart from that our EC2 instance, database, all we we will put into private subnet. Okay, so they will talk with net and from the net it the route will go to the internet gateway, not directly. Okay, so no one from internet will directly able to access our EC2 and database. Okay, now when you go to the EC2, you try to create launch EC2 instance. Now after providing the name network setting when you do add it, now you select your VPC. Okay. The subnet you want to put into public or private. Let's say I want to put into private. I don't want outer world to connect to my EC2 instance. Okay, so this thing now we have done. All is now security group which we will see next. But now at least we don't have to use the existing default one. Okay. So verification we have done. So clear? So we have created this network, AWS network. So public subnet public subnet private subnet is very important. Because ultimately the device we will create, host we will create, EC2, database, they have to be part of one particular subnet only. Okay. So whatever need to talk to the internet, we will put into public subnet. Whatever don't need to talk to the outer world, we will put into private subnet. And through net we will connect to the outer world. Any doubt, let me know. We can discuss further and see you in the next part where we will discuss the security group. Okay. Okay guys, bye.
