# AWS: Terraform Architecture & Core Blocks Explained

**Video ID:** 1Q9SBeqAF2Q  
**Source:** YouTube - AWS by Shrayansh  

---

Hey guys, Shawn this side. Welcome back. And today in the AWS series we are going to start with Terraform. Okay. So till now what we have done is we have already set up the network like we have created VPC within a region. We have created multiple subnets within this VPC. We have a public subnet. We have private subnet. Public subnet is connected with the internet gateway. Private subnet we have connected with the net gateway. And we have created route tables and all. But the problem with this is that we have created all this thing by clicking on the console. But in real world in companies if you work nobody create the infrastructure using this like this way like clicking on the console. The problem is that like let's say in some other region currently you are in region A. You have all this infrastructure set up. Now in region B you have to done the same set up. So you have to manually do the all the things. Okay, so there is a chance of mistake. Also if let's say some someday someone has changed certain value in this infrastructure. There is no way you can check the history like okay, what was the value before? Why it is changed? What's the purpose? So that's why infrastructure is never you can say that created using clicking on the console. So that's where the Terraform comes into the picture. So generally if you see in here our AWS there is something called cloud formation. This is also you can say that similar to Terraform which is used to create infrastructure. But as a file like as a code. Okay, infrastructure as a code. But cloud formation generally very specific to AWS. But, Terraform is very, very popular and you can say that it is like a framework. On top of it, we can write for Azure, we can write for AWS, we can write for Google, etc. Okay? And to be honest, Terraform is very, very easy to understand and learn. Okay? Even if you are learning it for the first time, right? As when we proceed further, you will see that hey, it's very easy. It's just like reading. Okay? So, let's see that. So, the Terraform, I already showed you the problem it solved. The problem is that we earlier we were doing through console, which which if you have to replicate, it can cause multiple human errors. Second, if something is changed, we don't have the history what got changed. So, Terraform is nothing but an infrastructure as code. Infrastructure as code means like we create .tf extension files. And we can put these files into Git and you can manage its history like how we used to manage the version in the Git, right? So, you can let's say you have let's say currently version one and the configurations. Now, if anybody has to change, they will raise a PR. Now, you can review. So, there is a proper process. You can review, then merge. Now, tomorrow if anybody has to change see that hey, this value, what was the previous value? You can easily check. Why change? What change? When change? So, everything can be properly, you can say that organized and managed via code itself. So, since these files are just text, we can store them in Git to to the version history. This allows us to track what changed, when changed, who changed, and why changed. Now, Terraform is like not an imperative infrastructure as code. It's a declarative infrastructure as code. See, there is a difference between imperative and declarative. Imperative means I am saying that, "Hey, first create VPC, then wait for it to complete. Then create public subnet. Then create private subnet. Okay? After that, you have to create internet gateway. After that, you have to wait. And after that, you have to create NAT gateway. So, you are providing the sequence when to wait, when to start, what first, what second, which API to call. So, that's imperative. So, like you are giving a proper instruction, like create VPC, then wait. Then create subnet, then wait. Then attach subnet to VPC. Create internet gateway. Then attach internet gateway to VPC. So, you are giving proper command. If you miss one proper command, or if you miss certain sequence, there is a chance that you won't get the desired result. So, in the declarative result in the declarative infrastructure as code, it's more like just tell me what you want. Hey, I want VPC and two subnets within it. It's like a command you are giving. I want a VPC and two subnets within it. That's all. So, now it will decide its own order that I have to create VPC first, and then create two subnets, and then I have to attach it. All I have to told is that VPC and two subnets within it. Okay? That much information we have to give. But we don't have to provide like all the minor minor details. Got it? It's like consider like providing a recipe for making tea or you just want I want tea. Like that. Okay? So, here in the declarative infrastructure as code means we describe only the desired end state. That is clear? Terraform is declarative infrastructure as code, not imperative infrastructure as code. Now, let me quickly tell you a Terraform architecture. See, we don't have to go inside Terraform and debug everything. Okay? That is unnecessary. We will complicate the thing, but I will try to keep one level higher what's the architecture of Terraform so that when we proceed further, we would be able to connect like what is happening. The very first is your configuration files, {dot} tf files. Here we will write our declarative instructions. That hey, create VPC, create public subnet within this VPC, create private subnet within this VPC. Like this, we will give the command. Cool. The second very important part is the Terraform core. This is the major you can say that the brain of the Terraform, Terraform core. So, it has five step. So, let me zoom it a bit. The first task of the Terraform core is to read and parse all {dot} tf files. So, read See, you can have only one tf files or you can have multiple {dot} tf files. Okay? Doesn't matter. You can like properly manage by creating multiple tf files that okay, only VPC related I will create into this Terraform files. And uh gateway related I will create in this or you can create into one itself. Doesn't matter. The Terraform code read all files which are ending with .tf and parse it. That is the first job. Second job is is to validate the syntax of .tf file. Like is there any missing bracket, any duplicate blocks, etc. So, second thing is validation of the .tf file. The third important part is the dependency graph. I told you, right? It's a declarative. We don't have to tell the steps. But, it has a built-in, you can say that knowledge, like how to create a dependency graph. So, using this dependency graph, okay. Hey, user told me that I need to create VPC and two subnets within that VPC. So, dependency graph, it will create like, okay, VPC, after that I have two child nodes, uh subnet one, subnet two. Let's say if you have something like, okay, uh create a net, or you can say that I'm just saying something, create a net gateway, attach with this one particular subnet. So, it will say that, okay, within this subnet, I have to attach net gateway. So, it is creating some graph, that, okay, first I have to create this, then this, then this. So, it will automatically determine the sequence of creation of your resources. Got it? The fourth very important part is a state manager. What does it mean? So, see, you have given the command, like, hey, I need to create VPC, I need to have So, this is your infrastructure as code. You have gre- You have said that I need VPC, I need subnet within that VPC, I need net gateway within that subnet. Let's say you have this infrastructure as code. Okay, now let's say you have run it first time. Now you have run it for the first time. When you run it for the first first time, let's say in the AWS console, you can see and check that a VPC is created. Within that VPC, you have subnet created. And within that subnet, you have net gateway created. So, this is created. And each one has an ID. ID ID ID resource number ARN number. Okay? Now, let's say this is an infrastructure as code. You can run it second time also. So, will it create a duplicate? No, that's where the state manager comes into the picture. So, what happened here is Terraform also maintains, apart from TF, one the first time you run it, or you can say that every time you run it, it also create dot TF state file also. Okay? So, it create dot TF state file. In that, let's say once it created this, it has complete information. Okay, VPC this is what we wanted. Let's say whatever we wanted. We wanted that this is the CIDR range. I think I hope this is CIDR range when I'm saying it, it's already clear because we have already done it. Okay? This is the CIDR range. Now, in the response, like okay, this is the ARN number. And let's say this is the ID or this is the name, something. What is the response I got? So, in the TF state, I have this complete information. Okay, this is the subnet. For subnet also, I have one CIDR range. Okay, that's what I wanted. But once it is created, what is the ARN number? Okay, uh and certain what is the name etc. All the outcome you can say that it is stored into dot tfstate. Similarly for the net. Now, when you run it for the second time, now I'm running it second time. What it do? It first compare your TF file. Okay. In the TF file, you have a VPC. Here, I want to create a VPC and this is the range something. Okay, 198.0.0.0/23. This is the CIDR for the VPC I want to create. I want to exist. I want to have exist. So, when you run it for the second time, will it directly create this VPC within your AWS resource? No. Now, it will do certain work. It will first check dot tfstate. It will check okay, the VPC which I have. It will check the ARN number. It will call your AWS API and check. Hey, can you describe this ARN? So, it will send a response. Now, it will compare everything. Okay. Here, I have this VPC already created for for this ARN has 198.0.0.0/23 already. Okay, so this is the details are coming for this ARN. It also match with its own. Okay. The CIDR range is also I have the same. So, nothing change in console and what I have. Nothing change. And what user wanted is also the same. So, means I don't have to create. So, you understand what it is doing? It is not blindly go and create the resource. So, it does three things. Compare three things. Dot files, .tf file considered like what we want at now. Because you are running, right? So, this is like what we wanted. .tf state is like what exist or you can say that what previously created. What previously created. And console is like a source of truth like what previously created is this the same exist in the console also or not. Because we can just can't rely on .tf state. What if somebody directly on the console has changed it? Let's say. So, this is what you want now. This is the VPC and this is the CIDR range. What previously has been created? This is the details I have. Using the ARN number, it just invoke AWS API, check the current that hey, for this ARN, is this everything same, right? Yes, everything is same. Match. Nothing is changed. And the same you wanted now. So, means I don't have to create something new. So, state manager. Task is to find out what all resources already exist. So, that it should not create duplicate resource. Then the last is plan generator. Plan generator is using this information, it will now know that okay, resource one I have to create. Resource two I have to update or resource three I have to delete. Something like that, a plan would be created. So, important part is that just understand that .tf is declarative. Let's say earlier, first time, this is your console. You have like resource one, resource two. And now when you run it, two resource got created into the console. Resource one, resource two. And in your dot tfstate, you have information about this two state. Resource one, it's all information. ARN number, everything. Resource two, it's ARN number, everything. Now let's say that these are just file. Now you have removed resource two. So earlier first time you run it, so you have all this. Now you have removed the resource two. That's all. And now you are running it second time. Now what would happen? Will it directly remove it? No. So first it will check the dot your tfstate. And it will compare. First resource one, resource one, this is what you wanted, this is what I have, and this is what there in the console. All same, so no change. Okay? Resource two, resource two, this is you don't want it. Okay, but I have it. Dot tfstate, dot tfstate, I have it. In the console also, I have it in the same. But now you don't want it, so means I have to delete the resource two. Okay? So now from your dot tf file itself it determine whether I have to create, whether I have to delete. Okay? So it create a plan, plan generator. What resources it will create, what resources it will update, what resource it will delete. There are certain resources which is allowed to update. If not, you have to delete it and create new. So with that one it create a plan. So you can say that estate manager is something helps in that. So Terraform is Terraform core is something the brain of your Terraform. That is clear? Now there is something called provider plug-in interface. So, this is as the name say it's an interface. Like ultimately your Terraform has to talk with your AWS or Azure or GCP or multiple providers, right? So, we can't have directly uh like have all this hardcoded into Terraform core. So, that's where the provider plug-in interface. So, all the APIs which Terraform code needed, let's say create, update, delete, read, something. So, it invoke this interface only. So, interface is just like no implementation. It's just like API it provided. Which Terraform code knows. Now, whichever the resource you are using, let's say AWS or Azure, they will provide the implementation. Okay? So, provider plug-in interface is nothing but exposing the endpoints which Terraform code required to create the resource, to delete the resource, to update the resource, to read the resource, etc. Or Now, this is just an interface. So, we need an implementation of this endpoints. That's where your provider specific plug-ins comes into the picture. AWS provider plug-in or Google provider plug-in or Azure provider plug-in. Like let's say if you are using AWS, then we need a AWS provider plug-in. So, this provider plug-in provide an implementation of this create, read, update, delete method. Okay? And now its task is to validate the arguments. Okay? Because now let's say it will say create VPC, let's say. Create VPC. So, to create a VPC it need to have certain arguments, right? I need a CIDR block. So, this is very specific to you can say that uh AWS AWS need to validate its own that there is no direct method that create VPC. Let's say create only. Now, within the create it has let's say resource conflict. Within the resource conflict it has let's say for create for VPC. Now, AWS provider plugin has to validate that okay the resource conflict is let's say for VPC but I need this argument mandatory. CIDR should be present. Okay? And rest are optional. So, it validate now what all the arguments which are required to create the resource in AWS that is valid or not. Okay? The Terraform core validation was very specific to .tf file. Okay? So, there are difference. There is a syntax for .tf file. Okay? Now, in that .tf file we are saying that again we need to create a VPC and these all values. Now, within the VPC these all values Terraform core doesn't know. Okay? So, let's say within the VPC all these arguments has to be validated by AWS that okay the argument which you are passing whether it is valid or not. But from the Terraform core core, okay, you have you provided the name of the resource. Okay, bracket is start bracket off. Yes, syntax is right. But now the resource which you want its argument I don't know. Check with the AWS provider plugin. It will do the validation. Now, if provider plugin what it does it it just have embedded AWS SDK code. It's not that you can say that separate. It's just an SDK which your AWS provider plugin itself embedded. Internally it has this. Now, it will invoke the AWS API. AWS API call the AWS resource. Okay? So, let's say provider plugin mostly do provide an implementation of this method, do certain validation, and you can say that internally invoke SDK. SDK what it will do, it actually do request creation, and ultimately invoke it, and the response comes, it will do parse the response because it knows that, and ultimately pass like this. Okay? So, this is your sequence. You write a configuration file. Ultimately, it goes to the Terraform core. Terraform core invoke an endpoint in the plugin interface. Depending upon which plugin you are using, it goes to the AWS, let's say provider plugin. Uh AWS provider plugin internally use make use of AWS SDK to call an AWS API. AWS API ultimately call the AWS resources. Clear? Generally, this is the high-level Terraform architecture. And these are the tasks which Terraform core does. Very important. Now, let's come to the next part of Terraform blocks. So, see, everything in Terraform is written in blocks. Everything. A block is like a container that hold information. There are different blocks. Okay, I won't cover all the blocks. So, gradually, let's say, I will Today, I will only cover those blocks which are sufficient to create the network which we have created. Like we have created VPC, subnet, internet gateways, NAT, and all. So, today I will cover the blocks those are sufficient to create the network which we have done. Like in future, when we cover more topics, and if then more blocks we have to add because everything we will have to create it using Terraform only. Okay? So, it's not like today we are covering the Terraform, it's like finished. It's you can say that gradually we will build the Terraform also. Anything console we have to create, we can create through Terraform. So, the more we proceed further, you can say that we can add new things into the Terraform. So, you can say that this is like Terraform file we are creating. Till now, what we have done. But since the Terraform we are touching for the first time, I'm just creating a building block. Otherwise, once we know that, we can directly create a infrastructure using directly files, which I will tell you in the next part of this video. So, blocks. Everything in Terraform is written in blocks. A block is like a container that hold information. So, you have a block type. Okay? So, block type you can say that this is what Terraform defines. This is a block type. It should be like this. Okay? Label. It's again it's a Terraform defined. We can't do give any label here. Label two is you can say that it is like a name, custom name. You can give any name. And the key value, this is I think about an arguments. We will see that, don't worry about this, but consider that a block is something which has which hold all the information. So, that's what I told you the note, there are different type of blocks. Today we will cover the one which are required to automate the below infrastructure. Like this is what we have created, right? So, we will just cover the blocks which are sufficient to create this. In future, we will add more and we will write its Terraform also. And if required, new blocks need to be covered, we will cover that. Gradually. Okay? So, the first the first important is Terraform block. It helps to configure Terraform itself. It does not create any resource, just configure Terraform setting. So now there, if you see, this is the block I have written. Terraform This is you can say that a keyword. The block you are started. Now within this block, we set up two things. First, required underscore version greater than equals to 1.0. Which version of Terraform core you need? See, this is our architecture we have covered, right? So you can say that required version is like you are decide the Terraform core. This provider plugin interface is a part of Terraform core itself. Okay? So which version? There are different versions of Terraform core present, right? So you are just saying that, "Hey, I want greater than equals to 1.0." So required version for the Terraform. And more specifically, Terraform core, which version you want? The second required providers. You know that you can have multiple provider like Azure, AWS, GCP. Which is the required provider? Like more specifically, you are talking about this too. Provider plugin, which provider plugin and its SDK. Okay? You have to download that, right? So I want AWS equals to. So you can say that it's a keyword. You can't just have anything. So I want AWS provider plugin. Okay, what is the version? Greater than 5.0. From where I have to download? HashiCorp/AWS. You can download this plugin from here. Terraform is developed by this company itself, HashiCorp. Okay? So they provide uh resources where you can download these plugins. So in the short the second part required provider is we are telling first that we want AWS provider plugin like this. And after that, okay, what version of it? And from where I can download it. So now, what we have specified in the first Terraform block is like this. In this architecture what Terraform core we have to download which version? AWS provider plugin, which version? Okay, SDK is embedded into this provider plugin itself. So we don't have to specifically tell for SDK whatever the version of provider plugin you will download it for the specific either Azure or AWS. Their specific SDK is embedded into that. So this is clear, the first block. We are configuring Terraform itself. The second is the provider block. Provider block in the sense is that now we are working with the specific AWS provider block. In this block we can configure the region in which we want to create the resource. Or more specifically you can say that how we can connect with provider. That's defined by the provider block. So now here provider, this is the keyword you can say provider block. And your provider AWS. Again, we can't use any name. This is you can say that like if you are using AWS provider the same we have to use here. Provider AWS. Now inside this we have like a region. Okay? Which region you have to use in this provider. It also has fields like access key. Here you can provide the access key. It also has like secret. You can provide the secret. But, generally that is not encouraged or recommended. Never provide access key and your token here into this provider itself. We can and it has the highest priority. Like if you provide it here, it will always read your token from here itself. But, it's never recommended to add it here because this files this .tf files are generally pushed to get and then your token might get leaked. So, that's why I haven't mentioned here, but I just uh theoretically mentioning here that apart from region, you can also add your access key, your secret key here itself within that provider AWS itself. Because provider block tells that how we can connect with the provider itself. And let's say for AWS specific, what it need to connect to AWS? Your secret key and the region. Because in the region, let's say US East -1, the endpoint which it create is specific to region only. US.East.1.AWS.Amazon. So, the end URL which it create is very specific to region also. So, that's why to connect to the AWS or provider, this provider blocks comes into the picture. And for AWS provider AWS, we need region, we need token to connect. So, region we have provided, token we can also provide here, but it's not recommended. Later I will show you how to provide the token safely. Okay? But, in short, we can configure the region in which we want to create the resource, but it does not create any resource as of now. So, this provider block is like, okay, you want till now you have configured that you want AWS plugin, but tell me how to connect to AWS. Means your password it is wanted in the region. Okay, so password we haven't provided. You are access key and the secret all I have provided region. During run time I will provide these tokens. Okay? Now, the very important part is the resource block. Now, everything your VPC, your subnet, your net gateway, your internet gateway, your route table is all but nothing but a resource in the AWS. So, that's why the resource block. And now when we are using the resource block, read it like many time we read it like create create. So, don't read it like create VPN, create VPC, create subnet, create net. No. Read it like I want this resource to exist. There is a minor difference. When you read it let's say if I have given resource VPC and if you read it like create VPC, in the mindset you have like it will always create. But I told you it has a step a state manager step where it check if the same resource is all already present with same configuration, it won't create it. So, the right way to read it read it as I want this resource to exist. It will make sure that it exist if you have written. Okay? So, this is an example. Resource is a keyword you have to use it. Then which resource you want to create? AWS underscore VPC. Again, you can't use any name in this. What is the name you want to give to this resource? Or you can say that not resource, you can say the block. This block, how you want to call this block? Like a function name. You can say that this is a function and this is a function name. So, I can call it like the main. Okay? And then within that, these are the arguments. Okay? So, resource is a keyword. We are creating a resource. Terraform knows that this is the resource we are going to create. Which resource? Okay? And within this, let's say I we want to create AWS_VPC resource. The custom name you want to give to this block. Okay? And after that, the arguments. Like, okay, to create the VPC, there are certain arguments which are mandatory. You have to pass that. Now, the question comes is that okay, Shreyansh, how do I know that what all arguments are mandatory? And how do I know that if I know if I have to create a VPC, I have to use AWS_VPC. Why can't it be just VPC? How do I know? Because these are like we can't use any name, right? So, see. There is a You can use this link. Terraform registry website. If you open this, okay? If you open this, you will see the resources here. Let's open this one. Okay? Let me copy it a bit. Okay? So, now here if you see, let me zoom it. Resources. Go down, down. J O Hm. This is it. Oh. Resources. VPC resources. Now here if you see, what all resources we have to create. So, you will find everything here. AWS_Net_Gateway. Okay? AWS_Route route table association, subnet, VPC. Now, let's say I create on AWS VPC. Okay, so AWS VPC, it you can see the example also. Here, if you see argument reference, like what all arguments you can pass, like what are optional CIDR BR optional, CIDR BR CIDR block optional. Which one? Like you can get to know like which are mandatory, which are optional. Like here all these things. Then you will see attribute reference, like what it can once you create it, an output what you will get. You will get ARN number, ID. Okay, so this all thing you will get. So we have we don't have to remember this like what all name. We don't have to remember what are the arguments we can pass. We can make use of this Terraform registry and know that, okay, we want to create VPC, okay, this is something AWS VPC. Go inside this, you can know that, okay, these are the arguments I can pass, this is the thing which I can get in the response. Okay? Now this one I have already told you that resource do not create these resources, it just says that resource should exist. That's what I have already told you that a state manager does this. So this is clear. I am not telling you like creating each resource like VPC, subnet. Now, I will show you in the next part when we will design our network, the whole network infrastructure through Terraform. At this point of time, you should just know that resource block is through which we can create these resources and resource name and arguments and the return type, we know from this registry itself. The last is block which I want to cover is the variable block. So variable block is like here, if you see till now the provider Okay, how to connect with the AWS? You can say that. The provider AWS, we have given the region US hyphen East hyphen 1. This is the value for this argument. Similarly, let's say while creating the subnet. So, the so subnet is a resource. So, resource AWS underscore subnet the name which you want to give to this block. Okay? It also required region argument. So, you have to use this. Similarly, private subnet I'm giving another AWS underscore subnet, but this one I'm giving the name private. And I want into the same region. All I wanted to tell that here if you see the value the same value is used in multiple blocks. Let's say in future you have to change from US hyphen East hyphen 1 to AP hyphen South hyphen 1. You have to change this value. So, you have to change in this block, this block, this block. And possible that all this block present into dot different TF file. I told you either you can have in the same TF file or different TF files. So, you have to make sure that you change this value all the places where it is used. And it is an error prone. That's where the variable block comes into the picture. So, all this value you can give certain variable, assign to that variable, and now in this one you can use that variable. Like this. So, this is again a dot TF file or within the same it's just like a variable block. Variable give any variable name you wanted. Okay? Any name. I just given AWS underscore region. Now, within this you can provide description, which is an optional. Now, you can have like type. Yes, this is an a string. This variable is a string. Type equals to a string. Default value is like US hyphen East hyphen 1. If you want to override, provide here itself. The reason equals to AP hyphen South hyphen 1. Okay? Otherwise, you can make use of this variable. So, I have created this variable. Type is a string. The value is US hyphen East hyphen 1. Default equals to US hyphen East hyphen 1. Now, here, if you see, in the provider, I am using var. So, it tells Terraform that look into the variable block. var. This is the variable name, AWS underscore region. Similarly, your resource, AWS subnet, region equals to var. AWS underscore region. Okay? So, now they are all using this variable, not using this value directly. Now, in future, if you have to change it, just change it here, only at one place. You don't have to change in all the block. Clear? So, that's where variable blocks are very handy. And it's very use very much frequently used. So, there are different types like uh you have a string, then you can use number type as number, integer, bool, false, true, list of type, means list of a string. Okay? Or like list of number, you can give. And then you have a like this. Like an array, and then you then you have a set of type, set of a string. It just remove duplicate, but ordering is not maintained. Map of type, like map of a string, like this, you can give key value pair within a block. So, these are the different types you can have, and like how their value you can create. So, with this, I have created a basic understanding of Terraform. And now, based on this, we can write whatever the infrastructure we have created, we can create our own and we Terraform files, and using that Terraform files, we will run it and create an infrastructure using that. Not through manually. See you in the next video. Okay, bye.
