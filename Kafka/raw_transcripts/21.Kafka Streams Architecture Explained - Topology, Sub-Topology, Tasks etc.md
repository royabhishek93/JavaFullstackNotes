Welcome back and today we are going to cover Kafka stream architecture. So in the previous video we have already seen what's the need of the Kafka stream.
Okay. So today let's go one level deep and let's cover the architecture before we do its uh understand how to create a stream and all.
The very first prerequisite is that we understand the consumer setup part because the Kafka stream reuse much of the Kafka consumer architecture itself. So if you are already cleared with this Kafka consumer setup and in the same I have also shared the Kafka consumer flow also the Kafka stream uses the same architecture also. Okay.
So that is the first and the second prerequisite is Kafka stream introduction like why exactly you need Kafka stream. Okay. So once this is clear let's start with Kafka stream architecture part. So first thing first if anyone asks what is a stream I would say that in simple term a stream has no start no end and events keep arriving.
So like order one order two order three order four it's never end. So that's what we call it as an stream.
Stream processing means processing events as they arrive continuously in real time. As when event come we are processing it not like a batch like we run at 3 p.m. and then we pick all the events and then processing it. No in real time as the events arrive it start processing it.
So Kafka stream is a client library for building the stream processing application.
So one thing very specific is that the Kafka stream is not uh like a cluster or infrastructure right it's just a jar file that's all once we add this jar in our application all this framework we will get automatically okay so it's
Discover more
Video player customization
financing
Language translation API
simply a jar file and we have to add it in our application okay now we know what is a stream the very important important part you can say that it's an heart of the stream the topology now let me stop it here and I will tell you once the context here like don't skip this part like if you are trying to understand the architecture don't skip this part because after that like even if you are proceeding with the syntax like how to create a stream and different processors in the streams without this it will not get clear and it is not very simple okay so I'll try to go little slow because from here itself now you can say that the real architecture of Kafka streams start so it's a logical flow of how data moves and get proceed from source topic to sync topics this flow is like a DAG directed a cyclic graph data flow in one direction no loops no cycle Okay. So what does it mean topology?
Topology means you can say that an assembly line in a factory. So you have a factory. So it's like a first raw material. Then after that different steps like you cut it, you paint it, you package it, then after that you will ultimately ship it. So you have a source, you have a sync.
Okay. And then you have processors like different different steps. So you always so it's a DAG directed a cyclic graph means you always you have a start point you have an end point and from a start when you always move forward there is no loop or cycle. Okay. You always move forward and ultimately reach to a sync.
Okay. So there can be why it is a graph because there can be multiple source there can be multiple syncs or there can be join also. I will show you. But in simple at this point of time we need to understand that topology is like logical flow of how data moves and get process from source topics to syncs topic. So here if you see we have a source let's say topic is order hyphen event. So from here we have to read it. So it's like a raw material in a factory example it's a raw material. Okay. And after that we have a processors which can have let's say multiple steps like cut, paint, package. Okay. And ultimately we have a sync the output topic like a shipping of a finished material.
So source is like an entry point read from one or more Kafka topics. No parent processor and source also you can say that here we decentralize bytes to key value pair forward record to its child processor. Okay. Now here is our actual business logic where transformation happens. Okay, you add filters, you add different groups. We will see that at this point of time we also don't have to worry about like what all different processors present and all because we are understanding architecture. We will see processors later. So we have a business logic here like multiple steps we can do. Okay. And you can say that we have when you have a step one, step two.
So whenever data moves from step one it do certain transformation and then pass it to step two. It do certain transformation pass it to the step three like this it data flows. Okay. And ultimately it reach to the sync exit point where record we write recall to the Kafka topic. Okay.
So this is let's say that for stateless processing stateless processing and we have a stateful processing. If you have uh seen this Kafka stream introduction, we have a stateless processing and stateful processing. So in stateful processing, we also store certain state.
Okay. While processing the record. So this is our source or raw material you can say that. So we have this source topic. From this source topic, we read event one by one and then we processes it. Our business logic can have multiple step. Step one, step two, step three, step four. Okay. So it it is possible that in certain step we have to save the state. Let's say we are reading total number of products. Okay. So product ID wise. So let's say we have to store product ID 1 then two then three. So we have to store it somewhere, right? So based on this for a stateful processing if you have certain process so it has processors are you can say that multiple steps you can say that multiple processing so if certain processing need to maintain the state it store it into the rocks DB we'll see that okay so this definition is clear logical flow of data which moves and get process from source topics to sync topic This is a DAC data flow in one direction. No loops and cycles.
So we define it once during application startup and after the Kafka stream executed forever. So topology is like we define it into our configuration and when your application start after that we can't change it. Okay. So after that this topology is used. We will see that.
But at this point of time just understand that during application startup it reads over topology.
So different examples of topology let's say simple linear topology. You have a source from source you get the data one by one. Then this is our processors like multiple steps you can say multiple processing filter let's say greater than 100. Okay don't worry about this for now. Just assume that these are different processings. Step step two. So filter then it maps ultimately goes to the sync and to the output topic. So this is linear. Then we have branching topology. The source from this source we will read event one by one.
And then we have multiple processing.
You can say that branch we have filter greater than equals to,000 it goes to this branch. If filter if your order is less than 1,000 rupees let's say it goes to a different branch okay so it's a step would be different and ultimately it will get sync it step would be different ultimately it will get sync multisource join topology you have multiple source then you will do a join and ultimately you sync it okay so this is just an example that I just wanted to mention that topology is not like a linear one like this simple it's not just like a linear it can have a branch it can have a join that's why I said it's a dag a graph directed or cyclic graph like it's but there would you will not find any loop or circle okay so that's the same thing I mentioned here at this point of time let's not worry about how to create a stream what are the different processors like filter map don't worry about this Okay. Uh how to put that result into the sync and all. So our agenda here is to understand the architecture. Okay. In next part we will cover these topics.
Okay. So focus on the architecture part.
Don't worry about again that okay filter how to define that filter or what all other processings uh are available.
So topology is clear. Right. Now the next very important part is task.
Read this once. It's a unit of parallelism. You can say that the small one task uh you can say that the smallest unit which cannot be break further. In a simple way I can say and it say that unit of parallelism. What does it mean?
Like when we say parallelism it's like we are running multiple tasks. Task one, task two, task three in parallel. Okay, within one task you can't run anything in parallel. That's right. It's a unit of parallelism. The smallest task or the smallest unit. Okay, so when we say parallelism in a stream, it means multiple task are running in parallel.
Okay, each task has complete topology. Means every task has this complete topology what we have defined. Means let's say this is my task.
Any event come okay? So this is picked by a task and it has a complete topology like source then you have a processors then sync. So that will get processed. Okay. Each task has a complete topology. Number of task equals to number of partitions. Each task run one partition. So take an example. I have a topic order hyphen events which has two part partition P 0 and P1.
So as per the definition we can have maximum two task task one task two.
Okay. So this task you can say that for each partition one task is get created. So and each task has the full topology. So this is topology source.
Okay. So this source is like topic order - events partition P 0 then the processors like different step one step two step three and if they have to if this task has to store something it maintain its own ros DB okay and then sync similarly we have task two it sources order events partition P1 the complete topology it's it has it has multiple processors step one step two step three it if It need to store certain information. It maintain its own ros DB and ultimately sync it.
Clear? So task is a unit of parallelism.
When we are saying parallelism in Kafka stream means two tasks are running in parallel.
Okay. But events within a task processed one by one.
Okay.
So now one thing is at this point of time who already works with Kafka stream at this point of time when I said each task has complete topology they might have got a doubt is it true is it like complete topology is hold by one task actually this is wrong okay I'll tell you why so I have mentioned here each task has complete uh topology actually this is not fully True. Why? Let's go one level more deeper. There is a concept of subtopology. Very confusing part. Okay.
And many times I see engineer miss this topic itself because it's not that easy to grasp. So I will show you. But this is where this line becomes [snorts] wrong like each task has complete topology. Now let's see that sometimes we need repartition in that case one big topology is internally divided among multiple subtopologies.
So to understand the topology we have to understand repartition what does repartition how repartition divide our topology into multiple subtopologies.
So let's understand this. So I am taking one use case. So we have order event.
Okay. So order event has let's say key order id and the value your order object.
What we have to do is we need to find out the product wise order count. Let's say product ID 1 it has total 50 orders total.
Product ID 2 it has total 100 orders.
This is our goal. Okay. And from the event you can find out not directly because the key is order ID. let's say one customer is C1 product ID is let's say P1 so for this P1 this order is for this product right so you can say that it will get counted for this okay so like this we have to find this is our goal so we need to understand the problem if there is no repartition first like there is no concept of repartition itself let's understand what is the problem so events are distributed across topic partitions. So for example, product P1 orders are distributed across two partition of topic order events. What does it mean? So now let's say in a Kafka cluster we have a topic order events which has two partition.
Partition zero, partition one. So partition zero will have certain events.
Okay. Similarly, partition one also has certain events. So now let's say our goal is that product wise count product product ID let's say product one let's say we need to count orders.
So now here if you see this event product ID P1 so this order belongs to product id P1 which is in present in partition P 0.
Similarly here if you see there is one more event which is again for product ID P1. So this order is also for the same product. So partition zero partition zero has two orders for product one.
Similarly partition one has product ID P1. So this order belongs to the product ID P1. So partition one has one order for product ID 1. Okay. So currently this is our situation because in uh topic a particular topic the events can be distributed among partition correct that we already know that now in the Kafka stream I told you we have different tasks. Let's say task one task two. So the number of partition we have that number of tasks we will have. So we will have let's say our topology is this source read from order event. So our topology first our topology is read from order events. This is our source. After that our processors processor is like group by let's say group by product id then you have to do count and ultimately we have to put sync in output topic that's our topology now let's say our each task hold this topology Okay. So this each task holds this complete topology. Now what would happen? You know that each task handles one partition. So our source is order event. So task one order event it handle partition zero. Similarly task two for order event handles partition one. Okay.
So they are both task are handling different partition.
Now this task will see when it do group by key product ID it will see that okay because it will read the partition zero. So when it will read partition zero it will see that hey for P1 there are two orders. Similarly task two will see that hey for P1 there is only one order. Now which one is true?
How to aggregate?
That's the problem.
Okay. So you got the problem. Now let's understand repartition. What repartitions? How repartition solves it.
So this is our topology. Okay. So now here if you see that the source is order event the processor in the processor let's say this is the one processing step which does repartition.
Okay. And after that again same group by key count and ultimately sync. Now what would happen?
See this how the task is created. So first it create topology subtopology.
So subtopology zero. We have a source.
Yes. Source order event. So source is order event.
Then the processor. So in the processor it has let's say select key product ID this one select key product ID. So this step is of repartition step. So what we are trying to do is that in order events topic we have multiple partition partition zero partition one. So we have events here. We have certain events here for the same product ID. Same product ID.
Now what we are doing is whenever we are getting an event we are saying that repartition based on product ID. So what it does is when it get an event particular event now which has let's say product ID P1 what it will do is it will again put into a topic certain repartition topic which has again the same number of partition P 0 P1 so this has let's say two partition order event so the same number of partition it creates. Okay. So it's a new this partition is created internally. We are not doing anything.
We just created this topology. This is in our hand. We don't know about like what internally how it is internally creating subtopology and all. It internally does that. But internal functionality I'm showing you. Okay. But this topology is which we created here.
We haven't defined any subtopology and all. We just defined in steps. We just told that hey repartition is based on product ID and after that next step next step like this but internally what it does is first it gives that okay source is okay then processor hey you want me to do repartition okay so what it will do repartition is that it will create a new topic which has the same number of partitions of the source Okay. So, P 0, P1. Now, in the repartition, it will do based on this product ID. So, now here when our producer publish an order event, the key was order ID, not the product ID. Order ID. Okay. Now, here during the repartition, our key is now product ID.
Okay. value is the same order whatever here it was but the key is changed now it is product ID now since key is same all this events will goes to only one partition either P 0 or either P1 okay so here if you see in the processor select key product ID so this is a repartition step and when a repartition step the it sync link is the repartition topic. Okay. So it will ultimately create a repartition topic and whatever the event is it will just publish them again but now with a key product ID. So that all these events now goes to one of the partition only not divide like earlier.
Okay. So this is subtopology zero. Then subtopology one is like read from source is now this partition topic from now from this part rep uh repartition topic it will read and then do the uh following step group by key count and the sync whatever the sync where you wanted to put.
So you got it internally it divides.
Okay. So whenever you want to do repartition it will first create or insert it into the repartition topic like whatever the event come. Okay. And based on the task which you have performed whenever do you want to do repartition it will just repartition based on your key which you have provided. So it will go back against to the certain topic and from there you have to read it again and complete the step.
Okay. So the same example I have shown here we just create a topology based on the processor. If repartition requirement framework internally automatically create repartition topic and create subtopology. I told you in know we just work with topology.
Subtopology is an internal concept.
That's why many times engineer miss it because we don't create topology.
We uh we don't create subtopology. we just only create topology okay based on the our processing or based on a step if that step is repartition step internally it creates a topology so see this example I hope with that everything will get clear now so let's say order event it has two partition partition zero partition one I told you that when the producer has published into this the key was order id value was order. Okay. So your order has process ID and all but since key is order ID partition zero has certain data partition one has certain data.
Okay. Now after repartition so repartition now key become your product ID P1 and then the same order. The key is now this. So for the key iss P1 order is this 01 C1 P1. Similarly the key is P1 when this event is get it will just republish it 03 C3 this one. So all this events ultimately when the task uh what it will do is it will do repartition it and during repartition what would happen is now because of the same key they will all goes to only one partition of a repartition topic.
So that when you further read it, okay, you will have a task, okay, you will have a task here on this also partition zero, partition one. So you have two task here uh which looks at partition zero, a task which looks at partition one of this repartition topic.
So only one task now will get all this events now. So it will able to see properly clear uh process ID let's say P - 1 count is three okay earlier it was two and one here now with the repartition this get resolved got it now let's comes to task again Okay, earlier I told you that earlier I told you that each task has complete topology. Now we have an understanding of subtopology also. So I would just rephrase that each task has complete subtopology for a specific partition.
So just see this example till here. So see this cluster and see this topology.
Pause it here. Divide this topology into subtopology and after that see how many task it required. Okay. So now let's say our Kafka cluster has only order events and uh this order event has two partition P 0 and P1.
And this is my topology which I have created that source is like order event and then this is the processor.
So in the processor the first it is doing is repartition based on the product ID group by the key. Third is count. So multiple step after that sync sync to the topic product or order count. So this is our output topic you can say that.
Now we know that this this step is the repartition step.
Repartition step. So we know that it will create multiple subtopologies. Can we create different subtopology?
This is one sub uh subtopology. This is second subtopology. Source is order event. Okay.
Processor select key by product ID.
Repartition happen at this step.
So it will sync into certain repartition topic. Okay. So now this will create a new topic into our Kafka cluster repartition topic which has the same number of partition which your source has. So source has P 0 P1 it will also have P 0 P1.
Okay.
Now subtopology one it will read from this repartition topic. It will read from this repartition topic. Now further step group by key count group by key count and the sync product order count.
So there will be some other topic it will sync it there. Okay. So how many task will be created? So this is also certain work we have to do. This is also certain work we have to do. So for let's say for this subtopology this subtopology has two partitions. Source is two partition. So task one subtopology zero partition zero task two subtopology zero partition one okay so subtopology zero sources order events right so it's has order event has two partition so subtopology zero means this is this has to be done in a particular ular task but partition zero. So whatever the sources order even it's partition zero.
Similarly like this okay so 000 here subtopology zero partition zero.
So source is order events partition P 0 and the whole topology processor select key sync is repartition topic. So this is one now subtopology right this okay now second is task 01 subtopology is zero partition is one so order events but partition is P1 okay the the rest of the subtopology complete subtopology select key and that will publish into repartition topic okay now let's comes to this subtop subtopology ology this source repartition topic also has now two partitions. So means it need two task.
So task one zero one is like subtopology one partition zero. So its source is so its source is repartition topic. So repartition topic partition P 0 repartition topic partition P 0 and processor rest all same. Okay, this all same similarly task one subtopology one partition one. So topic was repartition topic it's partition P1 it will read rest all same group by key count ultimately sync.
So you got it. So the now task number of task is like depend upon your subtopology. So for each subtopology it will check its source. So this source topic how whatever the partition it has that many task it will create. Okay. So this topology has order events. Order event has two partition. So task one task two for this one. Similarly this subtopology it source repartition topic has two partition. So for this also has task one task two for this one. This subtopology.
So task is created per subtopology.
Okay. It's clear now.
Now this four task will be distributed to our stream application instances. So whatever the spring boot application which we will create for stream processing these four tasks will be distributed among them. Let's say I am creating a spring boot application with thread two mean two threads I'm creating. So now this four task will get distributed among this two threads. So thread one thread two. So this four task is distributed among this two threads. This four task distributed among this two threads.
Okay.
So at this point of time now you got one doubt regarding rag DB. So you might be thinking first is like what is DB?
Okay, by the diagram we'll see that if we reconnect the dots from start each task. So here the uh last diagram you see each task if you see each task the smallest unit each task maintain its own ros DB.
Okay each task maintain its own ros DB.
So we have four task four task maintain its own rocks DB.
Okay.
So, and you know that Ross DB is nothing but a key value file stored in local disk.
So, Rox DB is nothing but a key value stored in a file in your disk.
So now the question might have come that okay so this task is maintained its own rog db file this task is maintained its own rog db file and this two task is running in this instance instance one. Similarly task three task four is maintain their own rock db files and they are running into this thread and these two threads are running into this application. What if this application get died?
So all this rogue db file will get because it's on a local disk right so this data get lost because see once your machine get die this task will be assigned to some other machine okay rebalance will happen so okay earlier your machine one where your instance was running so they has like task one task two task three task four now this machine dies is so a new machine would be assigned this task one task two task three task four but the question is that each task maintains the ros DB local file this maintain its own ros DB this maintain it but and it's there in the machine one if the machine one is died how they task is moved to another machine but what about the data which is which was present into the local disk how it will get moved.
So the answer for this is that the change lock topic there is one more topic like whenever we insert data into the Rox DB okay it will also publish an event to this change lock topic so that even when the machine goes down and the task moves to another machine they will fetch there is a change log topic From there it will fetch and rebuild the rocks DB.
Okay. So now see this complete diagram things will get clear now. Okay. So earlier Kafka cluster is you have let's say order events partition zero partition one. Correct.
So now based on your topology let's say multiple subtopologies created four topologies created right so let's say four task created task so you have just to recap you have topology you have created two subtopology was created subtopology zero subtopology one okay so it put into repartition topic it will read from repartition topic and ultimately put into the sync Okay. So now we have four tasks. So the same thing like we have task one, task two, task three, task four. The same thing here I have written just so task 000 means subtopology zero partition zero. So subtopology zero. So subtopology zero was like read from order hyphen events but it's a task right. Partition P 0.
Similarly, subtopology zero partition one source is read from order events but partition P1. So it's a different task and the rest of the sub subtopology processor select key sync. Okay. Like similarly we have subtopology one partition zero. So subtopology one says that read from repartition topic but since it's task so it's partition P 0 similarly another partition P1 so here if you see it will sync your subtopology your task 00 now can you read this when I say task 00 what does it mean task 00 means subtopology task which is associated with subtopology zero partition zero So subtopology zero you know that it's uh read from order hyphen events partition P 0 now okay and ultimately it sync into repartition topic now repartition topic here if you see has two same P 0 P1 so in which one it will store it it depend okay because you will publish based on the product ID the key is your product ID so whether it will go into P 0 or P1 depend Correct.
Now see this one I told you that it also has a change lock. So now let's say this step this task is also has certain just assume that certain step which touch the ros DB which is stores into Rox DB.
Okay. Similarly the subtopology one also has certain step which tells the ros DB.
So now you know that there will be change log present for this.
There would be a change log present for this also.
Okay. So now let's say for the subtopology zero I have given the rock DB name is let's say order hyphen store. So a change log order store hyphen change log will get created.
Okay. And it has the same number of partition like your source has. So your source order event has two partition right? P 0 P1. So this order - chain log will also have two P 0 P1.
Okay. So this task 0 0 means subtopology zero partition zero. So it will sync where it will sync into P 0.
The change log it will put into P 0 because it is processing the P 0 events.
Okay. Similarly this subtopology zero partition one. The rock DB is let's say order store. So its change log would be order - store- change log and in which change log partition it will publish an event whenever any certain event uh is certain data is updated into log db it will publish an event to the order hyphen store hyphen chain lock but partition p1 here so this will touch partition p1 change lock now subtopology one now subtopology one let's say the rock db is counten store.
Okay. Similarly, it has partition P 0, partition P1. So, this ROG DB whenever any change happen, it will publish an event to count - store- change log and it will touch its partition P 0.
Similarly, this subtopology will have log DB count log, but which partition it will touch?
P1.
Okay. So all I just wanted to show that like even though the rocks DB is local stored on a local but it will also publish an event which ultimately stored into the Kafka cluster and there is a change log topic itself in that it will get stored.
This is clear.
Don't worry about this if you have any confusion at this point of time.
agenda is that we know the high level we know like what is repartition topology subtopology ros DB okay and uh all this let's say complete end to end picture should be in mind so now we know the architecture of Kafka stream let's see the complete flow like a Kafka consumer so now you will see that okay the same flow which we have seen in Kafka consumer mostly the name.
So step one now let's say our stream application got started. I have given a group id stream analytics.
Okay. So what it will do?
It will find out the consumer offset like the partition number of the topic consumer offset. So you know that there are 50 partitions of consumer offsets.
Okay. So it need to find out that okay whatever the group ID I have given who like which partition is my group coordinator like so first it will find out that okay hash of your group ID mod 50 so 23 so partition so it need to find out that okay the partition 23 will hold all the information related to my group and it just need to find out that who is the group coordinator for this partition 23 exactly same as the kafa consumer part. So it will send a request to any broker. Broker will goes to the controller. Controller will take out the metadata. Metadata return will have that okay consumer offset partition zero leader is broker one. Partition two leader is broker three. Similarly like three partition 23 broker is leader is broker three. So it gets certain information.
So after that it will send the request to the broker three that hey I want to join the stream analytics group everything same. Now here very something different happened than the consumer. Now the group coordinator wait for all the members to join. Okay.
So group coordinator will wait for all the members to join. Let's say uh there are let's say 10 instance, 20 instance whatever the instance is. Now what it will do is that they will run the streams partition assigner. If you remember in the consumer what happened was let's say you have multiple instance of consumer consumer one consumer two consumer three they all wanted to join let's say ordery events and order events has let's say two uh three partition partition zero partition one partition two so this partition will be assigned to one of the consumers but now in case of stream I told you that the task is the working unit. Okay.
So what it does is it runs this stream partition assigner. So this discover topology create subtopology create task. Okay. It internally create repartition topic change lock topic whatever the things required.
Okay. And then distribute the task to the stream instances.
Okay. So now let's say it says that whatever the application you are saying that hey you have to handle task 0a 2 means subtopology zero partition 2.
Okay got it? This is one difference. So it does lot of things that discover topology subtopology task creation and create change lock repartition everything.
Now you got the task which you need to run. Okay. So now it will check okay I need.
Okay. So subtopology zero partition 2. So it know that sub from subtopology zero it knows that the subtopology zero it source is ordery events. Okay. So now it will say that okay for order events partition two till what offsets it is read properly. So it will just ask that hey f the last offset for the order event partition two like similarly like a consumer. Okay so it will say that okay till 100 it's all read you have to proceed further.
Same now one thing another new is that you every task maintains a ros db. So now since it's a new application it need to restore that ros db. So it will read its so for this order event. So this subtopology so subtopology zero partition two it will read or recover the change rox db from the change log.
Okay. So let's you can say that subtopology let's say zero partition p1.
So it will know that the rock dB the name is order - store because in the topology we have given the name when we have defined the topology we have given the name order - store. So order - change log which partition P1 it has to read order store change log from partition P1 it will recover. Okay. So it has partition two.
So it will read partition two. Let's say it will recover the rocks DB. Okay. Now what it will do is it will invoke the leader and fetch the data because it is now has to fetch like ordery events partition to data. Right? So it will invoke the broker read the data one by one. Okay.
So now let's say it got the data.
Now it will process it. So task has all the subtopology right. So it will process it. So like this one.
Now all the task which has to do it will do.
After that it will commit the same like a consumer and continues to the step nine. So the again fetch the next event process the task commit it continue again exactly same as consumer. If you have understood the consumer architecture this is the same flow only difference is that the group coordinator how the earlier partition was divided uh among the consumer instances. Now task is divided among the stream instances.
Okay. And it also has one more step where it will recover the Rox DB state from the change log. After that everything is same like it once it get the event the logic which has to be processes within present in the task itself. So every task has this subtopology it has to process that this steps clear. So I would say that go through it once again because things get little complex in this one like understanding subtopology uh understanding this change log. So go through it one more time and try to connect dots. Okay just see that like all we do is that we create topology.
Okay from the topology internally happens everything it creates subtopology internally.
Okay. It divide this topology into multiple subtopologies internally.
Got it?
So any doubt let me know because in the next step I will just set do a setup of this stream and once we are done with the setup then we will do like go depth into the processors like what all different processors we have filter group count and etc.
Okay but I hope this stream architecture is clear because that is critical. Okay guys, thank you. Bye.